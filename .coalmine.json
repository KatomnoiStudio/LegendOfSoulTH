{
  // Project override — see ~/.claude/.coalmine.json for full key documentation.
  // Docs-only paths never carry bug-risk/dead-code/resource-leak issues rot-canary looks for —
  // exclude them from auto-scan scope so touched-file budget goes to real source instead.
  "scanExcludePaths": [
    "**/*.md",
    ".agents/rules/**",
    "README.md",
    "AGENTS.md",
    "MEMORY.md"
  ]
}
