# MEMORY archive — items 201-225

> Bodies for MEMORY.md index entries 201 through 225. Same rules as the earlier files in this
> directory: item bodies are stored verbatim and `MEMORY/archive` is in `.prettierignore` so that
> ordered-list markers are not re-padded (see `tools/verify-memory-archive.mjs`).

## 201. The sprite pipeline's first two lanes landed, and main's own routing was the only thing that broke

Two of three dispatched lanes returned DONE and are on master; the third returned BLOCKED, and the
blocker was main's dispatch, not the problem.

**Main dispatched three agents into one working directory and did not take `.agents/workdir.lock`** —
one command after releasing it. AGENTS.md rule 21 exists for exactly this. The #100/#106 agent read
`WukongAdventure.tsx`, found the same lines rewritten under it minutes later, and refused to write
anything: #100/#106 and #107 live in the same file with no path-level split, so staging would have
committed another agent's unfinished work (rule 8) and `MEMORY.md`/`TASKS.md` are the two
highest-contention files in the repository. **Refusing was the correct behaviour, not a failure.**
Main corrected mid-flight by messaging both live agents to leave the ledger files alone and taking
them back as sole writer; both complied and both commits are ledger-clean.

**#107 — the preload burst (`826a0c5`).** `preload()` fired every frame at once: measured live at 96
requests inside one second starting t=748ms, and 144 requests for 97 distinct URLs. Now a 4-deep
queue that advances on `load` **or** `error` so one 404 cannot stall the rest, a `fetchPriority`
hint, a module-level requested-set, and fetch order flipped to idle → turn → walk, the order the
scene actually draws. The change that matters most is structural rather than numeric: the idle
sampling now lives in **one** function that both the renderer and the preload list call, so the
fetched set can no longer drift from the drawn set. Measured on the shipped files: 96 → 80 files per
character, re-fetch 1.48x → 1x by construction, monkey-king −462.8 KiB (−18.6%), pig-warrior
−753.2 KiB (−28.7%). The agent explicitly declined to claim a memory saving, because 98.18 MiB is a
decode ceiling and not a measurement, and declined to claim a re-measured live timeline, because a
browser is the only instrument for that.

**#101 — the frame contract (`aceb6c5`).** 579 lines, 18 tests, one `sharp` decode pass over all 359
frames. Four invariants: one canvas per prefix (not "all 640×512", false for 126 of 359 files); every
pinned path resolves and counts match **in both directions**, since too many is art the code loads and
never draws; `TURN_INDEX` and `DIRECTIONS` parsed from component source **at assert time** rather than
snapshotted, with battle order asserted ≠ turn order; and foot-line spread per animation kind against
the design lock's bands, with `inDir` and `xDir` never merged.

**Two disciplines in that work are worth copying.** The instrument was validated *before* a single
assertion was written — it reproduces every number already published in `SPRITE-CONFORMANCE.md`
(monkey-turn 51.00, tripitaka-turn 31.00, pigsy-turn 12.00, pigsy-walk 5.63/3.69%, spear-warrior 0).
And each invariant was **proven to fail and then restored byte-identical**: a frame resized to
400×376, a file renamed away, `left`/`right` swapped in TURN_INDEX (against a scratch copy, because
another agent was mid-edit in the real file), and one frame shifted 5 px down on an *unchanged*
canvas — the last proving the anchor gate detects drift rather than a canvas side effect. #107 did
the same by mutation: three defects reinjected, each confirmed red, each reverted. The red tests main
saw mid-flight were that, not breakage.

**The four failing groups land as named exceptions, and the anti-rot is the interesting part**: the
failing set must equal *exactly* those four, so a fifth reddens and fixing one without deleting its
row also reddens; every shipped group must be either contracted with a kind or explicitly unscored;
and each exception's measured pixels are pinned so an already-failing group cannot drift further
behind its own exception.

**Three judgment calls, all ruled correct by main.** Foot centre is measured but deliberately **not**
gated, because the tolerance register publishes no ceiling for it and inventing one would promote a
Layer C value to spec — the lock's own first standing rule forbids that. The unscored list is
**four**, not the two ordered: `aura`, `hound`, plus `monkey-pose` and `tripitaka-buddha-aura`,
none of which the conformance record classified either, and each carries its reason inline rather
than a guessed kind. And `monkey-attack-new` passes with **0.19 pp of margin** (22.81% against 23%),
which is real and fragile — one re-encode could flip it, and that is now recorded rather than
discovered later.

**Still open from the blocked lane, whose design is finished and numerically proven:** #100/#106
closes both rows together — size spread monkey 75.1% → 0.03%, pigsy 64.1% → 0.55%, erlang 46.8% →
0.09%, tripitaka 8.0% → 0.02%, and feet landing on the anchor at 356.0000 box units for all ten
families against 387–411 today. It needs re-dispatch against a clean tree. That agent also returned
three findings the brief did not have: the L1 provenance obligation is **three** sites, not two —
`CharacterModel.tsx:207` pins the same 1.2505 landscape aspect on `tripitaka-buddha-aura.webp`,
which is 1194×1317 **portrait**, a 37.9% mismatch and double the recorded character-mesh figure; the
provenance L1 demands is **unrecoverable from version control**, since `git log -S'4.018'` bottoms
out at a squashed initial commit and 4.018/3.213 sits 0.04% from 640/512 and 0.02% from
`ENTITY_SPRITE_ASPECT` so the code cannot distinguish them — which is L2's "record it somewhere
other than history" failing exactly as the lock predicts, and needs the owner rather than
archaeology; and after the fix `spear-warrior-stop-turn` becomes a ×1.091 upscale, pushing the P2
worst-case resolution floor from the recorded 2.68× to **3.40× at DPR 3**.

**One transient worth recording because nobody could explain it**: a tracked sprite,
`public/characters/pigsy-team-7.webp`, showed as deleted at 18:36 and had restored itself by 18:39,
attributed to no agent. Main verified afterwards that all 359 tracked frames are present and
non-zero. Three agents were writing one tree at the time; that is the most likely explanation and it
is another argument for the lock discipline rule 21 already mandates.

## 202. The sprite table was already right; the two consumers that never read it were the bug

`aaf57b1` closes `#100` and `#106` together, and the interesting part is how small the fix turned out
to be once the standard existed.

**`E3` did the work before the code did.** Locking "world size is DERIVED from texture pixels, never
hand-authored" (`a178e8e`, cited to Unity `Sprite.pixelsPerUnit` and Godot `SpriteBase3D.pixel_size`)
reclassified the task from *design a mechanism and tune numbers* to *extend a table that already
exists to the consumers that never read it*. The reclassification held: the fix is a 6-line exported
`deriveSpriteSize()` plus five measured calibration rows. `entitySpritePresentation.ts` had been
running the international convention under a local name since before the lock was written —
`pixelsPerCanonicalHeight` is `pixelsPerUnit`, `bottomInsetPx` is the per-sprite pivot `E1` demands,
in the same record. **23 families registered now, up from 18.**

**What changed at the two sites.** `LobbyScene/CharacterModel.tsx` lost all three hand-typed
`planeGeometry args={[4.018, 3.213]}`; the scene now types one number, `LOBBY_SPRITE_HEIGHT = 3.213`,
and the `<group position={[0, 1.393, 0]}>` wrapper is gone because each mesh's own y puts its feet on
`y=0`. `AdventureScene/WukongAdventure.tsx` no longer lets `object-fit: contain` decide size. The
worst single site was the one nobody had counted: Tripitaka's halo, a 1194x1317 **portrait** sheet
pinned to a 1.2505 **landscape** plane — 37.9% stretch, and no `L1` provenance row anywhere.

| | before | after |
| --- | --- | --- |
| size spread — monkey / pigsy / erlang / tripitaka | 75.1 / 64.1 / 46.8 / 8.0 % | 0.03 / 0.55 / 0.09 / 0.02 % |
| lobby idle-to-action pop | 27.4 / 31.8 / 7.4 % | 0.18 / 0.16 / 0.03 % |
| declared feet, 10 families | 386.1-408.8 | **356.0000** |
| true-alpha feet | 22.7 px band, 30-53 px *below* the anchor | 2.16 px band around it |

**Three things worth keeping, none of which are the numbers.**

**(1) An independent re-measurement is cheap, and it paid.** This dispatch re-derived every figure the
blocked dispatch had produced — all eight turnaround values, the four size spreads, the 1.091x upscale
— with its own alpha scan, and reproduced them to the unit. It also rediscovered the normalisation
rule on its own by reproducing a registered row exactly. That is why a fresh agent could safely trust
a design it had not done. Re-measuring is the cheapest form of trust available.

**(2) The cost got reported, not buried.** Deriving size makes walk frames render at 93% of source
instead of 53%, so the resolution floor got *worse* — and the conformance record says so in a table.
The stated 3.40x at DPR 3 turned out to be **unreachable today**, because the 1.091x upscale belongs
to `spear-warrior-stop-turn` and the adventure scene filters to characters with walk frames, which
erlang and tripitaka do not have (`walkKits.ts`, `walkPrefix: null`). Worst *reachable* floor is
2.68x -> 2.89x, every reachable state still a downscale. The 3.40x arrives the day erlang gets his
other seven directions. **A fix that lists only what improved is a fix nobody can audit.**

**(3) The mutation-proof discipline found a gap it was not looking for.** Six proofs ran red-then-green
with SHA-verified byte-identical restores. A seventh mutation was injected and stayed **green**:
re-adding `inset: 0` to `.sprite` un-anchors the feet, because the inline `width`/`height` win but
`inset`'s `top: 0` over-constrains and defeats the inline `bottom` — and jsdom never evaluates CSS
Modules, so no test can see it. Disclosed in the conformance record's FUTURE section rather than
quietly left, alongside two siblings: nothing asserts `CharacterModel`'s geometry (no WebGL context in
tests), and nothing stops a *new* consumer from typing its own numbers.

**The gate that was added cannot pass by agreeing with itself.** Invariant 5 in
`src/game/spriteContract.test.ts` (22 tests now, was 18) compares the **declared table against
alpha-measured art**, not against another constant — the precise defect class this project was burned
by four times the same day. Band 2%, labelled Layer C; measured worst 0.86%. All four pre-existing
`KNOWN_FAILING` exception rows still measure exactly what they recorded.

**Left deliberately unfixed, and named:** three registered `bottomInsetPx` read ~2 px shallower than
the art (`monkey-v2-idle` 11 vs 13, `monkey-v2` 34 vs 36, `monkey-walk` 62 vs 63.72; erlang and pigsy
agree exactly). Changing them moves the battle scene — a different topic on the same table, and rule
16's one-topic discipline says it is not this dispatch's to take. It is the entire residual: feet land
1.6-1.7 px above the anchor instead of on it, consistently.

**Nothing here was observed in a browser.** The `+81.5%` and the knee-height shadow were MEASURED-LIVE;
their fix is arithmetic over an alpha scan, and it is labelled that way. `npm run ci` fully green —
1119 tests / 121 files, verified independently by main rather than taken from the dispatch's report.

## 203. v0.19.0 shipped from a changelog that had stopped tracking its own work

v0.19.0 cut with `[Unreleased]` empty against 14 commits — the changelog had stopped tracking the work before the release, not at it. Entry written from the log, four version copies moved together (the test suite caught the one that was missed), and three things published as open rather than carried: `L1` in the character roster, a resolution floor that got _worse_ as the price of the size fix, and three foot offsets ~2px shallow. Standalone-repo work the same day: `HetCreep/SpriteDesignDatum` (public, v2.0.2, CC BY-NC-ND docs + MIT tools) — the design lock rewritten as an external standard, cited by tag permalink because the GitBook copy serves 0 of 13 rule anchors.

## 204. The whole open task ledger audited against the code instead of re-read and believed

The whole open task ledger audited against the code instead of re-read and believed — 59 rows, 8 parallel readers, then a refutation pass that attacked all 38 closed-or-moved verdicts and **broke 12 of them**. Result: 7 rows were work already done, 29 carried numbers that had moved, 4 were never code work at all, and 18 were accurate. The refutation pass is the finding: one auditor was about to close an unbuilt security item (`F6`), another was about to overwrite a correct count with a wrong one that differed only by a trailing-newline convention, and a third called a partial documentation fix complete. Full record with per-row evidence: `docs/TASK-LEDGER-AUDIT-2026-08-12.md`.

## 205. The first rule this repo has removed

**The first rule this repo has removed.** A memory wash found `AGENTS.md` rule 21 claiming HetCreep "asked for" the working-directory lock while a same-day memory recorded them saying it was unnecessary — the same incident, two records. Measured before deciding: `.agents/workdir.lock` was never created once in four days, no code read it, and no agent had followed it, including in this session's own three commits. The owner retired it: the collision is controlled by the operator switching between sessions only after a turn ends, which needs no agent to volunteer obedience. Number retired in place rather than renumbered, so rules 22/23 and the range references that cite them stay true. If collisions return the answer is `git worktree add`, enforced by the filesystem, not another advisory file.

## 206. A scan's findings that never reached the repo, and the narrowing that produced them

**A scan's findings that never reached the repo, and the narrowing that produced them.** The session's defects were filed into a session-local task list and nowhere else — `TASKS.md` (rule 16's single source of truth) never saw them, so three HIGH production defects were one session-end away from being lost: every Google-OAuth and guest account is issued a UID that fails `isValidUid` and cannot be typed into the friend search, with no write path that ever re-issues it; a stalled battle-texture preload leaves the player on a loading screen with no timeout and no exit button, when the sibling error branch has one; and `tools/remove-stray-alpha-artifacts.mjs:94` writes `sharp().toFile()` back onto its own input under `assets/raw/`, so a crash mid-write destroys unreproducible source art. All three are now in `docs/OPEN-FINDINGS-2026-08-12.md`, none are fixed. The file also records **why its coverage cannot be trusted**, because the same run would otherwise read as a sweep: every canary prompt carried a hand-authored "already filed, do not re-report" list, which has no sanctioned form — `rot-canary`'s only permitted narrowing is the Freshness cap, and that lowers the tier, never removes targets — and each dimension ran exactly one round when the convergence test is 3–5 rounds ending in a dry one. The cost argument behind the narrowing was never computed. Two claims corrected in the same pass: the "59 findings" figure quoted during the session was the ledger audit's **row count** (item 204), not a canary yield, and filing a defect is not fixing it.

## 207. Eight instruments, 29 agents, and the finding that the rules were the reason the other findings survived

**Eight instruments, 29 agents, and the finding that the rules were the reason the other findings survived.** A CoalBoard run (nasa/L3, two waves: `supabase/` and `src/game/realtimeBattle/`, four blind lenses plus an out-of-frame observer each) followed by seven CoalMine canaries and a four-dimension gold-standard pass. Deliberately **not** narrowed — the counterpart to item 206, whose own header records that its canaries carried a hand-authored do-not-re-report list. That difference is most of why this run found what it found. Report: `docs/AUDIT-2026-08-12-v0.19.0.md`, whose §0 states the ceiling before the first finding.

## 208. The archive law was two days overdue, and the purpose it serves got sharper too

`agent-memory-law.md`'s index+archive shape has stood since 2026-08-10 — every item's body to `MEMORY/archive/`, its index line to root `MEMORY.md`, same commit. It was not being followed: items 203-207 sat as full bodies in root for two days, including one written an hour earlier in this same session. HetCreep, mid-redesign of `.agents/` from its first-ever version (`bee3e3b`, 2026-08-05 — three mandates, no RULES_VERSION, 529 characters against the 15,581 this file now carries), drove two changes at once: mandate 2's stated purpose sharpened from a generic "update with past/present/decisions" framing to an explicit test — `MEMORY.md` exists so the *next* session knows how to continue, not to log what happened — and the overdue sweep was run, moving five items into `MEMORY/archive/201-225.md` with root reduced to index lines. Verified byte-for-byte against the pre-surgery root text before anything was written, since `tools/verify-memory-archive.mjs` is itself broken for this exact heading format (item 110) and could not be trusted to catch a mistake here. `RULES_VERSION` 32 → 33.

## 209. The Ring hierarchy is gone, and so is the machine-local workflow system — two pieces of machinery that only ever commanded one machine

HetCreep, continuing the `.agents/` redesign from its first-ever version, cut two constructs in one pass after asking which rules work only on this machine yet cost every machine to read. **Ring 0 / Ring 1 retired** (`AGENTS.md` rule 6, retired in place — 35 files carried the vocabulary, ~25 of them only as provenance stamps in balance config and migration headers): the tier ranked the owner's live word above the written rules, which was answering a question nobody had — their word is where the rules come from, not a competitor to them. Owner's framing: "ฉันเล่นของแรงไปแล้ว ถือว่ากฏสั่งฟรี." `ring0-authority.md` deleted, the session ring check dropped from §0, `ring0-traffic-control-law.md` renamed `merge-control-law.md`, and the vocabulary stripped from six more rule files plus `CODEOWNERS`. **Every mechanism survived** — PR-only code intake, the owner as sole merger, the fresh-tip re-check that closes the PR #19 silent-regression class, the owner-locked claim protocol; only the tiering is gone. Two clauses that were load-bearing were relocated rather than dropped: "conform the code to the rules, never the rules to the code" and the rule-precedence chain now open `AGENTS.md`'s ⚖️ section. **The machine-local workflow system moved out of the repository**: its law and its seat roster to a gitignored local directory, its seat definitions untracked in place. Measured cause — 632 tracked lines commanding one seat on one machine were auto-loading into every session on every machine, and none of it functions where Claude Code does not load `.claude/agents/`. Its installer, carrying the full mechanism, the registry template, an install-complete checklist, and a verify-the-data-before-deleting checklist — the registry accumulates hired session ids and lazily-written system owner rows that exist nowhere else, so a delete without that check is data loss, not cleanup. Rule 23 shrank to the installer pointer; the two clauses that ever bound anyone beyond that one machine already live in rules 16 and 18. `RULES_VERSION` 33 → 34.

## 210. The load layer split in two, and the shared half was checked for tools nobody else has

Item 209 moved that machinery out of the repository; this pass generalized the reason into a rule. **`AGENTS.md` + `.agents/rules/**` are the SHARED layer** — every sentence written so a dev on Cursor, Codex, Antigravity or plain git can act on it with none of this machine’s tooling installed. **`CLAUDE.md` is the LOCAL layer and is now gitignored**, holding what a machine loads on top of the shared law: its agent tooling roster, the roles it plays that others do not (merge controller, sole writer of `TASKS.md`’s claim columns, release train), and pointers to whatever machinery it has installed locally. A tracked one-line `CLAUDE.md.example` seeds a fresh clone, so untracking `CLAUDE.md` cannot silently stop the shared law from loading anywhere. Measured before deciding: **eleven** tool-name mentions survived in the shared layer after the de-ringing pass — `CoalBoard`/`ask-CB` × 8, `/gold-standard` × 3 — and reading every one of them showed **zero were prescriptive**; all were past-tense provenance (“found via a 4-seat opinion-lane sweep”), so the fix kept the provenance and dropped the tool name rather than moving the sentences somewhere they would be orphaned from their rule. The `<!-- coalmine: verified … revalidate 90d -->` stamps were deliberately left: they encode a real re-validation date, render invisibly, and cost a reader without that tool nothing. §0 now carries the test that keeps the split from eroding — can a dev with none of your tools installed act on this sentence? — with the yes/no routing to each layer. `RULES_VERSION` 34 → 35.

## 211. Finishing the removal — the vocabulary was still in forty files

Item 209 moved the files; it did not finish the job. A word-boundary sweep across every tracked file found the system’s vocabulary still in **40 of them** — the `(caretaker memory)` header on ~20 per-system `MEMORY/NN-*.md` files, both `MEMORY/` READMEs, `agent-memory-law.md` §1a’s audience table, `TASKS.md`, `CHANGELOG.md`, `assets/ATTRIBUTION.md`, `tools/test-lock.sh`, an applied migration’s comment, the dated audit docs, and the archive bodies. **The owner’s reason for finishing it rather than tolerating the residue**: a system an outside agent cannot run, described in files that agent auto-loads, gets planned around and imitated badly — “วางแผนไม่ดี แล้วลอกการบ้านเพื่อทำตาม ระบบมีแต่พังกับพัง”. Method: four scripted passes with assert-then-write, longest-match-first so a general rule could never eat a specific one, each followed by a grep that had to shrink; then hand-rewrites for the five files needing real prose (AGENTS.md rule 23, `agent-memory-law` §1a, both READMEs, the archive item headers). **Facts kept, names gone** — what ran, what failed and what was ruled all survive in the archive; only the system’s name and its seat vocabulary were removed. One verbatim Thai owner quote was dropped rather than reworded, because the quoted phrase WAS the term being removed. Rule 23 retired in place alongside 6 and 21. The installer left the repo as well — keeping it was itself the homework an outside agent would copy. `.claude/agents/` and `.claude/local/` are now gitignored wholesale, so no future file there can leak by naming. Final sweep: **zero**, the only match left being `belt-scroller` in a Streets of Rage citation — the beat-em-up genre term, a different word. `RULES_VERSION` 35 → 36.

## 212. Per-system memory left the repo; the 211-item record stayed

The owner asked for `MEMORY/` to be gitignored. Measuring first changed what the order meant: the directory holds two things that are not the same kind. **`MEMORY/archive/` (10 files) is the shared project record** — root `MEMORY.md` is an index of 211 numbered items and every one of those lines ends in `— MEMORY/archive/NNN-MMM.md`, so ignoring it would have left 211 dangling pointers and made `AGENTS.md` rule 1 (mandatory read) plus `agent-memory-law` §1 (“open the archive file the index names”) unfollowable for anyone but this machine. **The other 20 files are per-system working notes** — theirs, not the project’s. Owner picked the split: ignore the per-system half, keep the archive. Implemented as `MEMORY/*.md`, depth-1 only, which cannot reach `MEMORY/archive/*.md` because `*` does not cross a slash — verified with `git check-ignore` on both sides. The change created five dangling references it had to clean up after itself: `accountRepository.test.ts`, `progressionCostAuthority.integration.test.ts`, `20260810180000_p26_progression_cost_authority.sql`, `ErrorBoundary.tsx` and `check-bundle-size.mjs` each outsourced an explanation to a per-system memory file a clone will no longer have. Each now states the reason inline, which is what a code comment is for; pointing at a file the reader cannot open was the weaker form even before it became impossible. `agent-memory-law` §1a and `MEMORY/archive/README.md` updated to say plainly that per-system notes are gitignored and that a finding which matters to anyone else belongs in the tracked index+archive pair instead. `RULES_VERSION` 36 → 37.

## 213. The docs index, and the orphan that was holding a live gate

The docs-health audit had named the gap: `docs/` has no index, and two zero-inbound orphans. Writing the index meant first measuring what each of the 13 top-level documents actually is — inbound link count, size, and whether it still carries OPEN or HELD state. **The measurement nearly cost something.** `docs/pvp/P12_VERIFICATION_REPORT.md` scored zero on both axes: no inbound links, no open-markers by grep. On those numbers alone it was the clean delete. Reading it first — the rule that says look at a target before removing it — showed lines 52-57 hold the explicit graduation gate for private PvP: it stays at 90%, not 100%, until the migration and Edge Function are deployed and two real signed-in clients complete two clean rounds. That is the done-criterion for a task that is still open and blocked on the owner. **The zero-inbound count was the defect, not the evidence** — a live gate had simply never been linked from anywhere. It is now cited from both PvP rows in `TASKS.md`. The index itself sorts documents by the only question that decides whether to read one: **does it carry open state?** Authorities that govern work (`MASTER_BLUEPRINT_v3.0`, the sprite lock and its conformance record, the 29 per-system contracts) · things waiting on a person (three unfixed HIGH defects, the HELD blueprint-check findings, six asset-provenance questions the owner alone can answer, the PvP gate, the art brief) · finished records kept as provenance and stated plainly to instruct nobody (the ledger audit, the v0.19.0 audit, the sprite-format experiment, the design-lock handoff, the hero batch). Verified: every top-level doc is linked from the index, all 14 link targets resolve on disk, and the index is reachable from root `README.md`. **Nothing was deleted** — ~360KB of audit records looked like the obvious cut, and the one file that scored most deletable was the one holding a live gate.

## 214. Two records of one day, merged into one

The v0.19.0 audit already opened by pointing at `docs/OPEN-FINDINGS-2026-08-12.md` and saying **that file’s version of the shared finding was the better one**. Two documents recording the same day’s defects, in the same directory, one deferring to the other: that is one record pretending to be two, and it is exactly how the un-deferred half goes stale without anyone noticing. Folded in as §0b, source deleted. **Both coverage ceilings are kept side by side rather than blended into one softer sentence** — the audit’s limits (2 of 9 board waves flew, one round, not independence-clean, nothing executed live) and the earlier scan’s different and worse one (pre-filtered by a hand-authored exclusion list with no sanctioned form, one round). A merged claim would have been weaker than either. **The three HIGH findings became rows 34-36 in `TASKS.md`** — malformed public UIDs on every Google/guest account, the battle-loading dead end, and three tools that overwrite their own source — each with file:line evidence and a named fix shape. That is the point of the merge: item 206 recorded that findings never reaching `TASKS.md` was the defect, and a report is still not a work queue. **One real error, caught and corrected**: the first splice used `String.replace` with the merged text as the replacement string, and that text contains `^[1-9][0-9]{9}$` followed by a backtick — JavaScript read `$` + backtick as the “everything before the match” pattern and spliced the entire file header back in as a duplicate §0. Found by counting section markers rather than trusting the script’s own success message, reverted with `git checkout`, redone with a replacer function. Every inbound reference repointed: `TASKS.md` ×3, `docs/README.md`, `MEMORY.md` ×2, and three self-references inside the audit. The two surviving mentions of the old filename are deliberate — both are statements about what was true during that run.

## 215. The queue was leaking, in 52 places

Two category errors in one day, same root. First: three defects filed as rows 34-36 of `TASKS.md`’s Main Systems table — a table whose column is `System / Feature` and whose rows 1-28 bind one-for-one to the contracts under `docs/agent-blueprint/`. A bug is not a system. I did it because the queue felt invisible, and visibility was never the problem. Second, and much larger once measured: **52 `task #NN` references had accumulated across 27 tracked files** — 12 source, 8 docs, **4 applied migrations**, and 3 memory-archive blocks. **The owner’s reason is duplicate work, and it is sharper than clutter.** A queue number in a shared file is a pointer that resolves to nothing on the other end — no file in the repository translates it. A dev reading `(task #26/#35)` in `useAuth.ts:96` cannot tell whether that item is done, in flight, or abandoned, so they either redo finished work or leave a real gap alone believing someone owns it. An unresolvable pointer is worse than no pointer, because it looks like somewhere to go. The migration comments were the worst of it — `-- That is task #95, and it is the owner’s call` reads as “someone owns this” and points nowhere. **The rule, as ruled**: the queue is ours and does not ship. The single exception is a **deliberate handoff** — a specific item, to a specific person, acknowledged by both sides. Everything short of that is a leak. When a queue item genuinely needs to be visible in the repo, ship the **substance**, never the number: what the defect is, where, and what fixes it, written so it stands alone. That is the same test the shared-vs-local load split already uses. All 52 sites now carry substance; a repo-wide sweep returns zero. The lesson was recorded in the cross-project memory store rather than as a rule in `CLAUDE.md`, on the owner’s choice — the mistake was a thinking error about which layer a thing belongs to, and that travels to every project, while `CLAUDE.md` reaches only this one.

## 216. The gacha rate domain got a rule instead of a number

PR #118 asked for two named characters at 50% each — a request that reads as harmless until it is run through the invariants the domain didn't have yet: 50% of pull volume on a single rarity band collapses every other band to zero, and a pity counter that resets on every draw because every draw is already the pity rarity. That was the opening this session needed. An ultracode workflow (13 agents — 2 grounding the repo's actual numbers, 6 pulling real odds from Genshin, HSR, FGO, Arknights, Blue Archive, and Fire Emblem Heroes with sourced URLs, 1 synthesis pass, 3 adversarial breaks) derived a base×scale rule for the whole domain — band rate, per-character split, pity, cost, duplicate shards, star ladder — instead of a ratified number, because the owner's 2026-08-10 design lock (item 191) already forbids hand-placed values anywhere in the game's economy. What followed was ratification decision by decision, not a rubber stamp on the derivation: **`B[top]=3.3%`**, the midpoint of the exemplar corridor (0.6% Genshin/HSR to 6% FEH), with corridor `I16` replacing an `0<B<1` invariant that had no teeth. **4 bands, not the 3 every "dump tier" exemplar uses** — measured that this roster's rarest characters (`pig-warrior` at 1673 combined stat, ahead of the two legendaries) are not filler the way a 3★ Genshin weapon is, so the Arknights shape (rare characters stay playable) fit the roster's own kit design, not the other way around. **`P[legendary]=100`**, chosen over a derived `P=80` because reach at 100 (3.61%) sits below every exemplar's own reliance on pity, plus a proportional migration formula (`count_new = round(count_old × 100/30)`) for players already mid-cycle when the threshold moves, and a second base — `preserve_relative_progress`, true for the permanent Standard Banner and false for the not-yet-authored limited-banner type — so a future banner's own threshold changes don't inherit this one's migration obligation by accident. **`d=0`, `c=16` gem**, reached after two false starts: the first burden pass divided a flat "same USD everywhere" price by minimum wage across six countries and found the project's asking price 30% of a Bangkok day's wage — a number that collapsed once regional pricing turned out to be real (SEA prices 50-70% below USD nominal, confirmed against three market reports) and once minimum wage turned out to be the wrong denominator for China specifically (18-26% of that country's own average wage, against 47-66% here — the same word does not mean the same fraction of real income in two economies). The corrected pass — 9 real games' local-currency price against local *average* wage, no FX conversion — put the fair price at a 1.11% burden, six times cheaper than what was shipping. **`S[r]` derived once, then explicitly un-derived**: the shard-per-duplicate table first came out as a clean function of the same ratio (`r=2.687`) that splits the bands, until the owner asked whether that was actually wise — a future `B` re-ratification would silently reprice the entire duplicate economy, two systems that are not the same decision, coupled by a formula no surveyed game (Blue Archive, FGO, Arknights — all hand-authored, flat, independent tables) actually uses. Frozen to `1/3/7/19`, no longer a function of anything. **`L[r][k]` rebuilt once measured against real cost**: the first draft (a rarity-tiered ladder from ★2 to ★5) priced a maxed legendary at 909 pulls — three times FGO's NP5 requirement, the most duplicate-hungry game in the panel, and worse than Arknights, which needs zero duplicates for core power and treats them as an optional side-grade. Rebuilt around a uniform 6-duplicate target (the only value that divides evenly against all four frozen `S[r]` values at once), landing legendary at 364 pulls — inside Genshin's C6 range (350-540) and just under FGO's NP5 (500-567). **The one repo file this session actually changed**: `docs/MASTER_BLUEPRINT_v3.0.md §7.1` named Genshin's soft/hard pity ramp as the shape to build; every formula this session locked (`reach = (1-q)^(P-1)`, flat by construction) had been FGO-shaped from the first invariant, not Genshin-shaped, and no locked number ever actually borrowed Genshin's ramp even where it borrowed Genshin's raw numbers. Reconciled the document to the math rather than rebuilding four locked invariants and a shard economy to match a sentence. Everything else — this derivation, all twelve decisions and their arithmetic — lives in a scratchpad `GACHA-RATE-DESIGN-LOCK.md`, not yet in the repo; 8 of 12 are closed, `u` (featured share), `BATCH_01_GACHA_BANNER`'s fate, the still-unauthored second banner type, and physical schema placement remain open, and the banner itself stays unpublishable regardless — `monkey-king` fails its own sprite-conformance band by 51×, checked against `docs/SPRITE-DESIGN-LOCK.md` before any of the rate work started. **One error caught in front of the owner, not after**: an early reply asserted gacha games charge the same USD price globally and built a wage-burden argument on it before a regional-pricing search proved the premise false outright — the fix was not a smaller correction to the same number, it was standing down the entire method and rebuilding it on local-currency-to-local-wage comparisons that needed no exchange rate at all.

## 217. A green test stopped being enough — and task #73 closed on that new bar

The owner watched main review two PRs claiming the same fix and noticed the QC gap directly: "ดูที่ตัวแปรสร้าง ๆ อย่างเดียว... สร้าง gate ขึ้นมา แต่ไม่เคยเทสเลยว่า ป้องกันศัตรูได้ไหม" — a gate gets built, but nobody ever tests whether it actually stops the thing it claims to stop. Named by analogy to `src/game/spriteContract.test.ts`, which already does this for art (opens the real `.webp`, measures it with `sharp`, never trusts the declared config): a bug-fix test now has to clear the same bar. **`.agents/rules/mutation-verified-fix-law.md`** (`AGENTS.md` rule 23, `RULES_VERSION` 37 → 38) makes it standing: a defect-fixing PR needs a test checked out against the pre-fix source and confirmed red, then checked out against the fix and confirmed green — "a test exists and is green" is not the same claim. Run against the two PRs that prompted it, same day, both fixing task #73's two findings: **PR #128** (recovery-effect replay — `LobbyPage` passed an inline-recreated `onExit` closure, so every rerender looked like a new session and replayed `onGetPendingRewards`) had a real regression test; checked out against master's pre-fix `LobbyBattleSession.tsx` it went red (3 calls instead of 1, the exact reported symptom), confirming teeth. **PR #130** (three sites in `lobbyBattleRewardPipeline.ts` computing the same `progress.flags` merge twice — once into a local `flags` guard, once through `withFlags`) shipped no test at all, and reading the two computations showed why the gap mattered in a way the rule's simple red/green check almost missed: `withFlags`'s merge and the inline duplicate are byte-identical, synchronous, no state change between them — old and new code produce identical output for every real execution today. The defect is real (a future edit touching one write site and not the other), but it is a maintenance-drift risk, not a live bug, so no black-box input/output test can turn red against it — the classic check does not apply to this defect *class*, and main built a structural test instead (`src/game/reward/lobbyBattleRewardPipeline.test.ts:401-424`, a regex over the source text asserting no flag key is assigned outside `withFlags`), verified the same way: red against master's pre-fix file (2 duplicate-write sites matched), green against the PR's branch. Per rule 18/23 a contributor's duty ends at PR submission — a PR that fails this check does not bounce back to them, main writes what is missing. Both fixes landed on `master` directly (`58e1403` for #130 with the new test and rule in the same commit, `6f4c9d1` for #128 with its own regression test), both PRs closed with the mutation-check evidence quoted in the closing comment, task #73 fully closed.

## 222. The last two handoff items were filed as documentation clashes and both turned out to be defects

Items 4 and 12 sat open longest because they read as bookkeeping: two contracts each claiming the same function. Measuring the code instead of reading the contracts found real damage under both. **Item 12 is the sharper one.** `applyBattleExp` levelled the lead hero with a `*1.2` loop written inline in `RewardSystem.ts`, reading whatever `expToNext` happened to be stored on the character. Dungeon rewards had long since moved to `progressionService.applyHeroExp` over the Ring-0-locked P8 table. The two curves disagree by **51.5% in cumulative EXP to reach level 11 — 2,605 against 5,370** — so the same character levelled at a different rate depending on which battle mode granted the EXP, and had done since the systems were split. HetCreep asked the right question before accepting the fix: *is there a fixed answer to this worldwide?* Searched, and found no shipped game that varies the level curve by EXP source; Genshin is the clean counter-example, where every source feeds one Character EXP pool read by one table and only the amount gained varies. The structural argument is stronger than the exemplar: "level" has to answer one question with one answer, and a curve that forks by source is two stats wearing one name — the same shape as the gacha lock's "the number the player sees is the row the server rolls". Fixed by calling `applyHeroExpToLeadHero`, the function Dungeon rewards already used. **Then rot-canary caught the side effect the fix introduced, an hour later, in the same file it had just been used to fix.** Routing through progression made `applyHeroExp`'s `throw new Error('Unknown hero')` reachable from the reward path, where the inline loop it replaced had merely matched nothing; neither `lobbyBattleRewardPipeline` nor the effect driving it catches, so an account whose `teamSlots` disagreed with `ownedCharacters` — a half-applied grant, a migration that dropped a character — would have lost **gold and items as well**, not just the hero EXP that genuinely had nowhere to land. Closed with a guard before the call rather than a catch around it, because a missing hero is not an error, it is an absent destination, and a catch would also swallow the real errors progression raises. Both fixes verified red-then-green per rule 24. **The test shape earned its keep**: the equality case drives both paths with the same amount and compares the resulting hero rather than asserting a hand-derived number — my own hand-calculation of the expected level was wrong, and because nothing was hardcoded to it, the test stayed honest and did not send me to fix the wrong thing. All twelve handoff items now closed.

## 221. nustanakritwithai left, and the interesting part was which records that broke

Thirteen PRs this cycle, ten merged. The departure itself is unremarkable; what it exposed is that **a wait condition can outlive the person it names and still read as pending.** `docs/BLUEPRINT-CHECK-HOLD.md` declared every conflict HELD "until nustanakritwithai's design-lock answer lands" — ten of twelve had already been answered, and the remaining two now had a gate that could never open. Rewritten to say items 4 and 12 are HetCreep's, with the original ruling kept as history rather than deleted. The same shape had been written about hours earlier in `docs/BRANCH-PROTECTION-DECISION.md` ("a dismissal that outlives its reason is worse than the original finding") and then walked straight into from the other direction. **The genuinely load-bearing correction was in my own memory**, not the repo: a standing rule auto-approved their design-lock proposals once `user.login` verified. That gate still passes after someone leaves — an account is not deleted when a relationship ends, and it can be transferred or compromised — so the rule would have kept returning "verified" while the trust it stood for was gone. Revoked rather than left to age out, keeping the verification discipline (read the raw API field, never a comment's own prose about itself) because that part generalises to anyone. **Owner's two calls, both against the reflex**: keep their collaborator access ("ไม่ถอนเครดิตจนเกลี้ยง" — do not strip the credit clean), and close PR #127 on the departure rather than on the rejection. Every historical record was left exactly as written, including `assets/ATTRIBUTION.md`'s correction that they authored zero of the twenty images once credited to them — a record is only worth having if it survives the person's exit in both directions.

## 220. v0.21.0 — the three P1 defects closed, and two of the three were misjudged before anyone measured

All three had sat since the 2026-08-12 audit with **no tracking row anywhere**: they briefly had `TASKS.md` rows 34-36, the owner reverted those as belonging to a table bound to the system contracts, and the pointers in `MEMORY.md` (twice), `docs/README.md`, and the audit itself were never repointed — the audit, which is where the findings actually live, was directing readers elsewhere to find them. Fixed first, then the defects. **The malformed-UID fix was the one that mattered**: `substr(uuid, 1, 10)` produced 8 hex + hyphen + 1 hex for every Google and guest account, which fails the 10-digit contract — but the sharper detail is that the friend-search box strips non-digits from its input, so the value could not be **typed in**, not merely not found. Fixed at the server rather than the two callers that forgot the metadata, because patching callers leaves the default wrong and the third sign-in path someone adds later inherits it in silence. Verified by mutating the generator back to the pre-fix expression and watching the test go red on the real symptom (`'ab31b64f-f'`), with the suite running the migration FILE through PGlite rather than a transcription. **Then the count arrived and flipped the backfill question rather than answering it**: `101 malformed / 112 total`. The standard objection to reissuing an ID is that the player already gave it out — but a UID nobody could type was never given out in any usable form, so there was nothing to destroy. Backfilled after testing the DML against a PGlite database seeded to the real 101/112 distribution (seven checks including idempotence and "the 11 already-valid UIDs are untouched"). **The tools finding was misjudged in the audit and the correction went the other way from what a reader would guess.** The audit called the raw-PNG overwrite HIGH and "unreproducible"; measured, `assets/raw/` is fully tracked in git (walk 128/128, turnaround 36/36), so `git checkout` recovers it and that site is the *lightest* of the three. The real damage was the MEDIUM one: `optimize-images` skips on mtime alone, and a half-written `.webp` has a **newer** mtime than its source, so every later run skips the broken file forever with nothing signalling that `--force` is needed — git cannot help, because the bytes are not the problem, the logic refusing to revisit them is. Closed with one shared atomic-write helper, and **verified by running all three tools for real rather than reading them**, which is where the session's own errors surfaced: `sharp` infers output format from file extension, so a temp ending `.tmp` was a genuine risk that had to be disproved before it could be trusted; `--force` dirtied 120 files in `public/`, which was nearly committed as this work's doing until the pre-fix tool was run the same way and produced the identical 120 (pre-existing encoder drift, still open); and the first seeded test file for the stray-alpha tool was silently ignored, because that tool filters on a `pigsy` filename prefix — it reported "0 files" and looked like success while the write path had never executed. **Then rot-canary was pointed at the hour-old work and found three more, all mine**: an exported helper nothing called, temps that could survive SIGKILL inside `public/models/` and ship to players in the bundle, and a promote loop whose all-or-nothing claim covered validation but not the renames that follow. The last was fixed by reporting the partial state rather than pretending it cannot happen. `npm run ci` green across all six gates, 1168 tests over 133 files. Two things left open on purpose and named in the changelog: a pre-existing `AuthModal` flake (twice in ~10 full runs, then four clean when chased — raising a timeout without a reproduction moves the threshold and hides the signal) and the 120-file encoder drift.

## 218. v0.20.0 — eleven PRs dispositioned, and the review found two bugs the PRs did not contain

The owner ordered traffic control toward the next bump, so eleven open PRs (#119-127, #129, #131, all from `nustanakritwithai`, all based four commits behind `master`) were partitioned into five read-only review units and dispatched in parallel — main kept sole write authority and applied everything sequentially afterward. The partition found one real dependency and one real collision risk, and **both turned out to be wrong**: #127 was scheduled to land before #123/#124/#125 because it modifies the source files those three test, and two independent reviewers checked the actual assertions and reported no collision — none of the three touch img attributes, snapshots, or node counts. The brief main wrote for those agents also asserted #127 adds `fetchpriority`; it does not, and two agents caught the error separately. **Ten merged, one rejected.** #127 was rejected on measurement, not opinion: `sharp` over the shipped files shows `public/characters/walk/` is 640×512 across all 128 frames and `erlang-shen-v6-idle` is 640×512, both declared `396×376`, while the UI icons are 256×256 against declared `68×68`/`32×32` — and the declared numbers are unreachable anyway, because all nine call sites already pin both axes in CSS and one sets an `aspect-ratio` that contradicts the declared ratio outright. Left **open** with the repair path (read from `SPRITE_SHEET_CALIBRATIONS`, which already holds these numbers and is E3's documented source of truth) rather than closed, because the idea is worth keeping and the fix is mechanical. Three defects were **converted at intake rather than filed as debt**, per the base×scale ruling and the proven-good rule: #129 restated the `0.7` scale literal three times and now reads it from its one definition in `dungeonConfig` — deliberately *not* from `state.enemyHpScale`, which is the value under test and would have gone green on exactly the bug the test exists to catch, verified by mutating the orchestrator to forward `1` and watching it turn red; #120's `@react-three/fiber` mock omitted `useLoader`, a latent trap that would have killed the whole suite with a misdirecting error the day `SHOW_ARENA_SLOTS` flips; #125 carried the repo's only `AudioEngine` mock, never asserted and unnecessary since `playSfx` is already a no-op under jsdom. **The reviews found two live bugs in production code that no PR contained.** The larger one: `ToastProvider` kept its duplicate guard in a `Set` separate from the rendered list, so `.slice(-MAX_VISIBLE)` evicting a toast left its message in the guard until that toast's own timer expired — for up to 2460 ms the message was unshowable, swallowed silently with no toast, no sound, and nothing on screen to explain why the button did nothing. PR #119's fourth test builds that exact state and stops one assertion short of it. Fixed at the root by deleting the second source of truth: the guard now reads the rendered list, so eviction clears it by construction. Verified per the rule written earlier the same day — the new test fails against the pre-fix component (`Received: สองสามสี่`, expected `หนึ่ง`) and passes against the fix. **Main's own drift was the other find**: `AGENTS.md` said `RULES_VERSION: 38` while `MEMORY.md` still said 37, a mismatch main itself created when it added rule 24 — and rule 1 makes every agent seeing it fully re-read the whole governance layer before doing anything, so the tax was on every future session, not this one. Released as **v0.20.0** with `npm run ci` green across all six gates (1144 tests, 128 files, up from 1119/121), and the CI gate caught the bump itself: `GAME_INFO.version` is a third place the version lives, and the release was one test-run short of shipping with it stale.

## 219. The first of the three P1s closed, and the owner picked the server

Post-v0.20.0 the owner asked to walk the waiting-on-a-decision list one item at a time, starting with the oldest and worst: **every Google-OAuth and guest account had been issued a malformed public UID, live in production since the audit named it on 2026-08-12.** `handle_new_user` issued the UID as `coalesce(raw_user_meta_data->>'uid', substr(new.id::text, 1, 10))`, and only `register()` ever supplied that metadata — `signInAnonymously` and `signInWithOAuth` supplied none, so both fell into the substring branch and got 8 hex + `-` + 1 hex. The severity was not the malformed string itself but what sat downstream of it: the UID contract is `^[1-9][0-9]{9}$`, the friend-search box strips non-digits from its own input, and `profiles.uid` is `unique not null` with no update path anywhere — so an affected UID **could not even be typed in**, not merely fail to match, and the account stayed unfriendable forever with nothing on screen explaining why. Presented as a fork — patch the two call sites, or move issuance to the server — and **the owner chose the server**, which is the answer that survives the third sign-in path nobody has written yet; patching callers fixes today's two and leaves the wrong default sitting there to be inherited in silence. Two design choices inside the fix are worth keeping: issuance **inserts and lets the unique constraint arbitrate** rather than pre-checking `not exists`, because a pre-check is a race two concurrent signups can both win and the constraint is the only check that cannot be raced; and a `unique_violation` that is *not* the uid constraint is re-raised immediately, so a duplicate primary key is not mistaken for a UID collision and burned through all twenty retries before failing with a misleading message. Verified under the rule written the previous night: the generator was mutated back to the pre-fix expression, the tests went red on the real symptom (`'ab31b64f-f'` against the contract), then green on the fix — and the suite runs the migration **file** through PGlite rather than a transcription of it, the same posture as the sprite contract opening the real `.webp`. **Then the count arrived and finished it the same session.** `101 malformed / 112 total` — 90.2%, which flipped the backfill question rather than merely answering it: the standard objection to reissuing an ID is that the player already gave it to someone, but a malformed UID **could never be typed into the friend-search box**, so there was nothing to invalidate. The thing backfill would destroy did not exist. At 112 accounts this is pre-launch scale, so it went ahead. The backfill was a DML against live rows rather than the DDL before it, so it was **executed against a PGlite database seeded to the real 101/112 distribution before being handed over** — seven checks, including idempotence and an explicit "the 11 already-valid UIDs are not touched", plus old values preserved into `profiles_uid_backfill_20260813` as the only route back. Production confirmed `malformed 0 · total 112 · distinct_uid 112`. With the migration live, the follow-up it had been blocking became safe and was taken: `register()` no longer generates a UID client-side, and its three-attempt collision retry went with it — both were the server's job now, and a client that keeps drawing UIDs is a second generator waiting to drift from the first. One ordering constraint was recorded in the migration header rather than left to memory and it held: **the migration had to land before the client change**, since a new client that stops sending metadata, run against the old trigger, falls straight into the branch the migration exists to delete.
