Durable body store for root `MEMORY.md` — items 201–202. Later entries are append-only; root `MEMORY.md` is the index over them.

201. **Historical Gitleaks scan uses an exact-key allowlist, not an inline-only marker (2026-08-10T23:28:00+07:00, Operator: HetCreep, Agent: Codex as fork-test integrator)** — the inline `gitleaks:allow` marker stopped a worktree-only check but the GitHub workflow intentionally scans the complete commit history, so the first commit containing the publishable fallback still failed. `.gitleaks.toml` now extends the built-in rules and adds one exact regex scoped only to `generic-api-key` for the already-public `sb_publishable_...` value. It does not skip a commit, path, file, or rule and therefore continues to catch every other credential class—including `sb_secret_...` and `service_role`—across the full history. The config was verified against the fetched fork head with Gitleaks v8.30.1 before publication.

202. **Ring 0 opened Standard Gacha for the fork's real-device playtest (2026-08-10T23:30:00+07:00, Operator: HetCreep, Agent: Codex)** — replaced the misleading asset-readiness gate with the explicit `GACHA_PLAYTEST_ENABLED=true` approval and wired every Lobby entry point plus the MainNavigation behavior test to it. Added `20260810230000_enable_standard_gacha_playtest.sql`, which changes publication state only after verifying exactly five pool rows, a 100% rate sum, and a valid pity-rarity row; RNG, Gem debit, pity, ownership and idempotency remain inside `perform_gacha_pull`. PGLite exercises freeze→reactivate→real pull, and the focused UI/RPC suite passes 23/23. The Supabase connector available in this session does not own project `huxalspcxuhdavsvetul`, so the migration is committed but not claimed live until it is applied and queried there.
