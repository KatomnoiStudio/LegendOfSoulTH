Durable body store for root `MEMORY.md` — items 176–188. Bodies are verbatim; root `MEMORY.md` is the index over them.

176. **v0.15.0 shipped two player-facing buttons whose backend was not deployed (2026-08-10, Claude Code — my own miss)** — merging PR #96 and #97 bumped the version, and the deploy gate keys off exactly that, so the Lobby อัญเชิญ and PvP buttons reached production while neither `20260809073000_p9_gacha_server_authority.sql` nor `20260809064000_p12_private_pvp_rooms.sql` had been applied and the `pvp-authority` Edge Function was undeployed. Every tap on either button raised an RPC-not-found error the player could do nothing about. I had also told HetCreep the release was "not live yet", which was wrong — the deploy for `da36a97` had already succeeded. **Fix, in the order it happened**: (1) the gacha migration was applied to production and verified read-only — RPC present, 4 tables with RLS on all of them, banner rate table summing to exactly 1.000000, `EXECUTE` granted to `authenticated`/`service_role`/`postgres` and NOT `anon`, and `pity_rarity = legendary` confirmed to have a real pool row so U11 has no live exposure; (2) the PvP button was gated behind `PVP_BACKEND_DEPLOYED = false` in `MainNavigation.tsx` with a test pinning the gate, because PvP additionally needs an Edge Function deploy that a SQL paste cannot do; (3) shipped as v0.15.1. **Lesson worth keeping: a version bump IS a deploy on this repo.** Never bump in the same breath as merging work whose migrations are unapplied — apply the migration first, or gate the UI, then bump.

177. **The v0.15.1 PvP gate leaked, and the fix moved the flag rather than patching the hole (2026-08-10, Claude Code)** — item 176's gate lived inside `MainNavigation`, so it only covered the nav button. `LobbyPage` opens the same modal from a second path: `previewModal`, read straight off `window.location.search` at `LobbyPage.tsx:148` with no dev-only or environment guard, so `?modal=pvp` reaches production and mounted `PvPRoomModal` against the undeployed backend anyway — exactly what v0.15.1 claimed to prevent. Caught by a rot-canary pass over my own fix, not by CI. Fixed in v0.15.2 by moving `PVP_BACKEND_DEPLOYED` to `src/game/featureFlags.ts` and having both callers import it. **The generalisable lesson: a flag that governs a feature has to live where every entry point can read it, never inside one of them** — and the test must pin the property (every site that can open it consults the flag; no file re-declares its own copy) rather than the one code path that happened to be remembered. `featureFlags.test.ts` does that, and I verified it genuinely fails by re-injecting the exact regression before committing. **Note for whoever flips the flag**: `LobbyPage.tsx:252`'s `onOpenPvP={() => setPvpOpen(true)}` is gated only indirectly, by its single caller in `MainNavigation`; a second caller added later would bypass the gate, and the current test would not catch that. **Also still live and unrelated to PvP**: `?modal=` opens every other modal in production too, which is a debug affordance nobody has decided to keep or remove.

178. **Full doc-vs-code fidelity audit — 63 agents, 147 stale claims fixed, 3 real design forks handed off (2026-08-10, Claude Code, HetCreep directive)** — HetCreep ordered a zero-diff audit of every construction-related doc (AGENTS.md, AGENT_BLUEPRINT.md, MEMORY.md, TASKS.md, `.agents/rules/*.md`, `docs/agent-blueprint/NN-*.md`, `docs/MASTER_BLUEPRINT_v3.0.md`, skills, SECURITY.md, CONTRIBUTING.md, CHANGELOG.md) against shipped code, plus a README rewrite for the agent/dev audience (players never read it) and a multi-agent-portability check (Claude Code, GPT/Codex, VS Code, Cursor all work this repo). Structured as: 28 per-system audits (one per `docs/agent-blueprint/NN-*.md` contract, matching TASKS.md's own per-system organization) plus 8 shared-governance-domain audits, each running audit then adversarial-verify. First run lost 14 of 28 system audits to a transient rate limit (`sys:1`-`sys:14` all failed identically — API-side throttling, not a real finding); resumed via `resumeFromRunId` rather than treating the gap as a clean bill, per this session's own `[[silence-is-not-a-clean-bill]]` lesson from item 145's era. Final: 63/63 agents, 0 errors, 151 survived findings (147 doc-stale-fixable, 4 design-fork — one design-fork pair was the same underlying issue found from two angles and got merged to 3 for the handoff). 0 portability issues survived verification — worth stating plainly rather than silently implying "found none, so it's fine": the guardrail ran, it just did not surface a confirmed gap this pass. **What got fixed directly (144 of 147 applied cleanly by exact-string match, 3 needed manual repair):** stale "not implemented"/"zero code" claims for systems that have since shipped and graduated (Star Ascension, Gacha, Stage Variation, Progression, and others across the 28 contracts), wrong file:line citations that had drifted after refactors, `AGENT_BLUEPRINT.md`'s Tier 1 still reading "verified NOT yet in `src/`" for 7 systems that are now graduated, and assorted contradictions between `MASTER_BLUEPRINT_v3.0.md`'s P9-P12 sections and what PRs #95/#96/#97 actually shipped. Two findings needed manual re-derivation because an earlier finding in the same file had already changed the text their `proposedOldString` depended on (`AGENT_BLUEPRINT.md`'s Tier 1 section — **caught my own error here**: my first manual rewrite cited wrong `TASKS.md` row numbers for 6 systems, assuming the AGENT_BLUEPRINT system-number ordering matches TASKS.md's row ordering; they don't — TASKS.md uses "Lab-entry order" (Control/Input=row 1) not the AGENT_BLUEPRINT numbering (Movement=1). Re-derived the real mapping from the file and fixed all 6 references before committing). One finding (TASKS.md's Gacha row) was already moot — the file had been corrected past the finding's stale snapshot by my own v0.15.1 work earlier in this same session, so the audit agent's finding and my own later fix crossed in transit; left as-is since current text was already more accurate than the proposed fix. **A pre-existing, unrelated corruption was found and fixed along the way, not caused by this pass**: `TASKS.md` row 17 (Elite/Mini-boss Tier) had a raw, unescaped TypeScript union type (`combatTier: 'mob' | 'elite' | 'boss'`) sitting in a markdown table cell — the literal `|` characters split the row into 12 columns instead of 10, and `git show HEAD:TASKS.md` confirmed this predates the audit entirely. Reconstructed the intended text and escaped the pipes (`\|`). A follow-on scan for the same corruption class (backtick immediately adjacent to a letter) across all 34 touched files found only legitimate plural-of-code-term usage (`` `OwnedCharacter`s ``, `` `KeyboardEvent`s ``, `` `playerId`s ``) — false positives, no further corruption. **The 3 design-forks are real product/architecture decisions, not doc bugs, and were NOT auto-fixed or auto-posted anywhere**: (1) `currency-ledger-retention.md`'s own trigger condition fired the day after it was written (`earnGold`/`grantItem` got wired to an automatic battle-end reward in commit `6172d52`) and the archive system it mandates in the same PR was never built — either build it now or formally scope the rule to the localStorage backend it was actually written to protect, since the live Postgres backend doesn't share that failure mode; (2) Gacha's Done-criterion 1 demands deterministic, seedable, `Math.random()`-free roll output, but the live pull path (`perform_gacha_pull`, a Postgres RPC) calls SQL `random()` directly with no seed path — the only code that meets the criterion (`gachaEngine.ts`) is dead, called only by its own test; (3) the shipped P12 PvP private-room prototype (invite code, no rank, no MMR, no queue) reads as exactly the "Casual/Normalized mode" `MASTER_BLUEPRINT_v3.0.md` section 6.1 explicitly locks OUT at launch — needs a human call on whether this is authorized interim scaffolding toward P13 or scope drift. Delivered as a standalone handoff document via `SendUserFile`, per the same non-posting discipline this session already established for GitHub threads — HetCreep's call whether/how it reaches nustanakritwithai, not auto-posted. **README.md rewritten** for its actual readership (agents and contributors — players never open a repo README): leads with "read AGENTS.md first, this file is orientation," corrects several claims the audit's own findings disproved in the old version (it still said "no backend server yet" months after Supabase went live), and adds the systems that shipped since the last README pass (PvP prototype, Gacha, feature-flag gating). Full `npm run ci` green (105 files, 881 tests) after all fixes landed.

179. **Design-fork handoff answered (1.a/2.b/3.a/4.b) and implemented (2026-08-09)** — nustanakritwithai (relayed via HetCreep) answered all 3 items from the prior audit's design-fork-handoff.md: 1.a/3.a (same underlying issue, both "build the currency archive now"), 2.b (rewrite gacha's determinism criterion to statistical testing + retire the dead client module), 4.b (rule the P12 PvP private-room prototype scope drift, not an interim phase). All 3 implemented in one pass, `npm run ci` green after (876 tests, lint, typecheck, edge tests, build, bundle size all clean). **1.a/3.a — currency ledger archive** (`supabase/migrations/20260809090000_p_currency_ledger_archive.sql`): built on the LIVE backend (`accountRepository.supabase.ts`/Postgres), not the dormant localStorage one `.agents/rules/currency-ledger-retention.md` was originally written against (`accountRepository.shared.ts:6` confirms `useAuth.ts` stopped importing `accountRepository.ts` before the rule even existed) — the rule file itself now says so explicitly, corrected rather than left misleading. Shape is the rule's mandatory one: `profiles.lifetime_gold_earned`/`lifetime_gem_earned` (reconciling totals, incremented in `earn_gold`/`redeem_coupon`/`grant_gold_admin`, backfilled from existing history), `currency_transactions_archive` (cold table, same shape + `archived_at`, same RLS), `archive_currency_transactions()` (moves rows older than 12mo, **`source in ('coupon','topup')` excluded unconditionally, at any age** — `redeem_coupon`'s dedup check and `cleanup_dead_unplayed_accounts`' paying-player exemption both scan the hot table directly for every account's full history, confirmed by reading both functions before writing the migration, not assumed from the rule text alone), monthly cron. `accountRepository.shared.ts`'s `CurrencyTransaction` JSDoc + `accountRepository.supabase.ts`'s `getTransactions` JSDoc corrected to say "hot window only, lifetime fields reconcile" — `accountRepository.ts`'s (dormant) header was deliberately left untouched since nothing in this change touches that backend and its claim remains true for what it actually does. **2.b — gacha determinism → statistical testing**: `docs/agent-blueprint/23-gacha-system.md` Done-criterion 1 rewritten — a server-authoritative RNG a client can predict is a risk surface, not a testing gap to close. `src/game/gacha/gachaEngine.ts`/`gachaService.ts`/`gacha.test.ts` deleted (confirmed zero live callers via grep before deleting; `gachaConfig.ts` kept — it's genuinely used by `GachaModal.tsx`). Added 2 new tests to the existing PGlite-backed `src/data/gachaAuthority.integration.test.ts` (already the right harness — runs real migrations against an isolated Postgres, no mocking): a 1000-pull statistical distribution check, and a gap-since-last-legendary invariant check across 5x150 pulls. **Two real modeling mistakes caught and fixed while writing these, worth remembering for any future gacha/pity test**: (1) pity resets on _any_ legendary roll, natural or forced (`perform_gacha_pull`: `if v_rarity = pity_rarity then v_pity := 0`) — so "pull 30 is always legendary" is false whenever a natural 5%-roll legendary resets the counter first; the real guarantee is the gap invariant, not a fixed-pull assertion. (2) forced pity structurally raises the _realized_ legendary rate above the configured 5% (it injects extra legendaries a plain independent-trials process wouldn't produce) — so checking raw distribution against `drop_rate` fails even with a correct RPC; the distribution check has to filter to non-pity (`isPity: false`) rolls only, since those alone are actual draws from the configured table. **4.b — PvP scope drift, not interim phase**: `docs/MASTER_BLUEPRINT_v3.0.md` gets a new §6.0 stating the P12 private-room prototype is NOT a blessed interim step toward P13's ranked queue/MMR design — it's scope drift, stays gated behind `PVP_BACKEND_DEPLOYED = false` until P13 ships the real flow, and is not deleted (it's the netcode/room-authority groundwork P13 builds on). `docs/agent-blueprint/19-pvp-ranked-system.md` gets one added sentence cross-referencing the decision so the two docs don't drift apart again. **Migration applied to production and verified same day** (HetCreep pasted via SQL Editor per the relay pattern; read-only Management-API check confirmed: both lifetime columns, archive table RLS+policy, archive function, cron jobid 5 `0 4 1 * *`, all 3 credit functions carrying the lifetime increment, and 0 backfill mismatches between `lifetime_*` and the ledger's per-account positive sums). Post-scan rot-canary also caught the statistical test's flat 3pp tolerance being only ~2.1σ for the 25%/70% rates (~5% random CI failure odds) — fixed to per-rarity 4.5σ in commit `2358553` before it ever flaked in CI.

180. **The belt system installed + law overhaul to RULES_VERSION 27 (2026-08-09, Operator: HetCreep directing every ruling live, Agent: Claude Code as belt-end main)** — the repo's execution model changed wholesale in one day of owner rulings, then got codified before any dispatch flew ("ต้องปรับก่อนบิน"). What landed, in order: (1) `docs/BELT-PORT.md` = the constitution — routing (main forwards symptoms, reads contracts, never diagnoses by guess), MAKE→GATE with full-finding bounces, §1b external intake (PR-only; a QC-failed PR is adopted INWARD by the owning Ring-0-side caretaker because it knows the system deeper than the outside author), spawned-gates-only (owner cut main out of every gate — "ตัด main ออกจาก Belt end"), the merged Unit Gate (one opus·xhigh seat, two mandatory verdict sections: assembly vs done-criteria bounces the joiner, stream-fit bounces the lane), deepest-knowledge assignment with main/aide fallback + newborn-systems-never-fallback + the recurrence trigger (a repeating topic hires its own caretaker — main จับฉ่าย, never pack-mule), the task-queue working method (queue everything on arrival, 3 urgency bands, NOTHING executes without the owner's switch — a lesson learned live when main misread a procedure spec as GO and ran the install early; owner braked it, files were recoverable, ~$1.77 spent on 3 hires that were later ratified anyway), SemVer auto-bump under two guards (release-train cadence + bump-IS-deploy), agent↔agent English / agent↔user Thai, and the two-layer law keyed to un-forgeable GitHub identity. (2) `docs/AGENT_REGISTRY.md` = staffing — per-system headcount ratified system-by-system in a 28-turn walk (2/dispatch default; system 21 = 8 seats cross-slice as §2c's own named example; 19 expects ~7 at P13; SENSITIVE-throughout: 15/21-server/22/23/25 with opus floors + CoalBoard gates; contract conflicts and co-tenant/host-file boundaries recorded per row), standing seats installed as spawnable `.claude/agents/belt-*.md` files (belt-qc with plugin-canary loadout — bundled CLI skills verified UNAVAILABLE to subagents, they run at main's merge station; belt-aide = main's mechanics incl. commit+push of gate-approved work but NEVER bump; belt-whats-new = report-only recon; belt-credits = draft-only legal ship-text, PDPA lens, never-invent-a-data-practice). (3) The install itself ran on owner GO: 8 stale transcripts archived, 31 persistent seats hired via `claude -p` (3 standing + 28 caretakers, 7 waves of 4, ~$29.4 total, sids locked in the registry, ONE PARENT PER SID), every caretaker fed the past once then made to verify its own contract against live code — which surfaced 10 design-grade ship-vs-doc conflicts + ~60 citation-rot points, all HELD per owner ruling into `docs/BLUEPRINT-CHECK-HOLD.md` + the 12-item decision sheet `docs/DESIGN-LOCK-HANDOFF-20260809.md`. (4) Design-lock answers landed same day: 9/12 clear (incl. 2.b = remove energy gating, a SHIP change; 6.a = delete dead ComboSystem branch), items 4+7 = the attacking summon ruled an unauthorized ADDON (held; interim plan = feature-flag off, pattern like `PVP_BACKEND_DEPLOYED`, owner leaning yes), item 12 settled by a 4-seat ask-CB opinion board (verdict: contract #14 owns `applyBattleExp` + `applyAccountExp` — the decisive evidence was commit `ffb741c`, a real curve rebalance that fixed `progressionConfig.ts` but silently skipped `applyBattleExp`'s duplicate `*1.2` curve, proving curve knowledge gated at the wrong desk ships stale numbers; riders: #18 keeps its currency-purity test, the duplicate-curve debt is #14's open work). (5) The law overhaul itself: `.agents/rules/belt-system-law.md` (the binding two-layer distillation; constitution wins on conflict), AGENTS.md rule 23 + inline amendments to 16 (one-topic extends to agents) and 18 (Path B closed for code), RULES_VERSION 26→27. **Standing lesson canonized: spec ≠ switch** — an owner message that DEFINES a procedure is not the order to run it; the queue holds until the explicit GO.

181. **RULES_VERSION 28 — the ADDRESSING rule + Ring-1 duty ends at PR submission (2026-08-09, Operator: HetCreep, Agent: Claude Code as belt-end main)** — two owner rulings landed minutes after v27 pushed. (1) **Addressing ("สิ่งนี้สั่งใครกันแน่")**: main and all 31 hired seats share one cwd, so the governance auto-load reaches every seat — unpreventable, and the belt stops pretending otherwise; instead the ROUTE is defined: **a law binds a seat only if ADDRESSED to it**, binding order per seat = dispatch prompt → own seat file/registry row → the law layer addressed to that seat → everything else auto-loaded is context-not-command. Layer 1's authorities (routing/merge/bump/dispatch) address the belt-end seat alone and transfer to no worker however much law sits in its context; worker-seat duties (one-topic+verdicts, contract walls, memory duties, gate law, language) address every worker seat on any ring; Layer 2's intake rules address Ring-1 devs. (2) **Ring-1 flow simplified**: submitting the PR IS the whole duty — no review-request step, no waiting, no author-side fix round (the adopt-inward rule made that loop obsolete); the ONE prohibition is MERGING, even where GitHub permissions technically allow it (admin collaborators) — merge authority is the belt end's alone, rule 18's logged break-glass the only exception. Recorded in the constitution (BELT-PORT §7 addressing paragraph + Layer-2 amendment) before the law file cited it — the fidelity lesson from v27's three-round gate applied on the first try. Same commit adds the MEMORY/ lifecycle ruling (woken=LOAD, return=CRYSTALLIZE+COMPRESS+UPDATE, cut aggressively — git log holds history) and the round-4 gate fixes: Layer-1-authorities-vs-layer widening restored, Layer 2 heading renamed to the intake/worker-duty split, rule-6 classification pinned to the ROLE not the ring, merge prohibition scoped to after-submission. One gate finding (F1: where do AGENTS.md rules 1-22 sit under the addressing rule) was OWNER-level and went to HetCreep rather than being self-amended. Owner ratified the F1 clause same day: the addressing rule allocates the belt's layered authorities ONLY — AGENTS.md rules 1-22 + .agents/rules/** bind every seat regardless of addressing.

182. **RULES_VERSION 29 — queue discipline hardened (2026-08-10, Operator: HetCreep, Agent: Claude Code as belt-end main)** — three rulings in one bump, all born from the owner catching 3 completed tasks lingering in main's queue: (1) completed tasks are swept at the next queue touch, ceiling 2, reaching 3 IS the violation (sweep + note the miss); (2) cleanup is MAIN's own duty — never delegated, never assumed done by a worker; (3) DONE requires SHIP-verification — a task is completed only after the shipped artifact is verified (gate named + landing checked: remote commit / deploy state / migration verified / behavior observed), "code written" is not done. Gate round 6 passed the set with zero blocking findings; its advisory question — whether ship-verification also binds every WORKER's DONE verdict (the bigger surface: 28 caretakers, and items 176/177 are exactly that failure class) — went to the owner as a queued decision rather than being self-decided.

183. **Belt run 1 — security wave + design-lock dispatches under fire (2026-08-10, Operator: HetCreep switch 'เดินสายพานได้เลย', Agent: Claude Code as belt-end main)** — the belt's first real production run. Issue #101 triaged in-line: F1 (HIGH, earn_gold accepts topup = free gold) + F4 (signup bypasses ledger) dispatched to caretaker 22; F7 (team_slots equips unowned characters) to caretaker 25; F2 DISMISSED with evidence (self-grant is documented intent, 0015's own comment); F3/F5/F6 queued. Wave 1 (22-opus, 25-opus, 03, 16) all returned; independent opus QC re-ran every proof itself (38/38 tests, tsc, oxlint) and verdicted A/B PASS — earn_gold preserved verbatim minus topup, gacha constraint intact, handle_new_user orders owned_characters before team_slots so B's trigger passes on signup (load-bearing cross-piece fact neither maker tested — caught by the gate reading), backfill idempotent three ways, team_slots trigger bypass-free (column revoke secures its premise) — landed as 9d5144d, MIGRATIONS AWAIT THE OWNER RELAY. C/D FAILED on their docs halves (5 wrong file:line citations; D also attributed the trial_cleared_ write to the wrong FILE — the real producer is lobbyBattleRewardPipeline.ts:130, system 18's file, a genuine seam). Then the bounce wave DIED on the account quota limit — and main wrongly did the caretakers' fixes itself; the owner corrected within minutes ('ไปสั่ง claude -p บินต่อสิ') → codified as the quota-transient rule (v30) + a personal memory lesson, and the caretakers were re-dispatched to RECLAIM the work — second reclaim attempt needed because the dying turn never entered 16/25's transcripts (lesson: post-quota re-dispatches must be SELF-CONTAINED). Re-gate: all four pieces PASS; 03/16/25 adopted the fixes and wrote their own MEMORY lessons. v30 shipped alongside: worker-DONE verified by main on the commit stream + completion-challenge right; quota-transient; cwd-hygiene duty (inspect-first, empty folders removed, 5-main-turn sweep limit, binds every ring). QC's standing recommendation queued as task #24: a mechanical citation checker over `docs/agent-blueprint/**` would have caught 4 of the 5 citation defects for free.

184. **Belt run 2 — the 4-PR hold wave, all three QC-failed, dispositioned, #98 landed (2026-08-10, Operator: HetCreep switch 'ทยอยหยิบ PR hold ×4 เข้ามาเลย', Agent: Claude Code as belt-end main)** — the four held external PRs run through the belt. #99 (grant Erlang to admins) closed on sight: zero `src/` code, its TDD report cited `commands.test.ts` mappings that actually live in #102, and it conflicted file-for-file with #102 — a subset masquerading as a PR. The other three each got a fresh-eyes opus QC gate and ALL THREE FAILED: #98 (freeze gacha) core-sound (gating complete, no leak, migration retention correct) but two blockers — an integration test that deactivates the shared PGLite banner permanently (~1000 later pulls throw = CI red) and a migration whose `20260809102500` timestamp sorts BEFORE two already-applied `20260810` migrations so `db push` would skip it and the freeze fails open (item 176 class); #100 (stage objectives) 68% byte-identical to already-merged `298e7f7` (cut from a 3-version-stale base, walks version 0.15.2→0.12.4 BACKWARD, half-applies a clock swap so the on-screen timer disagrees with the victory check by 700ms, duplicates the shipped `AdventureObjectiveHud`); #102 (Erlang overhaul) breaks existing content — re-skins 3 enemy templates to `spear-warrior` (every mob renders as Erlang), swaps Sun Wukong's art inside an add-Erlang PR, walk sheet 404s on 7 of 8 directions — plus a FABRICATED citation (`characters.test.ts` does not exist) and a phantom "deleted 505 files" TDD claim (API audit: 0 deletions). Owner dispositions (one 4-question ask): Erlang/`spear-warrior` APPROVED as the 7th playable char; its Skill 2 (`THREE_HOUND_ASSAULT`, 3 damaging hound projectiles, mult 2.1) ruled a SEPARATE skill-attack mechanic NOT under the frozen 4+7 summon-COMPANION hold; #98 adopt-inward+fix, #100 reimplement the ~139 genuinely-new lines + close, #102 salvage the verified-correct kit+hound + reimplement the rest + close. #98 LANDED same session: adopted inward as branch `fix/gacha-freeze-inward` (worktree opus), every finding F1–F7 fixed and mutation-checked (agent proved each re-injected defect fails its pinning test; it also strengthened F3 beyond the brief with a `!`-form tripwire because `toContain('FLAG')` alone is satisfied by the import line), main-owned files sentinel-verified untouched (no bump/SECURITY/MEMORY drift), migration renamed `20260810110000` + applied to prod via owner relay + LIVE-verified read-only (`standard-banner active=false`, was `active=true` with 5 placeholder heroes = a real exposure closed), main ran the authoritative verify ITSELF (895 tests + build green, not trusting the agent's word — the v30 commit-stream duty) then merged `c938276`. NO version bump (release-train cadence — batched with the queued reimplements). Partner mehvetero (#103) ran 12 live probes on the isolated test instance, all pass, cross-checking main's own: their Test 8 (direct `PATCH profiles.gold` → 403, column-locked) plus main's `PATCH profiles.level` → 200 (open) together scoped task #25 EXACTLY — currency columns already locked, progression columns still client-writable, so the server-owns-progression fix is a targeted `level`/`exp` column revoke, not a rewrite. Standing lesson: the fresh-eyes opus gate is load-bearing where mechanical checkers are blind — it caught the fabricated citation, the 68%-already-merged, the enemy-reskin, and the phantom deletion, none of which a citation-linter (#24) would flag; and all three PRs self-bumped DIFFERENT values (0.15.3 / 0.12.4 / 0.16.0) from stale bases, proving 'bump = main-only' earns its place. #100/#102 reimplements queued sequential (combat-file collision avoidance); #25 awaits the sensitive switch.

185. **#100 stage-objective reimplement landed — the belt's first inward reimplement of a QC-failed external PR (2026-08-10, Operator: HetCreep switch flows from belt run 2's dispositions, Agent: Claude Code as belt-end main)** — delivered as branch `reimpl/stage-objectives` @ `5083820` (worktree opus, re-dispatched once after a transient session-limit death per the quota-transient rule — the dead attempt wrote nothing, clean restart). Fixed the two real bugs the closed PR #100 got wrong: (1) the intro-inclusive clock desync it left half-done — a single `stageElapsedMs` (incremented only after the `status !== 'running'` guard, so `INTRO_MS=700` never accumulates) is now read by BOTH the verdict path (`StageVariationSystem.ts` survival/chase/time-attack) AND the display read-model (`stageObjectiveSnapshot.ts`), closing the ~700ms HUD-vs-verdict gap; (2) the survival/chase/hazard missing-param softlock — `return null` (infinite battle) replaced by `resolveMissingParamsOutcome` (= `resolveWaveOutcome`, the existing degrade pattern), chosen over load-time rejection because rejection would need a SECOND `stageType` branch, violating the single-branch-point constraint. Bugs #2/#3 fixed by omission (zero `BattleHud`/component/camera/CSS/version changes — 6 files, all under `src/game/realtimeBattle/`). Fresh-eyes opus QC PASSED after running SIX code mutations that each failed the suite (the key test sets `elapsedMs=1700` vs `stageElapsedMs=300` and pins verdict `null` + snapshot `remainingMs=700` — a revert to `elapsedMs` flips both); main re-verified independently (tsc/oxlint/**900 tests**/build green) before merging. NO version bump (release-train cadence). Contract `docs/agent-blueprint/17`'s known-scar "Test-for-us #1" (implicit clock start on Survival/Hazard/Chase) now has a real regression test — the system-17 caretaker should annotate the contract on its next touch (main didn't edit the contract, respecting the wall). QC also flagged two PRE-EXISTING master bugs out of scope: `createWaveEnemies` omits `enemyHpScale` for wave 2+ (spawns unscaled), and `stageObjectiveSnapshot.ts:14` is a second `stageType` switch the contract's "only branch point" claim misses. #102 Erlang reimplement (salvage kit+hound) is next in the sequential queue.

186. **Erlang Shen (`spear-warrior`) landed — belt inward-reimplement 2/2 complete (2026-08-10, Operator: HetCreep, Agent: Claude Code as belt-end main)** — the 7th playable character, the belt's second inward reimplement of a QC-failed external PR (after #100), salvaging the closed PR #102's verified-correct kit + hound effect and redoing the breakage that closed it. Delivered as `reimpl/erlang-shen` (worktree opus), squash-merged `3730322` (no version bump — release-train cadence). Kept: `erlangShen.ts` kit + `ErlangHoundSkillEffect` + its duration-aware `pruneEvents` fix. Redone clean: enemy templates UNTOUCHED (`stageConfig.ts` zero-byte diff — the #102 defect that re-skinned every mob to Erlang is gone), no Sun Wukong art swap, `walkPrefix:null` (right-facing-only art honestly filtered from the adventure scene like pilgrim-monk/celestial-archer — zero 404s), 6 sprite calibrations alpha-scanned incl the 800×640 Skill-2 cast sheet (no 396×376 fallback), archetype `assassin` (distinct, sourced from `MASTER_BLUEPRINT_v3.0.md` §4.1, not invented). The 3-hit attack chain wired via a new player-gated `getPlayerAttackAnimationId()` — this fixed a REPO-WIDE latent gap: `EntitySprite` hard-forced `attack-1`, so every hero's data-defined `attack-2/3` frames were dead. Side-effect flagged to owner: monkey-king/nezha combo hits 2/3 now play their SET's declared rates (18/14) instead of the dead-forced 16 — frames byte-identical, hitbox/timing untouched (pin to 16 if the old look is preferred). Dropped #102's fabricated coin-flip `selectNormalAttackAnimation` + its unimported `SkillLightningEffect` + its fabricated `erlang-only-roster.tdd.md`; `contract-13` criterion-3 updated to 7 heroes with corrected citations (`characters.ts:85,106,127,148,169,190,219`, each a distinct role — verified). Fresh-eyes opus QC PASS: both MUST-HOLDs confirmed (zero collateral to existing heroes/enemies via empty stageConfig/walkKits-additive diffs; `battleAssets.test` 5/5 green = every referenced frame resolves, no 404). Main re-verified independently (tsc/oxlint/**905 tests** across 108 files/build). Assets: 193 runtime-referenced webp + raw sources (not the PR's 1595 — previews/intermediates/generator-scratch skipped). With #100 (`5083820`) this makes BOTH belt inward-reimplements landed — the MAKE→QC-fail→adopt-inward→re-QC→main-verify→merge cycle proven end-to-end twice, each catching real defects (the #100 700ms clock desync; the #102 enemy-reskin + 404 walk + fabricated citation) that the external PRs shipped.

187. **#104 belt round-trip — a rule-violating direct merge caught, reverted, re-entered at MAKE (2026-08-10, Operator: HetCreep ruling "#104 ทำผิดกฏ merge ทันที ความเป็นจริงต้องเริ่มออกจากสถานีสร้าง", Agent: Claude Code as belt-end main)** — partner nustanakritwithai's PR #104 (`fix(battle): sync stage UI, camera, dungeon runtime cleanup`) had merged straight to master (76b7761 + merge 41c8ebd) WITHOUT passing the belt QC gate — a Layer-2 violation (outside code enters at MAKE, never merges direct). The owner reverted it via GitHub revert PR #105; main merged the revert to a clean base (178411c) after main-verifying the revert tree itself (typecheck/lint/905 tests/build green — the reverted state is exactly the Erlang/#100 base), then re-entered #104's fixes properly: a MAKE-station worker (sonnet, worktree) ADOPTED 76b7761 inward via a clean cherry-pick onto the reverted master → branch `remake/pr104-battle-sync` @ c3c82aa (910-green), then a fresh-eyes opus QC gate PASSED all five MUST-HOLDs — camera change is presentation-only (`combatCameraConfig.ts` screen-height-ratio 0.3→0.24, one divisor in `computeDesiredZoom`, no hitbox/damage/hp touched); the dead-sprite fix roots the REAL bug (`useDungeonStageBattle`'s `subscribe` was memoized on a stale `[orchestrator]` while `dungeonOrchestrator.loadStage` disposes+rebuilds the runtime per stage, so from stage 2 the store stayed subscribed to a disposed runtime whose listener Set was cleared — no notifications, dead sprites persist; fixed by tracking `activeRuntime` as explicit state); #100 objective-clock + #102 Erlang untouched (`stageConfig.ts` empty diff, `StageVariationSystem.ts` comment-only); the +5 tests real (boundary-pinned, cross-checked vs the untouched catalog incl trial-08=mini-boss). Main re-verified independently (tsc/oxlint direct/910 tests/build) then fast-forward-merged c3c82aa, branch deleted. The revert→re-apply history (76b7761 applied → c20b116 reverted → c3c82aa re-applied-after-QC) is the honest belt trail, not history rewriting. QC's two non-blocking observations queued as task #33 — the dead-sprite fix has ZERO regression coverage (`DungeonSession.test.tsx:85` mocks the hook wholesale, so the repaired subscribe/swap path can regress silently; a proven-good test to write per rule 12) + the camera baseline const now names a link (`COMBAT_CAMERA_V082_BASELINE`) that no longer holds for that one field. Landed alongside: the 3 low-stakes lane decisions from the prior lane-expansion all closed — #8 owner-ratified keep-separate for system 08/skill-targeting (registry a4b1331; softTarget.ts is its own home, AllyAISystem 2nd consumer), #19 SKIP codeql paths-ignore (keep full SAST scan; the CI-trigger premise was already falsified — CodeQL runs fine on master-push), #20 dismiss Scorecard security-policy alert #1 won't-fix + reformat SECURITY.md's bare advisory URL as a `[Report a vulnerability](url)` markdown anchor (0b73367) so the OSSF probe sees "linked content". Standing precedent: a bypassed merge is NOT fixed forward — it is reverted to a belt-controlled base and re-entered at the MAKE station, so the QC gate that was skipped actually runs on it.

188. **#25 server-owns-progression code LANDED — first full claude -p caretaker → fresh-eyes gate → merge cycle + the roster/dispatch corrections (2026-08-10, Operator: HetCreep switch "เริ่ม #25, CB confirm เลย", Agent: Claude Code as belt-end main on Fable 5)** — the CB-audit F1/F4 fix (`b0c66f5`, squash of `security/25-server-owns-progression`; migration RELAY-PENDING). CoalBoard convened (3 blind opus lenses — empirical/formal/adversary — `.coalboard/reports/server-owns-progression-2026-08-10.md`) found the "targeted level/exp column revoke" scoped from the #103 probe was NECESSARY BUT INSUFFICIENT: the adversary reproduced that `commit_lobby_battle_progression` is a wide-open parallel write path — any authenticated client calls it with arbitrary level/exp (no bound, idempotency guard read-then-overwritten from client `p_flags`, no rate limit) AND mints an unowned hero (ON CONFLICT INSERT, arbitrary `p_lead_character_id`, SECURITY DEFINER). Owner chose scope **B — harden the RPC** (over A defense-in-depth-only / C full server-recompute). The migration (`20260810130000`, timestamp load-bearing > 20260810110000 or a replay re-opens it): narrows BOTH tables' column allowlists to drop level/exp/exp_to_next (both were already column-locked — profiles by 0009, owned_characters by 20260808204905/p9); adds a server-owned `lobby_progression_commits` idempotency ledger (guard OUT of client p_flags); rate limit + ownership check (UPDATE-only, kills the mint) + monotonic + per-call gain ceiling 20 (the client's own `applyBattleExp` guard<20 max) + hero cap 60 with a `clamp_zero` exemption at level 60; EXECUTE lock (public/anon revoked). Client `savePlayer` stops sending the 6 columns (deploy-first). **The belt flow, done right for the first time end-to-end via the ROSTER:** dispatched the SYSTEM-25 caretaker (`56ad0a85`) via `claude -p --resume <sid> --model opus` (NOT an ephemeral Agent spawn) with a past-feed step (read MEMORY 183-187 to re-sync a seat dormant since its 2026-08-09 onboarding) to ADOPT+own+refine the CoalBoard draft — the caretaker caught a `clamp_zero` max-level edge the ephemeral draft missed (level-60 hero returns exp 0, a blanket `<=0` reject would have frozen the whole account) and crystallized `MEMORY/25-*.md`. Fresh-eyes opus QC (SPAWNED, the belt-correct gate mechanism — a gate has NO hired sid by design, maker≠gate) reproduced the board exploit IN PGLite (raises, mints nothing, level unchanged) + mutation-verified every test; main re-verified independently (tsc/oxlint/**915 tests**/build). **Four owner corrections earned this session, all durable-saved:** (1) don't default-to-self — dispatch to the deepest-knowledge seat; (2) use the tier the owner set (Fable), opus is the sanctioned sensitive floor not a forbidden down-tier; (3) the belt runs on `claude -p --resume <sid>` persistent caretakers (roster in `AGENT_REGISTRY.md`) + a past-feed, NOT ephemeral spawn — with the split that GATES stay spawned-fresh (no bias); (4) verify a capability upfront + surface blockers early (main can't self-grant `Bash(claude:*)`; the auto-mode classifier blocks self-escalation — the OWNER added the permission, which unblocked the whole claude -p dispatch mechanism). Migration relay-pending: owner pastes `supabase/migrations/20260810130000_*.sql` into the SQL Editor AFTER the client CI-deploys (else 42501 rolls back all saves) — 3 caveats in the file header + the QC record (prereq `select proname ... in ('check_and_log_rpc_rate_limit','commit_lobby_battle_progression')` both present · all-or-nothing transactional DDL · register `schema_migrations` by hand after). SECURITY.md rule-19 update DEFERRED to post-relay (the server-ownership posture isn't live until the migration applies). Residual = board scope C (no absolute account-level cap, no server EXP recompute) — tracked, out of #25 scope. #26 (F2 skill/talent, still client-writable by design here) is the sibling task.

189. **The 12-agent audit, the memory split, and five lanes that all failed their first gate (2026-08-10, Operator: HetCreep, Agent: Claude Code as belt-end main)** — the largest single sweep this repo has had. **THE AUDIT:** a gold-standard run (7 opus dimension scanners, fresh web-verified exemplars) scored the project **68% overall** (Security 78 · Compat 72 · Testing/CI 71 · Observability 70 · UX-DX 68 · Governance 67.5 · Correctness 65 · Docs 65 · Maintainability 60 · Distribution 59 · Performance 56), then all 7 remaining CoalMine canaries flew (supply-chain, resilience, telemetry, testability, scale, drift, rot-DEEP), then a 5-seat CoalBoard at nasa rigor (data/opus · truth/**fable** · feeling/opus · adversary/**fable** · sub4/opus) — the owner's `fableConsent: always` persisted to `.claude/.coalboard.json`. **52 findings, all filed as tasks #34-#87, 6 of them P0.** The board's structure earned itself twice over: `truth` and `drift-canary` converged on the `grant_item` overload from different evidence; `feeling` (which RUNS things) measured that the citation checker reports 81 defects and **exits 0**; `data` (which FETCHES) pulled the LIVE Scorecard — 6.8, Signed-Releases 0/10 because `actions/attest` targets `dist/**` which is never released; `adversary` found a SECOND lying test (`formulas.fuzz.test.ts:46` promises "does not exceed the original" and never asserts it — and the claim is FALSE for negative inputs, which main verified by hand); and **sub4, summoned only because nasa forces an out-of-frame check on consensus, found the thing all eleven other agents missed**: `0006_guest_cleanup.sql:31-33` deletes anonymous accounts by `created_at`, i.e. ACCOUNT AGE, so a guest who plays every day is deleted on day 31 — the sibling job at `0014:62-74` carries all three activity guards the guest job lacks, the app tells players "ข้อมูลไม่หาย", and the backend went live 2026-08-07 so the first irreversible deletion fires ~2026-09-06. **OWNER RULINGS:** context-budget work approved but the law stays binding · CAPTCHA enabled server-side (`security_captcha_enabled: true`, verified live; main's Management-API channel is unaffected, scripted sign-in is the only casualty, and main will NOT work around a CAPTCHA it can simply ask to have toggled) · branch protection = outsiders only, `enforce_admins` stays FALSE because GitHub forbids self-approval and a solo reviewer would be locked out of their own repo — MEMORY 180's original call was right and the audit's framing was wrong · Signed-Releases is NOT applicable to a Pages SPA that distributes no artifact, so fix the CLAIM not the pipeline · and the memory split by AUDIENCE: `MEMORY/<system>.md` is the belt seats' on this machine, root `MEMORY.md` is every dev on every machine. **THE MEMORY SPLIT (this commit's own subject):** root MEMORY.md went 156,117 → 41,506 bytes (−73%) as a 285-line index with all 188 bodies verbatim in `MEMORY/archive/` blocks of 25; `agent-memory-law.md` §1a records the audience ruling and resolves a real contradiction where dispatches said "do not touch MEMORY.md" while rules 2/3a said every seat must maintain it — a seat that obeyed the law looked like it broke a rule, and N parallel lanes each appending to one file guaranteed conflicts in the record whose whole job is reliability. **THE VERIFIER IS ITS OWN LESSON:** as written it read the worktree (CRLF under `core.autocrlf=true`) against a `git show` (LF) and reported 0/188 and 387 failures on a perfectly good tree; main then raised a FALSE alarm of its own on top, because `git cat-file blob | grep -c $'\r'` counts CRs the Windows pipe inserts — settled only by comparing raw blob buffers in node (188/188, LF both sides). Rebuilt to compare content eol-normalised, assert LF on the committed blob, PIN the pre-split base at 5531ebb (comparing against a moving `origin/master` made the tool destroy its own baseline the moment its own work merged), and print which of its two modes actually ran instead of claiming the migration proof in both. **THE WAVE:** nine lanes were routed by file-collision, six dispatched — five to `claude -p --resume` caretaker seats (sys25 SQL on fable, sys22 persistence, sys27 observability, sys09 battle-loop, aide CI) plus belt-credits for asset provenance. **ALL FIVE FAILED their first QC gate**, every finding real and invisible to its own maker: a `revoke ... from public, anon` that leaves `authenticated` holding EXECUTE because Supabase's bootstrap grants it directly (proven in a Supabase-shaped PGLite fixture, and the repo's own test harness is structurally blind to that ACL class) · two phantom item ids seeded into the very catalog table added to stop unreleased items being minted · a crash-screen backup button that still fails 100% because its new callee is a stub · a `normalizeError` that can THROW and is called outside the try, turning the last-resort error net into the error · a hoisted `living` list that lets a corpse shove a live enemy **103.4 units**, measured with a fixed-RNG probe · publish-hoist mutation surviving at 2 of 3 sites while the seat's own memory claimed all three were verified · a bundle tool whose replacement baseline was wrong in the same block it was fixing · and a `fetch` wrapper that stacks on postgrest-js's own retry so a 15s deadline becomes ~127s and up to 64 requests. A `tools/test-lock.sh` was added mid-wave after the owner flagged CPU contention: lanes write in parallel, `npm test` takes turns, so a resource-starved false failure can never be mistaken for a regression. **STANDING LESSON:** maker≠gate is not ceremony. Five seats, five clean-looking deliveries, five real defects — and the one finding that mattered most came from the blind seat that was summoned precisely because everyone else agreed.

190. **Asset provenance recorded, and the day a document had to be corrected twelve times to stop lying (2026-08-10, Operator: HetCreep, Agent: Claude Code as belt-end main + belt-credits seat + belt-qc across four gate rounds)** — two unrelated pieces of work, kept together because they taught the same lesson from opposite directions. **PART A — THE PROVENANCE RECORD (merged).** The repo shipped 870 tracked binary assets with **zero** origin records, under a `README.md` that claimed "every image is original — no external assets", cited a header comment in `src/game/characters.ts` that does not exist, and asserted "no copyrighted character design appears directly" against the project's own never-checked finding. `assets/ATTRIBUTION.md` now exists: ten commits on `docs/asset-attribution`, four full QC gate rounds plus a scoped re-verify, **DRAFT by design**. **What is now measured and gate-verified in git:** the art author is **`kaoshock123`** — owner-confirmed as the person who answered the provenance questionnaire, so the record is first-hand — and **849 of 870 assets are theirs**, the only exceptions being the 8 Kenney CC0 sounds and the 13 `erlang-shen-skill-2-fx` frames. All **693** non-Erlang assets trace to them: 0 other authors, 0 unresolved, re-derived independently by the gate across all 870 files. **180 of the 193 images** in HetCreep's `3730322` ("inward reimpl of #102") are **byte-identical** to `kaoshock123`'s unmerged `e1fd040` — the inward reimplementation applied to the CODE; the art was carried across untouched. The repo has **two root commits** (`62c0000` kaoshock123 19:06, `12bcd09` HetCreep 21:59, same day) holding **344 byte-identical image blobs at identical paths**, which is why a naive path-introduction count misattributes ~470 files to HetCreep. **`nustanakritwithai` committed 20 image files and authored none** — `c58c3c2` is a pure `R100` rename at identical blob SHAs, `062dcda` a WebP re-encode of those same ten. **Still open and load-bearing, the reason it stays DRAFT:** reference/source images were used and *their original sources are not recorded*; no prompts or seeds were retained; `favicon.svg` origin UNKNOWN; the Adobe product and channel unrecorded; **no provider's commercial terms are stated anywhere, deliberately** — those must be retrieved before a payment gateway is wired. **PART B — TWO ACCOUNT-DELETION CRON JOBS DISARMED (prod).** Item 189's sub4 finding (`0006_guest_cleanup.sql` deleting active guests by ACCOUNT AGE) got its zero-risk interim: `cron.unschedule('cleanup-stale-guest-accounts')`. Verifying that led straight to a sibling **the entire 12-agent audit had missed and which main had explicitly vouched for as the good example** — `cleanup_dead_unplayed_accounts`, ACTIVE daily at 03:30, deleting **registered** accounts by `created_at` with a single `battle_history` guard, no `cleanup_exempt_profiles` (the table exists; the function never reads it), no `topup` guard, no `last_sign_in_at`. Measured on prod: 0 deletions that night, **15 of 17 registered accounts qualifying within 30 days**. Also unscheduled. All three surviving cron jobs were then read line by line — none touches `auth.users` or `profiles`. **THE LESSON, one failure mode wearing four costumes, and main supplied every one of them.** Main searched 1,727 local `.jsonl` transcripts for Adobe MCP invocations, found zero against a working control of 272 browser-tool calls, and told the seat to write **"no Generative Fill entered the chain"**. Then, corrected, **"the owner did it by hand in the desktop app"**. Then, corrected again, **"an unresolved discrepancy"**. Then **"the credit balance is route-independent"** — an assertion about Adobe's metering policy stated one sentence before the file disclaims characterising any provider's policy. `git` dissolved all of it: the generation ran on **kaoshock123's machine**, and HetCreep's own fitting pass (`16f764b`) is dated **2026-08-06** while the earliest retained transcript is **2026-08-08**. **Absence of evidence was read as evidence three times before anyone asked whether the evidence could have existed there at all.** The seat produced the same class independently — two per-commit splits of the 693, both published as "measured", both retracted; the second because the attribution map recorded the *most recent* add instead of the earliest (`git log` walks newest-first and `--reverse` was missing). And a revision put **`nustanakritwithai`'s name on 20 images they did not author**, because the file's own byte-tracing rule was applied rigorously where a correction was expected and not at all where the number looked unremarkable — *selective rigor, not bad arithmetic*. The final document **declines the apportionment entirely** and says why: three reasonable measures give 510 / 366 / 194 for one commit, and all of them land on the same author anyway. **Removing unsupported precision produced a stronger document than any of the numbers did.** Gate round 4's catch is the one to remember: a retracted sentence had been written in **two** places and the retraction commit fixed **one**, so "the owner's *human* mechanical fitting pass" survived four commits and three gates — in the section written to be handed to a lawyer, where human authorship is the entire question. **Retracting a claim means sweeping every site it was stated, not the site where it was noticed** — the documentation form of fixing one call site and leaving the sibling, which Part B hit on the same day in SQL. **Not one of the twelve corrections was caught by the party that wrote it.** Every single one came from the QC gate or from HetCreep.

191. **DESIGN LOCK — game values are BASE × SCALE under one rule, never per-case magic numbers (2026-08-10, ruled by HetCreep, recorded by Claude Code as belt-end main)** — **THE LOCK, in the owner's words:** *"ไม่ว่าจะเป็นศัตรูตัวไหน ๆ ให้กำหนดด้วย base แล้วสเกลเอา ไม่ต้องปรับจูนเยอะ หลักการให้ใช้กฎทอง"* — every enemy is defined by a **base** value and derived by **scale**; there is no per-stage, per-wave, per-encounter tuning knob. The owner tied it explicitly to a lock they had already made on the UI side: **`AnchorPin(X,Y) + (BaseDimension(W×H) × AdaptiveScale) + SafeAreaInsets`**. **"กฎทอง" means the `/gold-standard` method** — owner-confirmed 2026-08-10 after main guessed wrong twice. It is neither the golden ratio nor a vague "one governing rule": the base/scale rule itself is to be **derived by running a gold-standard pass against named world-class exemplars**, not invented in-house. Main first read it as "the single governing rule", which makes the owner's sentence *"หลักการให้ใช้กฎทอง"* circular — "for the principle, use the principle" — and that circularity was the tell. The owner asked *"กฎทอง = /gold-standard หรือฉันทำให้คุณเข้าใจผิด?"* rather than correcting outright; the honest answer was that they had been clear and main had guessed. **ENFORCEMENT, owner-stated and standing:** *"PR ไหนหลุดค่าแบบนี้มาอีกให้แปลงมันใหม่เดี๋ยวนั้นเลย"* — a PR arriving with a per-case tuned value is CONVERTED to base × scale on the spot, at intake, not merged-then-filed. **THE REASON, which is the part to carry forward:** *"มันคือการสร้างระบบที่ไม่คิดถึงตอนบำรุงรักษาระยะยาว"* — a magic number is cheap to write and expensive forever; each one is a place a future change has to remember to visit, and the system stops being derivable from its own rules. **WHAT TRIGGERED IT:** the perf lane `perf/audit-wave1-battle-loop` carried a one-line change threading `state.enemyHpScale` into `createWaveEnemies` for wave 2+, inside a commit titled "cut per-tick allocations". The QC gate caught it as a scope crossing — a gameplay-difficulty change riding a perf review's authority, in files unmerged PR #107 also edits — and HELD it for the owner rather than ruling on it, which is exactly what belt law asks of a gate. Main then compounded the error in the other direction: it reported the impact as "all survival stages, waves 2+", when the measured truth was **one wave of a two-wave tutorial stage in `P5_TEST_DUNGEON`, a vertical-slice test dungeon** (`dungeonConfig.ts:4`, *"Vertical slice dungeon — data-driven, no hardcoded stage index logic"*). The config's own comment reads *"Tutorial-easy: ~30% less enemy HP on stage 1 only (Ring 0)"* — scope stated as the STAGE while the code applied it to wave 0 only, so the change was arguably a bug fix toward the documented intent. **The owner refused the question as posed** — neither "fix the bug" nor "fix the comment", because `enemyHpScale: 0.7` as a hand-placed per-stage knob should not exist in either form. **STANDING LESSON, distinct from the audit lessons in item 190:** a gate that HOLDS a product decision instead of ruling on it is working correctly, and an owner who answers a two-option question by rejecting both options is telling you the options were framed inside a broken model. Neither of those is a failure to route around; both are the system doing its job. The failure that WAS main's: inflating a measured one-wave change into "all survival stages" before checking what `P5_TEST_DUNGEON` actually is — the same read-then-assert-beyond-the-evidence habit item 190 records four instances of, applied to a config file instead of a log.
