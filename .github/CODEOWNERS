# Every path in this repository is owned by HetCreep.
#
# Paired with `require_code_owner_reviews` on the master branch protection, this makes
# HetCreep's approval mandatory on every pull request. It exists because two non-admin
# collaborators holding write access could otherwise approve each other's work and land
# it with the owner never in the loop — the "1 approval" rule alone does not say WHOSE.
#
# This encodes AGENTS.md rule 18: the owner is the standing merge controller. Break-glass is
# unchanged and deliberate — branch protection keeps `enforce_admins: false`, so a
# repository admin can still land work directly when HetCreep is unavailable. That path is
# meant to be logged, not silent (rule 18's break-glass clause).
#
# A single line, deliberately. A per-path owner table would be a second source of truth
# about who reviews what, and it would drift from this file the first time a directory moves.
* @HetCreep
