# MEMORY.md — Project State & History Journal

> **Operator / Human User**: `HetCreep`
> **Repository**: `LegendofSoulTH/LegendOfSoulTH`
> **Default Branch**: `master`
> **Last Updated**: 2026-08-07T11:25:00+07:00 by `Cursor Agent (cloud)`
> **RULES_VERSION last synced: 14** (see `.agents/rules/rules-freshness-check.md` — this exact line is what the check greps for; the compression pass that rewrote this file dropped it, restored here)

> **2026-08-06 overhaul**: this file had grown to 65+ interleaved, verbose items (two colliding numbering
> tracks from concurrent sessions/forks) and was getting expensive to read every session. Compressed to
> essentials per HetCreep's explicit request. **Full narrative detail for anything below now lives in
> `git log`/`git show <sha>` — commit messages this session are detailed and are the source of truth for
> "why," not this file.** Keep future entries to 1-3 lines; link a commit SHA instead of re-narrating it.

---

## 👤 Identity & System Context

- **Owner**: `HetCreep`
- **Project**: Legend of Soul-TH (repo slug `LegendOfSoulTH`) — mythic real-time action RPG, React 19 + TypeScript (strict) + Vite 8 + Three.js/R3F, Oxlint, client-only (no backend, localStorage persistence)
- **Live**: https://legendofsoulth.github.io/LegendOfSoulTH/

---

## 🎯 Current Status

- **Repo**: 🟡 local commit เหลือ push เอง (`f22f732`, working tree สะอาด) — `git push` ผ่านเครื่องมือ agent ใช้ไม่ได้ในเครื่องนี้ (GCM ต้องการ tty ที่ sandbox ไม่มี), HetCreep รัน `git push origin master` เองจาก terminal ปกติ. `npm run ci` (typecheck+lint+test+build) green — **195 tests / 28 files**, รวม fast-check property-based fuzz test 4 ไฟล์ (DamageSystem/formulas/format/vite.config resolveBasePath — ปิด gap "Fuzzing" ของ gold-standard) + component test 4 ไฟล์ (AuthModal/ErrorBoundary/GlobalErrorBanner/EnemyHealthBar) · lint gate เป็น `oxlint --deny-warnings` warning เหลือศูนย์ (fast-check ต้อง named import เช่น `import { assert, property } from 'fast-check'` ไม่ใช่ `import fc from 'fast-check'` — เจอ `no-named-as-default-member` ถ้าใช้ namespace import). Coverage วัดได้แล้ว (`npm run test:coverage`) — **31.77% statements** (ขยับจาก 24.42% หลังเพิ่ม component tests).
- **Performance (task #12, 2026-08-07)**: `vite.config.ts` แยก `vendor-react` chunk ออกจาก app code (react เปลี่ยนนาน ๆ ครั้ง, cache ข้าม deploy ได้) — three.js ไม่ต้องแยกเพิ่ม อยู่ใน chunk แยกจาก `lazy()` ที่ LobbyScene/BattleScene อยู่แล้ว. `tools/optimize-images.mjs` เพิ่ม `MAX_WIDTH_BY_DIR` (resize ก่อนแปลง WebP, `withoutEnlargement`) — คำนวณจากขนาดแสดงจริงที่ตรวจแล้ว (ตัวละครแสดงกว้างสุด 540px, พื้นหลังออกแบบที่ 1600x900) ผลจริงวันนี้: มีแค่ 6 ไฟล์ (`monkey-attack-new-12..17`) เกินเพดาน 1200px ที่เหลือ (ui/backgrounds) เป็น ceiling กันของอนาคต ไม่ใช่บีบของเดิม (คล้าย pattern ของ `build.target`). **Service worker/precache — ตัดสินใจข้าม**: ชั่งแล้วว่าเสี่ยงชนกับระบบ deploy-gate ที่เพิ่งทำ (SW cache เก่าอาจค้างขวางไม่ให้ผู้เล่นเห็นเวอร์ชันใหม่ที่ตั้งใจ gate ไว้) ประโยชน์ (แคชรอบสอง) ไม่คุ้มความเสี่ยงตอนนี้ — ไม่ได้ implement.
- **Resilience fixes (task #13, 2026-08-07, เสร็จครบ)**: `RealtimeBattleRuntime.requestExit()` เคยทับ `state.status` เป็น `'exiting'` แม้ผลตัดสินแล้ว (`'victory'`/`'defeat'`) — guard แล้ว ผลที่ตัดสินแล้วแก้ไม่ได้อีก. `accountRepository`'s `Database` เพิ่ม `rev` counter — `saveDb()` เทียบ rev ปัจจุบันในสตอเรจก่อนเขียนทุกครั้ง ปฏิเสธการเขียนถ้าอีกแท็บ (หรือแม้แต่ async operation อื่นในแท็บเดียวกันที่ interleave ผ่าน `await`) เขียนแซงไปแล้ว — คืน `false` เข้าช่องทางเดิมที่ทุก caller เช็คอยู่แล้ว ไม่ต้องเพิ่ม UI ใหม่. ทั้งสอง fix differential-verified (ปิด guard ชั่วคราว ยืนยันเทสต์แดง แล้วคืนกลับ). เพิ่ม prettier (`.prettierrc.json`, semi:false/singleQuote/trailingComma:all/printWidth:100 — อ่านค่าจาก `.editorconfig` เดิม) ผูกกับ lint-staged เท่านั้น (ค่อย ๆ format ไฟล์ที่แก้จริง) **ไม่ผูกกับ `npm run ci`** เพราะโค้ดเดิมยังไม่ prettier-clean ทั้งชุด (ต้อง format ทีเดียวทั้งรีโปก่อนถ้าจะผูก CI ทีหลัง). CI ผูก Node 22/24 matrix — ต้องเพิ่ม job `ci-status` (fan-in) คงชื่อ "Continuous Integration" เดิมไว้ ไม่งั้น branch protection ที่ required check ตรงชื่อนี้เป๊ะจะพัง (ยืนยันกับ live API แล้ว). `CONTRIBUTING.md` เพิ่มหัวข้อ Release process.
- **Auto version-bump policy** (HetCreep, 2026-08-07): "auto bump ตลอดเมื่อ main ตัดสินว่าลงตัว" — bump `GAME_INFO.version` (+ `package.json` + `CHANGELOG.md`) เองได้เมื่อจบงานที่เห็นว่า "ลงตัว" แล้ว ไม่ต้องถามซ้ำทุกครั้ง (ยืนยันด้วยการยิง deploy-gate ให้ตัดรอบ release ใหม่).
- **releases page เคยว่างเปล่า — root cause หาแล้ว** (2026-08-07): commit ที่ bump เป็น 0.2.0 จริง (`fdaf569`) run deploy ถูก **cancelled** (ชนกับ push ถี่ + GitHub Actions outage ที่เริ่ม 2026-08-06 15:22 UTC) ก่อนตัด release สำเร็จ — ทุก commit หลังจากนั้นเวอร์ชันยังเป็น 0.2.0 เดิม gate เลย skip ถูกต้องทุกครั้ง (ไม่ใช่บั๊ก) ไม่เคยมี release ไหนสร้างสำเร็จเลยจริง ๆ ตั้งแต่ระบบนี้มีผล ต้อง bump เวอร์ชันใหม่อีกครั้งเพื่อให้ gate สร้าง release รอบแรกจริง. GitHub status ตอนตรวจ (2026-08-07 ~04:00 UTC) ยังเป็น **Partial System Outage**.
- **Gold-standard FINAL AUDIT (task #14, 2026-08-07, เสร็จครบ)**: 6 scout พร้อมกันคะแนนจริงต่อไฟล์จริง (ไม่ใช่เดา) — **รวม ~84%** (ขยับจาก baseline 58% ของ 2026-08-06) — Security 91% · Distribution 89% · Observability+Error-handling 97% · Compatibility 84% · Governance 88% · Docs 86% · Maintainability 88% · UX/DX 85% · Correctness 83% · Testing/CI 69% · Performance 62% (ตัวต่ำสุด — component coverage 4/58 `.tsx`, `<img>` ไม่มี width/height, ไม่มี bundle-budget gate ใน CI). รายละเอียดเต็ม + gap register (MUST vs EXCELLENCE, priority) → `.agents/rules/gold-standard-baseline.md` หัวข้อ "2026-08-07 FINAL AUDIT". นี่คือรายงาน ไม่ใช่คำสั่งไปแก้ทุกจุดทันที — ตาม `proven-good-do-it-now.md` เอง.
- **Task list**: #1–#14 ครบทุกข้อจาก "เติมให้ 100%" ของ HetCreep แล้ว. งานที่เหลือ (จาก gap register ด้านบน) เป็น backlog รอ HetCreep ชั่งน้ำหนักว่าจะทำต่อข้อไหน ไม่ใช่คิวที่ต้องเดินต่อเอง.
- **Auto-bump policy (2026-08-07, ตั้งโดย HetCreep)**: bump `GAME_INFO.version`/`package.json` เองทันทีที่งานหนึ่งก้อนลงตัว (build+lint+test+build ผ่าน + commit แล้ว) ไม่ต้องถามก่อน — deploy gate ผูกกับเลขนี้อยู่แล้ว.
- **Deploy webhook หลุด 1 ครั้ง (2026-08-07)**: push bump `cf8b5de` (0.3.0) เอง workflow `deploy.yml` ไม่ถูกจ้างเลย (0 run สำหรับ commit นั้น) — ช่วงที่ GitHub ยังฟื้นจาก outage (สถานะตอนนั้น "Partial System Outage") แก้ด้วย manual `workflow_dispatch` (`force:true`) ผ่าน REST API แทน — deploy/release ผ่านปกติ. commit ถัดไปด้วยกลไกปกติ (0.3.1) trigger เองสำเร็จไม่ต้องแทรกแซง — ถ้าเว็บไม่อัพเดตหลัง push ที่ bump เวอร์ชันแล้ว เช็ค `actions/runs?head_sha=<sha>` ก่อนสงสัยโค้ด (อาจเป็นแค่ webhook หลุด ไม่ใช่บั๊ก).
- **Session expiry (2026-08-07, แก้แล้ว, v0.3.1)**: `accountRepository.ts`'s session เดิมไม่มี TTL เลย (`{uid,email}` เท่านั้น) ล็อกอินครั้งเดียวค้างตลอดไป — เพิ่ม `expiresAt` แบบ sliding window 30 วัน (`readActiveSession()` เป็นจุดอ่านเดียวที่ทุกฟังก์ชันต้องผ่าน, ต่ออายุทุกครั้งที่อ่านสำเร็จ, session รูปแบบเก่าไม่มี `expiresAt` = หมดอายุทันที).
- **History rewrite (อีเมลแอดมินเก่าใน git log)**: ตัดสินใจแล้ว **ไม่ทำ** (HetCreep เลือก "ไม่แตะ (แนะนำ)" 2026-08-07 หลังเจอ fork จริงที่ยัง active — `nustanakritwithai/GameTurnBase`) — ห้ามหยิบขึ้นมาทำอีกโดยไม่มีคำสั่งใหม่.
- **Version**: **0.2.0** — เลขอยู่สองที่และต้องตรงกันเสมอ (`src/game/gameInfo.ts` = เลขที่ผู้เล่นเห็น + เป็นตัวสั่ง deploy, `package.json` = เลขที่ SBOM/tag ใช้) `src/game/gameInfo.test.ts` บังคับไว้
- **Battle**: realtime action battle (`src/game/realtimeBattle/`) is the ONLY live combat path — attack, dash, 3-hit combo, one skill (Monkey King only, `skills.ts`). Old turn-based system (`src/game/battle/engine.ts` etc.) fully deleted (twice — see Lessons). `types.ts`/`formulas.ts` in that same folder are NOT dead — still imported by `BattleResultAdapter.ts`/`useGameFlow.ts`/`DamageSystem.ts`, keep them.
- **Lobby entry**: "ต่อสู้" and "เริ่มการผจญภัย" both open `LobbyBattleSession` → `trial-01` directly, skipping exploration. Exploration/NPC/dialogue subsystem (`GameExplorationSession`, `useGameFlow`, `game/dialogue/*`, `game/npc/*`) still exists but has **zero live entry point** — deliberate per PR #11 (ask-CB flagged concerns: mislabeled "เริ่มการผจญภัย" button, `trial-02` unreachable, no exploration re-entry — HetCreep chose to merge anyway, accepting the trade-off). Files remain; wire a new entry rather than rewriting.
- **Skill system** (PR #12, `c58c3c2`): ask-CB'd 2026-08-06 — code/architecture solid (3/4 seats bless as-is), one real bug found: `SkillButton` mobile-landscape touch target ~41.3px, under this file's own established 44px floor (Attack/Dash in the same file already fixed to this bar). Fix pending.
- **Other-dev systems ask-CB backfill**: complete as of last sweep — Auth/Economy/Roster/3D/Battle/Exploration/Dialogue/UI-HUD/Assets/Infra, WorldChat, realtime battle overhaul, 3 input systems, Items/Friends/WorldChat-admin, PR #11, PR #12 all reviewed. Standing rule still active: **any new system merged by another dev gets sent through the 4-seat ask-CB opinion lane (realtime/reality/feeling/outdim) retroactively, reworked per verdict** — flag if a future sweep finds a gap.
- **Reward integration**: deferred, not a risk yet — `BattleResultAdapter.ts` hardcodes `earnedGold`/`earnedExp`/`droppedItems` to `0`. Revisit the client-authoritative trust boundary consciously when real rewards get wired up.
- **Open/unbuilt, disclosed not hidden**: no quest system, no real drop table, no gamepad support. Audio placeholders unresolved by choice: `portalOpen`/`levelUp`/`victory`/`defeat` SFX + lobby music (no confident mood-matched CC0 file found). `ItemsModal`/`AddFriendModal`'s "block" list has no populating action anywhere — honestly empty, not broken.
- **UI/UX gold-standard backlog**: `.agents/rules/gold-standard-baseline.md` — ~70-89% depending on dimension, MUST-HAVEs closed, EXCELLENCE-tier gaps tracked there with exemplar citations, not scaffolded (3D character-select keyboard path, tab-strip `aria-controls`, `AuthModal` live-validation, breakpoint tokens, a few reduced-motion gaps).
- **Security posture**: client-only, no backend — documented in `SECURITY.md`'s Out-of-Scope (editable localStorage, demo PBKDF2 auth, client-only admin gate in `src/data/admins.ts`). CodeQL + Dependabot + Secret Scanning + Gitleaks + NPM Audit + branch protection (basic) + Action SHA-pinning all on. `secret_scanning_validity_checks` won't toggle via API — check manually at Settings → Code security.
- **Repo access** (re-verified 2026-08-07 against the live API): collaborators are `HetCreep`, `nustanakritwithai`, and `DemoGODRTX`, **all three holding full `admin`**. **`kaoshock123` = `DemoGODRTX`** (confirmed by HetCreep 2026-08-07) — the git author name and the GitHub account differ, which is why an earlier note read as an unknown identity with push access; it is not. `src/data/admins.ts` เคยใช้อีเมลจริงของบัญชีนั้นเป็นค่าประตูผู้ดูแล — เปลี่ยนเป็น `admin@legendofsoulth.invalid` แล้ว 2026-08-07 (ask CB 4/4). Note for the Ring check in `.agents/rules/ring0-authority.md`: it keys on `git config user.name`, so a session committing as `kaoshock123` does **not** match `HetCreep` by name — grade Ring by the authenticated actor there, not the git author string alone.
- **Branch protection** — `required_status_checks: null`, `required_pull_request_reviews: null`, `enforce_admins: false`, rulesets `[]`. All three admins can push straight to `master` past every CI gate; ~93% of the history (199 commits, 13 PRs) went in that way. **Open, HetCreep's to decide** — it is a real trade-off, not an oversight: requiring PRs on a repo where the owner is the main author adds friction with no reviewer to gain. The narrower move is required status checks without required reviews. Not an agent's call.
- **CI/Deploy** (กติกาปัจจุบัน ตั้งแต่ 2026-08-07): **เว็บปล่อยเมื่อ `GAME_INFO.version` เปลี่ยนเท่านั้น** ไม่ใช่ทุก push — job `gate` ราคาถูกตัดสินก่อน แล้วจึง build → test → deploy → ตัด GitHub Release พร้อมแนบ SBOM · push ที่ไม่ได้ bump จบที่ `gate` · อยากปล่อยโดยไม่ bump กด Run workflow เอา · วิธีปล่อยเขียนไว้ใน `README.md` หัวข้อ "การปล่อยเว็บ"
  - ที่มา: ก่อนหน้านี้ทุก push ยิง deploy พร้อมกันแล้ว `concurrency.cancel-in-progress` ตัดกันเอง **เว็บจริงเคยตามหลัง master 18 commit** ทั้งที่ workflow รายงาน success (ก่อนหน้านั้นอีกคือ debounce + queue-check ที่ PR #13 ถอดออก เพราะ skip ทุกรันด้วยเหตุผลคนละแบบ — รอบ #88–#100)
- **Player accounts/currency/items/friends/chat**: all functional locally, entirely client-side. `WorldChat` is same-browser-only (`localStorage`+`BroadcastChannel`), not real cross-device chat — disclosed via an in-app `SCOPE_NOTE`.
- **License**: MIT (HetCreep's explicit decision, gold-standard rule forbids an agent auto-picking).

---

## 🧠 Lessons & Conventions (`[[tag]]`)

- **`[[browser-pane-not-compositing]]`** — this environment's Browser pane sometimes doesn't render/composite frames (canvas stuck at default size, `screenshot` errors "not compositing"). When it happens, verify via code review + typecheck/build + DOM/layout inspection instead of visual screenshots, and say so honestly rather than claiming untested visuals worked.
- **`[[css-fix-overcorrection]]`** — resizing/rescaling a themed frame (proportional CSS changes) without also checking what's positioned _inside_ it against the same math causes overflow/misalignment. Always recompute inner-element positions by the same scale factor, not just the outer box.
- **`[[percent-maxheight-grid-indeterminate]]`** — a percentage `max-height` on a grid/flex item whose container sizes to content (`display:grid; place-items:center`) never resolves (circular sizing). Use `dvh`/viewport units or an explicit container size instead.
- **`[[test-existence-not-authenticity]]`** — a test file testing a code path proves the code was once exercised, not that it's reachable in production today. Always trace real callers/entry points before trusting "it has tests" as evidence of aliveness.
- **`[[shared-hook-refuted-by-divergent-semantics]]`** — a "let's extract one shared hook/component" instinct across superficially-similar systems (UI modals, input handling) should be checked against real behavioral divergence first; ask-CB has independently refuted this proposal twice this project (UI/HUD systems, then input systems) because the systems' downstream semantics genuinely differed.
- **`[[verify-before-assuming-regression]]`** — a reverted-looking diff from a concurrent push/merge can be a legitimate supersession (e.g. an asset that was deleted got re-added with new content), not a regression. Check the CURRENT state of the file/asset before "fixing" it back.
- **New-systems law**: any system a session builds, or discovers another dev/session merged, that's genuinely new (not a fix/content-addition to an existing system) gets a CoalBoard "ask CB" 4-seat opinion-lane pass before being considered done. Ring-0-scoped instruction text lives in gitignored `MEMORY.local.md`; the practice itself is documented here since it governs project history.
- **Pre-push sync law**: `.agents/rules/pre-push-sync-law.md` — `git fetch` + check ahead/behind, merge any incoming commits, re-run full verify, only then push. Binding on every machine, every push.
- **Personal-scope law**: `.agents/rules/personal-scope-law.md` — personal/off-project content never goes in this file (mandatory reading has a token cost every future session pays); use gitignored `MEMORY.local.md`.
- **Commit-granularity law**: `.agents/rules/commit-granularity-law.md` — one completed task = one commit.
- **PowerShell 5.1 has no `` `u{XXXX} `` unicode escape** — silently inserts literal text instead of erroring. Use `[char]0x201C` or type the character directly; always verify inserted content with Read afterward.

---

## 📜 Timeline (compact — see `git log --oneline` / `git show <sha>` for full detail)

All entries 2026-08-05/06 unless noted. Roughly chronological.

1. Lobby scene + 2.5D graphics engine (R3F) built.
2. CI/CD + automation set up (build/typecheck/lint/CodeQL/security workflows).
3. Branch/settings consolidation.
   4-5. GitHub security enforcement + automated security workflows enabled.
4. Player accounts + gold/gem currency system (`c019bb7`, refined `6157f89`/`0a66592`).
5. ECC coding rules installed.
6. gold-standard AUDIT+FILL, 11 dimensions, ~25% (not inflated).
7. Ring system + rules-freshness-check installed.
8. Merged concurrent-session work from two machines by hand.
9. Pre-push sync law codified (RULES_VERSION→2).
10. CI cleanup, broken images fixed live, GLB models stopped shipping to prod.
11. Ring 0 detection fixed for cloud agents (RULES_VERSION→3).
12. ponytail-audit cleanup, README refresh, AuthModal Esc-to-switch-tab.
13. Battle/exploration/dialogue/NPC system merged from an untrusted external PR (`Hih#11`) — investigated file-by-file before merging (Ring 1 due diligence), 40 genuinely new files pulled in, `publicUrl()` subpath-404 sweep extended.
14. False-positive "lost account data" investigation (no real bug found — browser-automation tool artifact); commit-granularity law added (RULES_VERSION→4).
15. `SECURITY.md` added, written for this project's actual (client-only) shape.
16. `.coalmine.json` scan-exclusion fix + Dependency Review + Harden-Runner added.
17. `.coalmine.json` untracked from git entirely (personal tuning knob, not team-shared).
18. `GameViewport` fixed-1600×900 letterbox removed → fluid stage; 2 real breakages fixed (`LobbyPage` CSS Grid min-width, `WukongAdventure` world→screen coordinate mapping).
19. rot-canary follow-up: fixed a stale-`sceneSize` bug in item 20's fix (missing effect dependency).
    22-26 _(fork-side, `Claude Code cloud, Ring 1`, `nustanakritwithai/GameTurnBase`)_: realtime battle built step by step — room/runtime base, movement/camera/joystick, enemy AI chase+attack, hitbox+damage, 3-hit combo+dash. Built and verified on the fork; landed on this repo's `master` later (see #45 below).
20. Modal background unification (4 modals migrated to the gold-panel design language).
21. Version sync + first tagged release `v0.1.0`; `SettingsModal`/`CharacterRosterModal` character-count stat fixed.
22. Repo renamed `GameTurnBase`→`LegendOfSoulTH`, all infra/prose references swept.
23. GitHub "About" panel filled in (description/homepage/topics).
24. Gold made purchasable — `GemShopModal` generalized to `CurrencyShopModal`.
25. Esc-to-close audited across every modal, 4 real gaps fixed.
26. Image pipeline built (`assets/raw/`→WebP→`public/`, 77% smaller), 88MB of dead assets archived.
27. Icon-scaling audit + CPU re-render fix (`WukongAdventure`'s per-frame `setState`) + deploy-watcher banner.
28. Personal/off-project content law added — `MEMORY.local.md` created (RULES_VERSION→5).
29. FPS/render-sync pass: refresh-rate-aware `dpr` + capped React commit rate at 60Hz.
30. WebGPU primary renderer, WebGL2 fallback, `three@0.185.1`'s `WebGPURenderer`.
31. rot-canary: `WebGPURenderer` disposal-on-fallback resource leak fixed.
32. gold-standard AUDIT+FILL: ~67% vs OWASP ASVS/GitHub Community Standards/Keep-a-Changelog/SemVer/pmndrs-r3f (RULES_VERSION→6).
33. gold-standard CONFORM: 5/6 MUST-HAVEs fixed (CSP meta tag, CHANGELOG.md, CONTRIBUTING.md, CODE_OF_CONDUCT.md, husky+lint-staged pre-commit).
34. Gold-standard recomputed honestly to ~89% (not 100%) — SBOM+build-provenance attestation added, 2 real lint warnings fixed properly (not silenced), analytics declined (no backend, same wall as Sentry).
    42-45. HUD icon shrink (desktop, then reverted-on-mobile after scope correction) + naga-frame/treasury resize iterations, 3 rounds of refinement including a measured (not eyeballed) text-overflow fix by decoding the actual PNG art — `@media (min-width:761px)` restructure now the load-bearing desktop-only convention for `TopBar.module.css`.
35. Repo settings hardened via `gh api` (basic branch protection, push-protection, SHA-pin enforcement).
36. `LICENSE` installed: MIT, HetCreep's explicit decision.
37. Admin command console + `/givecharacter` + `grantCharacter()` — client-only gate, self-disclosed as not real security.
38. **10-system CoalBoard ask-CB sweep** (Auth/Economy/Roster/3D-Render/Battle/Exploration/Dialogue/UI-HUD/Assets/Infra), 40 seats, all verdicts applied: PBKDF2 120k→600k iterations + save export/import, dev-only-gated admin console (later superseded), GLB pipeline stopped shipping to prod, `SkillDefinition` discriminated-union refactor, 40+ new unit tests, `useModalA11y` shared hook (6 modals), new-systems-ask-CB law established.
39. `.agents/rules/ask-cb-on-new-systems.md` retired from the public repo (Ring-0-only content) → moved to `MEMORY.local.md` (RULES_VERSION→8).
40. `ring0-authority.md`'s hardcoded real email removed from its Ring-0 detection signal (redundant with git-identity/`gh api` checks) — RULES_VERSION→9.
41. Audio engine built (`src/lib/audio/AudioEngine.ts`) — Web Audio API, no dependency (Howler.js researched and rejected, unmaintained since 2023). Same-day rot-canary catch: `initAudioEngine()` never fired for returning logged-in players (bypassed `TitlePage`) — fixed.
42. 8 real CC0 SFX files sourced from Kenney.nl, wired into shared touchpoints (modals, dialogue, battle hits, toasts, nav buttons). `CommandConsole` reskinned into `WorldChat` — visible to every player, admin commands hidden inside, same-browser-only chat honestly disclosed.
43. "เดินชมจันทร์" character picker added to `ProfileModal`.
44. `WorldChat` retroactive ask-CB pass (first use of the new-systems-law extended to catch OTHER sessions' unreviewed systems) — found and fixed a real message-loss race (`Promise`-chained write queue), `storage` event→`BroadcastChannel`, a11y gap, zero-test gap. Escalated 2 product questions to HetCreep (label honesty → added `SCOPE_NOTE`; admin command exposure → added a private nudge).
45. Error-code helper system (`src/lib/errors/`) — `as const` registry (never `enum`, `erasableSyntaxOnly` forbids it), `import.meta.env.DEV` toggle, `tier: 'silent'|'visible'` split (deliberately deviated from the literal "wrap everything" ask, explained why), `no-console` oxlint rule enforces it going forward.
46. Loading-screen system, scope deliberately narrowed after ask-CB found the literal "every scene transition" request would be a regression (only one real async boundary exists) — reused the existing 魂 seal motif instead of a generic spinner.
47. gold-standard UI/UX AUDIT+FILL+ADOPT vs Star Rail/Genshin/Material Design 3/WCAG 2.2 AA/Apple HIG — ~70%, hand-written a11y scaffolding chosen over adding Radix (litigated twice, same answer both times), 5 MUST-HAVEs closed, rest tracked as backlog (RULES_VERSION→10).
48. Adaptive performance/FPS quality-scaling system built — live FPS-sampled tier drives `dprMax`+shadow `castShadow`, asymmetric hysteresis, tab-visibility guard, manual override short-circuits the sampler entirely.
49. AuthModal/NameModal short-viewport overflow fixed, ported from a fork PR; found the real fix needed `dvh` not `%` (grid-sizing circularity) — same bug then confirmed+fixed in 4 more modals (`task_afb9645b`).
50. GitHub Pages `base` path was hardcoded to this repo's name — forks deployed blank pages. Fixed via `resolveBasePath()` deriving from `GITHUB_REPOSITORY`.
51. Battle room was unreachable in the shipped game — 2 stacked bugs (`ExplorationControls` z-index below the scene it sat under; an NPC positioned inside a map obstacle) — both fixed.
52. Cherry-picked the fork's realtime battle overhaul (items 22-26) onto this repo's `master` — 6 commits, full static verify green.
53. Retroactive ask-CB audit of the just-landed realtime battle system found a CRITICAL gap static verify missed: battle never transitioned to victory/defeat, never advanced past wave 1. Fixed (`RealtimeBattleRuntime.checkBattleEnd()`), 3 new tests lock it in.
54. GitHub Pages deploy queue-congestion diagnosed and fixed — debounce step, then a stronger `queue-check` step (this repo's own push volume was outrunning Pages' backend, not a code bug).
55. PR #10 (`nustanakritwithai/GameTurnBase`) reviewed and merged (battle sprite/asset fixes for the realtime cherry-pick).
56. Retroactive ask-CB audit of the 3 independent input systems (battle/exploration/moonlight-walk) — broad "unify into one hook" proposal REFUTED 3/4 (genuinely divergent semantics), narrow real bug confirmed and fixed instead: `useExploration.ts` used last-write-wins instead of a held-key Set, causing stuck/dropped movement.
57. Repo-wide constant/config centralization via an `ultracode` Workflow (13 agents) — 6 new/extended central modules, 34 files, 3 confirmed CSS color-drift bugs fixed as a side effect of forcing one canonical value. Caught and resolved a real merge collision with a concurrent push mid-deploy.
58. Dead old turn-based battle subsystem deleted (9 files, confirmed zero production callers) — corrected scope twice mid-task before the final list was right (see `[[verify-before-assuming-regression]]`-adjacent lesson: always re-verify import scope with a comprehensive grep before deleting, not a partial one).
59. PR #11 (`nustanakritwithai`) reviewed via 4-seat ask-CB — flagged real concerns (mislabeled button, `trial-02` orphaned, exploration cut from main path) but HetCreep chose to merge as-is anyway, informed trade-off.
60. Full ask-CB backfill sweep across every other-dev-introduced system found so far — Items/Friends/WorldChat-admin all reviewed; `AddFriendModal` had a real bug (success toast with zero persistence), fixed (`1a8609a`). Caught a merge-conflict regression mid-push, investigated before assuming (asset had actually been legitimately restored, not broken) — see `[[verify-before-assuming-regression]]`.
61. PR #12 (`nustanakritwithai`) — realtime battle skill system — merged directly, reviewed via 4-seat ask-CB after the fact: code/architecture solid, one real touch-target bug found (`SkillButton` mobile-landscape, ~41.3px vs this file's own 44px floor).
62. **This item — MEMORY.md compressed** from 65+ verbose interleaved items to this timeline, per HetCreep's explicit request. Nothing lost — `git log` has the rest.
63. Searched the Claude Code skill marketplace (awesome-claude-code + web) for game/web-dev skills — found nothing not already duplicated by the existing Coal-ecosystem, except real 3D/R3F-specific gaps. Vendored `react-three-fiber`+`threejs-webgl` skills (MIT, `freshtechbro/claudedesignskills`) into `.claude/skills/`, ECC-style (git-tracked, zero per-dev install) — picked over a from-scratch "Game Engine" skill and a non-R3F-aware generic Three.js skill because these two name-match the actual libraries in use. Anthropic's official `frontend-design` plugin was found too but NOT vendored — its repo is all-rights-reserved, can't be copied into a third-party repo like MIT content can; HetCreep installs it personally instead (`/plugin install frontend-design@claude-code-plugins`).
64. Added `.github/workflows/upstream-skill-watch.yml` (weekly cron) — compares the two vendored/cherry-picked upstream sources' (`freshtechbro/claudedesignskills`, `affaan-m/ECC`) current HEAD sha against `.github/upstream-watch/state.json`, opens an issue with the ahead-by count + compare link if either moved, self-updates the state file after. Excluded that state path from `ci.yml`/`deploy.yml` so its own bookkeeping commits don't trigger a full build+deploy. Verified end-to-end on a real Actions run (`workflow_dispatch`), not just locally.
65. Two real lobby bugs from a player screenshot: (1) "login stuck" — `LobbyScene.tsx`'s WebGPU `renderer.init()` had no timeout; if the adapter negotiation stalls (real failure mode on some GPU drivers), the promise never resolves _or_ rejects, so the WebGL2 fallback never runs — the player sees this as the game hanging right after login. Fixed with a 4s `Promise.race` timeout. (2) "mobile scale broken" — three layout rules (`StartAdventure`/`MainNavigation`/`LobbyPage` module CSS) only checked `max-width`, never `max-height`, so a landscape-mobile viewport wider than their breakpoints but short kept desktop positioning — the "เริ่มการผจญภัย" button visibly collided with the bottom nav. Added `, (max-height: ...)` (OR) to all three, same pattern already used for BattleScene's touch-target fixes. Also found and merged two full duplicate `.button`/`@media` blocks left in `StartAdventure.module.css` from before it switched to an image-based design. Verified live via dev server + `getBoundingClientRect` (not just code review) at 4 viewports (900x400/812x375 landscape — confirmed real overlap before, clear after; 1280x720 desktop and 390x844 portrait — unaffected).
66. Full-repo CoalBoard audit (manual `/coalboard`, rigor `high` / depth L3, 4 blind seats: `[opus] data` · `[fable] truth` · `[opus] feeling` · `[opus] adversary`, judge on main) over 214 files — `src/**` + `.github/**` + `tools/**` + root config. Full report (gitignored, local only): `.coalboard/reports/audit-2026-08-07-001709-v0.1.0.md`. Found and fixed **1 CRITICAL + 5 HIGH**: (a) `importSave()` validated only the account fields, never `player` — and it commits the account + session to localStorage _before_ dereferencing `player`, so a save file without one bricked the game permanently (every reload re-threw from `getSessionPlayer`, only clearing localStorage recovered it); (b) `loadDb()` returned the shared module constant `EMPTY_DB` rather than a fresh object, so a register that failed to persist still left a phantom account that the next login accepted; (c) `AuthModal` left `busy=true` forever on any rejected promise, disabling both its buttons with no message; (d) `deploy.yml` ran lint+build but never tests and had no `needs:` on `ci.yml` — a test-red commit could ship; (e) the always-mounted lobby walker's `window` keydown handler had no target check, so typing w/a/s/d in the coupon / friend-UID fields walked the character and `preventDefault` on arrows/Space blocked cursor movement (WorldChat was already shielded, those two were not). Plus MEDIUM fixes: unbounded PBKDF2 iteration count parsed from a user-editable hash, `EnemyHealthBar` capacity computed per-wave while the runtime accumulates enemies across waves, a zero-enemy wave making a battle unwinnable _and_ unlosable, and three workflow issues (`codeql.yml` had no top-level `permissions`, `ci.yml`'s bundle-size step lacked `pipefail` so it reported green while printing nothing, `upstream-skill-watch.yml` held write perms workflow-wide and hard-failed forever if upstream force-pushed). `accountRepository.ts` and `password.ts` had **zero tests** — added two files covering exactly the fixed paths, differentially verified (each new test proven to fail when its fix is reverted, pass when restored): 18 files/165 tests → 20/174.
67. **Judge disconfirmed one of its own seats' HIGH findings** — `data` claimed every realtime-battle sprite renders untextured (`material.map` assigned in `useFrame` without `needsUpdate`, and `HAS_MAP` _is_ a real three.js program parameter, so the argument was structurally sound). Killed it by reading R3F's actual loop: `flushGlobalEffects('before')` runs every `useFrame` callback _before_ `gl.render()`, so the map is already set when three.js compiles the program on the first frame — no `needsUpdate` needed. **Lesson: a well-argued finding from a strong seat still has to lose to what the code actually does at runtime.** Related: `deploy.yml ไม่รันเทสต์` had 3 of 4 seats agreeing, but all three read the same files and ran nothing — counted as **one** voice, not three (theatrical-consensus guard); it survived because the judge verified it independently, not because it was popular.
68. **Cursor/fork sync verified empty, not assumed** — added `nustanakritwithai/GameTurnBase` as a temporary remote and diffed every branch against `origin/master` rather than trusting the handoff document. PRs #8–#13 were all already merged; the remaining fork branches carried only sync commits in the _opposite_ direction or a docs-only analysis file, and the WIP branch the handoff warned against cherry-picking (`cursor/battle-result-return-e117`) no longer exists on the remote at all. Nothing to pick up. Same lesson as `[[verify-before-assuming-regression]]`, applied to a cross-session handoff instead of a merge.
69. Full `gold-standard` AUDIT, all 12 dimensions, 6 parallel scouts, bar re-derived fresh (**OpenSSF Scorecard's 18 checks** · OWASP ASVS 5.0.0 · **excalidraw/excalidraw** as the closest structural comparable · pmndrs/react-three-fiber · web.dev CWV / Vite docs / MDN / Keep a Changelog 2.0.0 / agents.md / Sentry / OpenTelemetry — every version-sensitive claim fetched live, none from memory). **Overall 76.5/132 = 58%.** Per dimension: Security 76 · Error handling 69 · Compatibility 69 · Governance 67 · Distribution 59 · Docs 58 · Correctness 58 · Testing-CI 50 · Performance 50 · Observability 50 · UX-DX 43 · Maintainability 41. **58% is lower than the earlier 67%/70% passes because the bar is harder and covers five dimensions those passes never scored** (Security, Distribution, Observability, Maintainability, UX/DX) — not because anything regressed. Shape of the result: the code and in-app security are solid (most MUST-HAVE rows 75-88%) while the _enforcement_ layer is thin almost everywhere — the gates that should stop bad work don't, and several rules that claim to be enforced aren't. Genuinely ahead of both code exemplars on: SHA-pinned actions (r3f still floats on `@v5`), `dependabot.yml` and `SECURITY.md` (neither exemplar has either), signed release tag, zero `dangerouslySetInnerHTML`, allocation-free `useFrame`.
70. **Verified directly rather than relaying the scouts: three accounts can push straight to `master`.** `gh api` shows collaborators `HetCreep` + `nustanakritwithai` + `DemoGODRTX`, **all three `admin: true`**, with `required_status_checks: null`, `required_pull_request_reviews: null`, `enforce_admins: false`, and **rulesets `[]`** (closing the one gap a scout flagged as unverified). ~93% of history (199 commits, 13 PRs) bypassed review. This is the top gap in the audit and it is HetCreep's to close in repo Settings — no agent can. Related: the `kaoshock123` git author name maps to the `DemoGODRTX` account (confirmed by HetCreep) — see the Repo access line above.
71. **RE-VALIDATE pass — 7 binding rules were asserting things that are not true** (`RULES_VERSION` 11→12). All verdicted _stale-but-needed_ and rewritten in place with the correction recorded inline, none retired, so no tombstones. (a) `gold-standard-baseline.md` item 6 claimed coverage was "a single file, `format.test.ts`" — 20 test files exist; **four independent scouts caught this separately**, conclusion held, evidence didn't. (b) `ecc/web/compatibility.md` marked the browser floor **"CLOSED — browserslist means Vite targets the declared floor"** — verified false: no plugin-legacy, no autoprefixer, no postcss, no babel, and `vite.config.ts` sets no `build.target`; **three source files had been citing that non-existent guarantee** and were corrected too. (c) same file called `LobbyScene` the only `<Canvas>` mount — `RealtimeBattleRoom` also mounts one. (d) `ecc/web/observability.md` still ordered "pick a client error-tracking sink, `@sentry/react`" while `MEMORY.md` had already recorded the decision to decline analytics — an agent reading either file alone would violate the other; reconciled, and the section now states what having no sink actually obliges instead. (e) same file proposed `console.error('[storage]…')` as the try/catch fix, superseded the next day by `codes.ts`/`reportError`. (f) `ecc/react/performance.md`'s GLB/Draco section read as page-weight guidance for a pipeline nothing ships. (g) `MEMORY.md` still described the `queue-check` step PR #13 removed. **Lesson: rules rot faster than code, and a rule that asserts a checkable falsehood costs the whole ruleset its authority — the freshness stamps caught none of these because every stamp was 1-2 days old andยังไม่ถึงกำหนด; what caught them was re-deriving the bar from scratch.**
72. **ปล่อย 0.2.0 + เปลี่ยนกติกา deploy เป็นผูกกับเวอร์ชันเกม** (HetCreep สั่ง). ต้นเหตุที่ต้องเปลี่ยน: ตรวจพบว่า deploy สำเร็จล่าสุดคือ `070ef94` — **เว็บจริงตามหลัง master อยู่ 18 commit** ผู้เล่นยังไม่ได้รับทั้ง fix CRITICAL (save-import ทำเกมเปิดไม่ได้ถาวร) fix WebGPU ค้างตอนล็อกอิน และ fix สเกลมือถือ ทั้งที่ workflow รายงาน success มาตลอด เพราะทุก push ยิง deploy พร้อมกันแล้ว `concurrency.cancel-in-progress` ตัดทิ้งไล่กันเอง กลไกใหม่: job `gate` ราคาถูกเทียบ `GAME_INFO.version` ใน `src/game/gameInfo.ts` (เลขที่ผู้เล่นเห็นบนหน้าไตเติล/ตั้งค่า ไม่ใช่ `package.json`) กับจุดก่อน push — ต่างเมื่อไหร่ถึง build+deploy+ตัด GitHub Release พร้อมแนบ SBOM (SBOM/SLSA attestation สร้างถูกมาตลอดแต่ไม่เคยไปโผล่ที่ไหนให้โหลด จึงเป็นเหตุผลที่ Scorecard's Signed-Releases มองไม่เห็น) เทียบกับ `github.event.before` ไม่ใช่ `HEAD~1` เพราะ push เดียวมีหลาย commit ได้ และ bump ที่อยู่ commit กลางจะถูกมองว่าไม่เปลี่ยน · `src/game/gameInfo.test.ts` บังคับให้เลขเกมกับ `package.json` ตรงกันเสมอ (differential verify แล้ว: แก้ข้างเดียวเทสต์แดง)
73. **GitHub Actions major outage 2026-08-06 15:22 UTC เป็นต้นไป** — ระหว่างงานชุดนี้ run ทั้งหมดค้าง `queued` และ commit ใหม่มีแต่ CodeQL ถูกสร้าง deploy ไม่มี run เลยตั้งแต่ `b203b5a` **ผลคือกติกา deploy ใหม่ยังไม่ได้ยืนยันบน runner จริง** (dry-run ในเครื่องด้วยข้อมูล git จริงผ่าน: version เท่าเดิม → `should_deploy=false` ถูกต้อง · YAML parse ผ่านทุกไฟล์ · SHA ของ action ใหม่สองตัวยืนยันกับ API แล้ว) และ **0.2.0 ยังไม่ขึ้นเว็บ** จะออกเองเมื่อ Actions ฟื้น ถ้าไม่ออกให้กด Run workflow ที่ Deploy to GitHub Pages
74. เก็บหนี้จาก audit ที่เหลือครบ: **บั๊กเงียบ 4 จุด** — `updatePlayer` ทิ้งผลของ `savePlayer` (เส้นทางเขียน progress ทั้งเกม เซฟไม่ลงแล้วหน้าจอยังโชว์เหมือนสำเร็จ ตอนนี้ย้อนหน้าจอกลับ + `PLAYER_SAVE_FAIL`) · `LobbyScene` ไม่ถอด listener `webglcontextlost` (ออกจากระบบทีขึ้น error หลอกทุกครั้ง — `RealtimeBattleRoom` แก้รูปแบบนี้ไปก่อนแล้ว ลอบบี้เพิ่งตาม) · `ErrorBoundary` ไม่มีทางกู้ข้อมูล ทั้งที่ `exportSave()` มีอยู่ (เพิ่มปุ่มสำรองไฟล์ + แยก `src/lib/saveFile.ts` ให้สองทางใช้ร่วม) · `useDeployWatcher` กลืน error เงียบ ๆ **metadata** — CHANGELOG ค้าง 115 commit, description/topics/release notes ยังเขียน turn-based + URL ตาย, README structure ครอบครึ่งเดียวและไม่มี `game/**` เลย, bug template ไม่มีช่องรหัสข้อผิดพลาด **toolchain** — `build.target` เขียนชัด (ปิดเรื่อง browserslist ที่ไม่มีอะไรบังคับ), coverage tooling (**ของจริง 24.42%**), `engines.node`, `.editorconfig`, `tsconfig.node.json` strict, CODEOWNERS, Scorecard workflow รายสัปดาห์
75. **ยังเปิดอยู่ ต้องให้ HetCreep ตัดสิน ไม่ใช่ agent**: branch protection (สาม admin push ตรงได้ — trade-off จริงเพราะเจ้าของเป็นคนเขียนเอง ไม่มีคนรีวิว, ทางแคบกว่าคือเปิดแค่ required status checks) · เทสต์ระดับ component (0 ไฟล์ `.test.tsx` — `gold-standard-baseline.md` ข้อ 6 ห้ามเริ่มกวาดโดยไม่ถาม) · โหมดสำรวจเก็บ/ลบ · อีเมลส่วนตัวใน `admins.ts` · เปิด `--deny-warnings` (CI จะแดงทันที 34 ข้อ ต้องเคลียร์ก่อน) · `SHOW_ARENA_SLOTS` + perf-tiering · cut-pigsy 7 สคริปต์ · `publish_results` ของ Scorecard (เปิดแล้วคะแนนเป็นสาธารณะ)
76. **กฎทองปิด gap ที่ปิดได้จนหมด** — (a) **lint gate ใช้งานได้จริงแล้ว**: `oxlint --deny-warnings`, warning เหลือศูนย์. 34 findings เดิมถูก*ตัดสิน*ไม่ใช่ไล่ทำให้ตัวเลขสวย — 18 ตัวคือ `prefer-tag-over-role` ที่สั่งเปลี่ยน `role="dialog"`→`<dialog>` ฯลฯ ซึ่งเปลี่ยนพฤติกรรมจริงบน 8 modal ที่ตั้งใจ hand-roll ผ่าน `useModalA11y` (ปฏิเสธ Radix มาสองรอบ) → ปิดกฎพร้อมเหตุผล · 2 ตัวเป็น false positive (`BroadcastChannel.postMessage` รับอาร์กิวเมนต์เดียวตามสเปก, `import './index.css'` คือวิธีของ Vite) · 6 ตัวอยู่ในโหมดสำรวจที่ไม่มีทางเข้า → override แบบมี**เงื่อนไขหมดอายุ**เขียนกำกับ · 7 ตัวที่เหลือแก้จริง. เหตุผลทั้งหมดอยู่ใน `.agents/rules/lint-policy.md` เพราะ `.oxlintrc.json` เป็น JSON ใส่คอมเมนต์ไม่ได้. (b) CSP `base-uri` → `'none'` ตาม ASVS V3.4.3. (c) **`RealtimeBattleLoop` ได้ seam + เทสต์ 7 ตัวแรก** — ตัวคุมเวลาของห้องต่อสู้ทั้งระบบที่ไม่เคยมีเทสต์เลย. (d) `engines.node`, `.editorconfig`, `tsconfig.node.json` strict, CODEOWNERS, coverage tooling (**24.42%**), Scorecard workflow, `build.target` เขียนชัด
77. **`no-shadow` พิสูจน์ตัวเองสองครั้งในชั่วโมงเดียว** — ตอนแก้ warning ใน `AddFriendModal` การเปลี่ยนชื่อพารามิเตอร์ `map((player) => ...)` โดยไม่แตะการใช้งานข้างในทำให้ `player` ในลูปชี้ไปที่**ผู้ใช้ที่ล็อกอิน**แทนเพื่อนแต่ละคน — TypeScript จับไม่ได้เพราะชนิดเดียวกัน (จะเรนเดอร์ชื่อตัวเองซ้ำ N ครั้ง). แล้วชั่วโมงถัดมา gate ที่เพิ่งเปิดจับรูปแบบเดียวกันในโค้ดที่ผมเพิ่งเขียนเอง (`catch (error)` บัง error state ใน `AuthModal`). **บทเรียน: rename ต้องไล่การใช้งานทั้งบล็อก ไม่ใช่แค่จุดประกาศ — และ gate ที่ล้มเหลวได้จริงคืนทุนตั้งแต่ commit ถัดไป**
78. **รัน CoalMine canary ที่เหลือครบ 6 ตัวขนานกัน** (`drift` · `resilience` · `telemetry` · `testability` · `scale` · `supply-chain`). ทั้งหกชี้ธีมเดียวกันโดยไม่รู้กัน: **ตรวจเจอ + จัดการได้ + กู้คืนได้ แต่ไม่เคยบอกคนที่โดน**. แก้แล้ว: `loadDb` เคยไม่แยก "ยังไม่มีข้อมูล" กับ "มีแต่อ่านไม่ออก" → เขียนครั้งถัดไปทับบัญชีหายทั้งเบราว์เซอร์ ซึ่งจะเกิดวันที่มีคนขึ้น `Database.version` เป็น 2 พอดี (จำลองยืนยันแล้ว) · `normalizePlayer` เติมแค่ 3 ฟิลด์ แต่ `ownedCharacters`/`teamSlots`/`currency` ถูก deref ตรง ๆ = brick วนซ้ำแบบเดียวกับบั๊ก `player` หายที่เพิ่งแก้ · catch ที่ไม่ผ่าน `reportError` 4 จุด · session write ไม่เช็คค่า 3 จุด · toast บอกสำเร็จก่อนเขียนเสร็จ · action pin ที่ **ผมเองเพิ่งปักไว้ตกรุ่นใหญ่ 3-4 เวอร์ชัน** (verify SHA ตรง tag แต่ลืมเช็คว่า tag ยังใหม่ไหม — ครึ่งเดียวของงาน)
79. **ยังเปิดอยู่ ต้องตัดสินก่อนทำ**: `reportError(..., 'visible')` **ไม่ได้แสดงอะไรบนจอ** ใน 3 ที่ (`globalErrorHandlers`, `useAuth.updatePlayer`, จอ error ของห้องต่อสู้) — สัญญาของมันเองบอกว่า "ผู้เรียกต้องแสดง code เอง" แต่สามที่นี้แสดงไม่ได้/ไม่ได้แสดง. ทางแก้คือมี channel กลาง ซึ่ง **ask-CB เคยตัดสินว่า over-engineer** (ดูคอมเมนต์หัว `reportError.ts`) — ตอนนั้นทุกผู้เรียกยังแสดงเองได้ ตอนนี้ไม่จริงแล้ว **เข้าข่าย ask-CB รอบใหม่ ไม่ใช่กลับคำเอง**. อื่น ๆ: เขียนทับข้ามแท็บ (สองแท็บบัญชีเดียวกัน เขียนทับกันเงียบ ๆ) · `requestExit` ทับสถานะ victory · `CurrencyTransaction[]` โตไม่มีเพดาน (วัดได้ 19.7ms/เขียน ที่ 20k รายการ ขณะที่ `battleHistory` cap 50 อยู่แล้ว) · ไม่มี notice ที่มาของไฟล์เสียง Kenney CC0 นอกจากคอมเมนต์ในโค้ด
80. **HetCreep ตัดสิน 7 ข้อที่ค้าง (2026-08-07) — ทำครบทุกข้อ** `RULES_VERSION` 12→13. (a) **branch protection เปิดแล้ว**: required check `Continuous Integration` + `required_conversation_resolution` + กัน force-push/ลบ branch · **`enforce_admins` ยัง false โดยตั้งใจ** admin สามคนยัง push ตรงได้ — เปิดแปลว่าทุก push ต้องผ่าน PR รวมถึงเจ้าของเอง และตอนนี้ deploy ผูกกับ bump แล้ว commit เทสต์แดงไม่ขึ้นเว็บอัตโนมัติ ความเร่งด่วนลดไปมาก (b) **โหมดสำรวจ ปิดด้วย `//` ไม่ลบ** — ขอบเขตหาจาก import graph จาก `main.tsx` ไม่ใช่ grep (grep ผิดสองชั้น: นับการอ้างอิงกันเองในกองที่ตายด้วยกัน และ `battle/types` ไปแมตช์ `realtimeBattle/types`) ได้ 21 ไฟล์ 1,579 บรรทัด + เทสต์ 3 ไฟล์ · `dialogue/actions.ts` **ไม่ตาย** `LobbyBattleSession` ใช้ `appendBattleHistory` อยู่ · ใช้ `//` ไม่ใช่ `/* */` เพราะไฟล์มี JSDoc ที่ลงท้าย `*/` จะปิดบล็อกก่อนเวลาแล้วโค้ดที่เหลือหลุดออกมาเป็นโค้ดจริง (c) กฎใหม่ `proven-good-do-it-now.md` (d) `publish_results` ของ Scorecard เปิด
81. **error channel ใช้งานได้จริงแล้ว** — `reportError` มีตัวส่งต่อกลาง (`Set` ของ callback ไม่มี store/state/context) + `GlobalErrorBanner` วางข้าง `<App/>` ใน `main.tsx` นอก context ทุกตัวโดยตั้งใจ **นี่คือการกลับคำ ask-CB เดิม** ที่เคยตัดสินว่า relay กลาง over-engineer — ตอนนั้นถูก เพราะผู้เรียก 'visible' ทุกรายเรนเดอร์เองได้ ตอนนี้ไม่จริงแล้ว 3 canary ชี้ตรงกัน · ยืนยันในเบราว์เซอร์จริงไม่ใช่แค่เทสต์: ยิง `unhandledrejection` จริงแล้วแถบขึ้นที่ top:10 อ่านว่า `GLOBAL_REJECTION` ปุ่มปิด 24×24 ตรงเกณฑ์ WCAG SC 2.5.8, ปิดได้, error ตัวถัดไปเด้งใหม่, และ error tier `silent` (ยิงผ่าน quota-exceeded จริง) ไม่เด้ง
82. **ask CB 4 ที่นั่งบนสองคำถาม** (`admins.ts` + ledger). **Q1 เอกฉันท์ 4/4**: เปลี่ยนค่าในกลไกเดิม ไม่ทำ hash (ค่าถูกพิมพ์เองตอนล็อกอิน เอนโทรปีต่ำ และไฟล์บอกเองว่าไม่ใช่ security boundary) ไม่ทำ env var (Vite inline ลง bundle เหมือนกันเป๊ะ ต่างแค่ดูเหมือนของลับ ซึ่งแย่กว่าเพราะหลอกคนอ่าน) → ใช้ `admin@legendofsoulth.invalid` (RFC 6761 สงวนไม่ให้ resolve ได้) ยืนยันแล้วว่าไม่เหลือใน bundle **Q2 แตก 3-1 และเสียงข้างน้อยชนะ**: สามที่นั่งบอกให้ทำเพดานทันที ที่นั่ง `reality` บอกว่ายังไม่ต้อง เพราะไปวัดมาว่า **ไม่มีอะไรทำให้ ledger โต** — `earnGold`/`grantItem` ไม่มีทางเรียกจริง คูปองมีใบเดียวและกันแลกซ้ำ เหลือแค่ปุ่มเติมเงินที่กดทีละครั้ง · judge ตรวจเองแล้วยืนยัน จึงตัดสินตามเสียงข้างน้อย เขียนเป็นกฎ `currency-ledger-retention.md` แทนการสร้างของที่ยังไม่มีปัญหา (มาตรฐานอ้างอิง: บัญชีคู่ + PCI DSS 10.5.1 ร้อน/เย็นห้ามลบ) **จุดที่ทั้งสี่ที่นั่งชี้ตรงกันและสำคัญที่สุด: `.slice()` แบบ `battleHistory` จะเปิดช่องแลกคูปองซ้ำ** เพราะ `redeemCoupon` อ่านทั้งอาร์เรย์เพื่อกันซ้ำ — "แก้" ปัญหาประสิทธิภาพแล้วได้ช่องโกงแถมมา
83. **ยังเปิดอยู่ ต้องคุณตัดสิน (ใหม่จากบอร์ด)**: อีเมลเดิมอยู่ใน git history เป็น **commit-author 49 commit** บน repo สาธารณะ รวม commit ที่สร้างฟีเจอร์นี้เอง — แก้ไฟล์ปัจจุบันไม่แตะประวัติเลย ทั้งสี่ที่นั่งชี้จุดนี้ตรงกัน · ลบจริงต้อง `git filter-repo`/BFG + force-push ซึ่งพัง clone/fork ทุกตัวที่มีอยู่ และ cache ของ GitHub ต้องขอ purge แยก **เป็นการตัดสินใจคนละขนาดกับที่สั่งไว้ ผมไม่รวบเอง**
