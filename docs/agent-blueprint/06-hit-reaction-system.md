# 6. Hit Reaction System

> Category: Combat core · Generated via gold-standard FILL + adversarial CB-lite verify (2 seats), 2026-08-07 · **revised after verify flagged an issue**.

### Scope

Owns the moment-of-impact reaction on the **target**: applying knockback displacement and driving `hitStunRemainingMs`/`invulnerableUntilMs`, which is what actually locks out movement/attack input until stun expires. Every input gate now routes through the shared `isControlLocked(entity)` helper (`combatReaction.ts:109-117`) rather than checking `hitStunRemainingMs > 0` inline: `MovementSystem.ts:97`, `ComboSystem.ts:73`, `SkillSystem.ts:50` all call it. `isControlLocked` returns true on `hitStunRemainingMs > 0` OR `state === 'knockdown'`/`'getUp'`/`'dead'` OR `isCcLocked(entity)` (`statusEffects.ts:18`) — so this system's control-lock is no longer purely its own signal, it folds in the knockdown/getUp lifecycle and status-effect CC state too (see Dependencies). `combatButtonState.ts:18,28` is the one holdout still checking `state === 'dead' || hitStunRemainingMs > 0` directly rather than via `isControlLocked`. None of these gate on `state === 'hit'`. `state = 'hit'` is set in four places — `combatReaction.ts:62` (the main hit path, reached via `DamageSystem.applyDamage`'s delegation, `DamageSystem.ts:72-79`), `combatInterrupt.ts:50` and `:60` (cancel-on-interrupt for combo/skill respectively), and `EnemyAISystem.ts:159` (enemy self-freeze when its own `hitStunRemainingMs > 0`, gated at `:156`) — and read by four consumers, all cosmetic-or-reset, none of which gate input: `EntitySprite.tsx`'s `animationForState()` (`:43-52`, invoked `:112`) switches to the `'hit'` sprite animation; `EntitySprite.tsx:138` separately swaps a red tint for visual feedback; `RealtimeBattleRuntime.ts:620` and `src/game/pvp/PvPAuthorityEngine.ts:256` both reset `state` back to `'idle'` once `hitStunRemainingMs <= 0`. The `PvPAuthorityEngine.ts` reader is a **cross-system dependency worth naming**: PvP reads this system's `state` field directly, so a future rename/removal of the `'hit'` state value must update PvP too. It does **not** own damage math (that's `calcDamage`, same file, `DamageSystem.ts:54-69`), and the attacker-side interrupt-on-hit-while-casting flow from blueprint §3.6.4/§3.6.6 IS implemented: `AttackDefinition.interruptible`/`phaseOverrides` (`attacks.ts:75`, `combatMoveSchema.ts:14-18`), `resolveInterruptible` (`combatMoveSchema.ts:89-93`), and `ComboSystem.ts:145`'s cancel-on-hit-if-interruptible logic, covered by a dedicated `combatInterrupt.test.ts`, and knockdown/get-up IS implemented: `AttackDefinition.knockdown` (`attacks.ts:60`), `EntityState` now includes `'knockdown'` and `'getUp'` (`types.ts:29-30`), and `combatReaction.ts`'s `applyCombatReaction`/`tickKnockdownState` drive the full knockdown→getUp→control-restored lifecycle, gated to elite/boss tiers only via `canApplyKnockdown` (`combatReaction.ts:15-19`), covered by `DamageSystem.test.ts`'s "Done-criterion 5" and `EliteTierSystem.test.ts`'s "Done-criterion 3" suites.

### Inputs/Outputs

- In: `DamageContext { attacker, target: RealtimeBattleEntity, attack: AttackDefinition, elapsedMs, random }` (`DamageSystem.ts:46-52`). Relevant `AttackDefinition` fields actually present: `damageMultiplier: number`, `knockback: number`, `hitShape` (`attacks.ts:31,44,39` respectively), plus `knockdown?: boolean`, `hitstunMs?: number`, `knockdownMs?: number`, `interruptible?: boolean` (`attacks.ts:60,70,72,75`), consumed via `combatMoveSchema.ts`'s `resolveHitstunMs`/`resolveKnockdownMs`/`resolveInterruptible`.
- Out (mutates `target` in place): `hp -= amount`; on survival sets `state = 'hit'`, `hitStunRemainingMs = resolveHitstunMs(attack)` which defaults to `HIT_STUN_MS` (200, `DamageSystem.ts:17`, `combatMoveSchema.ts:34-36`) but is overridable per-attack via `AttackDefinition.hitstunMs`, `invulnerableUntilMs = elapsedMs + 120` (`combatReaction.ts:64`, inlined — the doc's `HIT_INVULNERABLE_MS` named export no longer exists anywhere in the codebase), and displaces `target.position` by `push * attack.knockback` where `push` is `horizontalKnockbackVector(attacker.combatFacing)` for `hitShape === 'horizontal'` else `directionVector(attacker.facing)` (`applyKnockback`, `combatReaction.ts:68-81`). On death instead sets `state = 'dead'`, zeroes `velocity` (`combatReaction.ts:44-45`). Returns `DamageOutcome { amount, critical, defeated }`.
- Consumed downstream: `RealtimeBattleRuntime.tickTimers` (`RealtimeBattleRuntime.ts:610`) decrements `hitStunRemainingMs` every tick (`:616`) and resets `state` to `'idle'` once it and `hitStunRemainingMs` both clear (`:620`); `MovementSystem`, `ComboSystem`, and `SkillSystem` gate via `isControlLocked` (see Scope), `combatButtonState` still gates on `hitStunRemainingMs > 0` directly; none gate on `state`.

### Dependencies

- **combat** (HitboxSystem for hit detection feeding into this, ComboSystem/SkillSystem for the attacker's `AttackDefinition`, MovementSystem consuming the resulting stun/knockback lockout via `isControlLocked`) — all within the same `realtimeBattle` combat-core cluster.
- **status effects** — `isControlLocked` (`combatReaction.ts:109-117`) calls `isCcLocked(entity)` (`statusEffects.ts:18`), so any CC status effect now co-determines whether this system's control lock holds, not just `hitStunRemainingMs`/knockdown/getUp/dead alone. This dependency did not exist when this contract was first written; it was added later without a doc update.
- **enemy** — EnemyAISystem reads its **own** `state`/`hitStunRemainingMs` (`EnemyAISystem.ts:156-160`) to freeze itself when the enemy is the one stunned. There is no code path where enemy AI reads a stunned player/target's state to change its chase/attack behavior — grep for `player.state`/`player.hitStunRemainingMs` in `EnemyAISystem.ts` returns zero matches. Any AI-side "exploit the stun window" behavior is not implemented.
- **pvp** — `src/game/pvp/PvPAuthorityEngine.ts:256` reads `entity.state === 'hit'` (alongside `hitStunRemainingMs <= 0`) to reset state back to `'idle'`, mirroring `RealtimeBattleRuntime.ts:620`'s own-loop reset. This is a real cross-system read, not a hypothetical — PvP depends on this system's `state` field shape.
- No dependency on hero/adventure/economy/backend/social systems today.

### Done-criteria

1. `DamageSystem.test.ts` and `DamageSystem.fuzz.test.ts` pass (existing suite already covers this system — extend, don't fork a parallel test file).
2. A hit that leaves `target.hp > 0` always sets `state === 'hit'`, `hitStunRemainingMs === HIT_STUN_MS`, and `invulnerableUntilMs === elapsedMs + HIT_INVULNERABLE_MS` — assert via unit test.
3. Knockback displacement direction is deterministic: for `hitShape === 'horizontal'` it matches `horizontalKnockbackVector(attacker.combatFacing) * attack.knockback`; for all other shapes it matches `directionVector(attacker.facing) * attack.knockback` — regression-test both branches.
4. `MovementSystem` rejects movement input while `hitStunRemainingMs > 0` and accepts it again the tick after it reaches 0 (boundary test at `deltaMs` crossing zero).
5. Knockdown and per-attack `interruptible` are now implemented (§3.6.5/§3.6.6/§3.6.8) — `AttackDefinition.knockdown` is confirmed **inert against normal-tier enemies** per §3.6.5's tuning-table-is-source-of-truth resolution via `canApplyKnockdown` (`combatReaction.ts:15-19`, tier-gated to elite/boss only); the required done test exists as `DamageSystem.test.ts`'s "Done-criterion 5: knockdown flag is inert against normal mobs but active on elite/boss" and `EliteTierSystem.test.ts:342`'s normal-mob-with-inflated-stats regression test.

### World-class bar

Separating **hitstun duration from knockback distance as two independently-tunable-per-move numbers** (never derived from one formula) is standard per-move frame-data practice across fighting games with a move-data table (Street Fighter, Tekken, KOF, Smash, and others) — worth borrowing because it's exactly the shape `hitstunMs`/`knockback` already take in this codebase's `AttackDefinition` (§3.6.7); the gap today is that hitstun is a single global `HIT_STUN_MS = 180` constant rather than per-attack, so different move weights currently feel identical on landing.

### Stay-current note

The per-`AttackDefinition` `hitstunMs`/`knockdownMs` fields speced in §3.6.7/§3.6.12 already shipped (`attacks.ts:230,249`, resolved via `combatMoveSchema.ts`'s `resolveHitstunMs`/`resolveKnockdownMs`) — `HIT_STUN_MS` (`DamageSystem.ts:17`) now serves only as the fallback default when an attack omits `hitstunMs`, and the doc's `HIT_INVULNERABLE_MS` named constant no longer exists (the 120ms invuln window is inlined in `combatReaction.ts:64`).

### Low-maintenance-cost design

Keep hit-reaction tuning as **plain-object data on `AttackDefinition`** (as `knockback` already is in `attacks.ts`), not a class hierarchy or strategy-pattern per reaction type — this project already treats `attacks.ts` as the single source of truth for per-move numbers; the single-mutation-point now lives in `combatReaction.ts`'s `applyCombatReaction` (`combatReaction.ts:37-66`), which `DamageSystem.applyDamage` delegates to (`DamageSystem.ts:72-79`) — the doc-comment this section previously quoted ("ทั้งหมดอยู่ที่เดียว...") no longer exists anywhere in the codebase, having moved out with the mutation logic itself. When `hitstunMs`/`knockdown`/`interruptible` land, add them as new fields on the existing `AttackDefinition` object and branch once inside `applyDamage`/`MovementSystem` — do not introduce an `IHitReactionStrategy` interface for what is still a single implementation (YAGNI, matches ECC common-rules bias against premature abstraction).

### Known scars (real historical precedent)

- **Scar**: In _Super Smash Bros. Melee_, Ice Climbers could chain grab-pummel timing ("wobbling") so the opponent never left grab-hitstun, producing a guaranteed 0%-to-death lock with no escape option — the cited incident is a Tennessee four-region tournament-ruleset ban that followed a "fake-ban" prank (Melee received no further balance patches, so the fix lived entirely at the ruleset layer, not in-game). — Source: [SmashWiki — Wobbling](https://www.ssbwiki.com/Wobbling); [Kotaku — "Super Smash Bros. Melee Technique Gets Fake-Banned And Then Actually Banned"](https://kotaku.com/super-smash-bros-melee-technique-gets-fake-banned-and-1832473327)
  **Test-for-us**: Try chaining a grab (or any attack whose `hitStunRemainingMs`/`invulnerableUntilMs` reset happens on a fast enough cadence) back-to-back on the same target and see whether a new `applyDamage` call can land and re-arm `HIT_STUN_MS` before the previous stun value ever ticks down to 0. This codebase already has a named anti-wobble guard, not an open question: every input gate (`MovementSystem.ts:97`, `ComboSystem.ts:73`, `SkillSystem.ts:50` via `isControlLocked`; `combatButtonState.ts:18,28` directly) checks `state === 'dead' || hitStunRemainingMs > 0` (the shared gates also fold in knockdown/getUp/CC-lock — see Scope), and `HitboxSystem.ts:105` (`if (target.invulnerableUntilMs > elapsedMs) return false`, mirrored for the locked-target branch at `:146`) rejects a hit while the target is still inside its post-hit invuln window. No comment in the codebase currently explains the 120ms figure — `combatReaction.ts:64`'s `invulnerableUntilMs = elapsedMs + 120` is inlined with no rationale text attached; a prior revision of this contract attributed a specific Thai-language rationale comment to `DamageSystem.ts:27-28`, but that quote does not exist anywhere in the repo and lines 27-28 are actually the tail of an unrelated knockdown/getUp comment block plus the `KNOCKDOWN_DURATION_MS` export — corrected here. Don't take the invuln guard on faith — try to construct the loop anyway: e.g. multiple simultaneous hitboxes that all resolve before `invulnerableUntilMs` is set from the first hit, or a skill/attack whose active-hitbox window outlasts its own `HIT_INVULNERABLE_MS` so a second hit from the _same_ attack lands after invuln was granted but before stun is consumed. Confirm the guard actually closes every path, not just the obvious one.

- **Scar**: Before hit-stun deterioration existed as a design pattern, fighting games could produce genuine infinite combos: hitstun from repeated hits didn't decay within a combo, so once a loop connected it could be repeated indefinitely — this is exactly the failure mode hit-stun-deterioration mechanics were later introduced to close. — Source: [SuperCombo Wiki — Hit Stun Deterioration (Marvel vs. Capcom 3)](https://wiki.supercombo.gg/w/Marvel_vs_Capcom_3/Combo_Limiters/Hit_Stun_Deterioration); [infil.net Fighting Game Glossary — Hit Stun Deterioration](https://glossary.infil.net/?t=Hit+Stun+Deterioration). _(Note: these two sources document the general mechanic and the design problem it exists to solve, not a specific Street Fighter II Champion Edition / M. Bison case — that named specific was dropped rather than asserted, since it could not be independently verified this session (WebSearch budget exhausted) and neither cited source actually supports it. If a Bison/Scissor-Kick-specific claim is wanted, it needs its own SF2CE-specific citation, e.g. SRK wiki or Shoryuken forums.)_
  **Test-for-us**: Land repeated hits on the same target back-to-back within one continuous engagement (not a single grab-lock, but a normal multi-hit combo/attack string) and check whether `HIT_STUN_MS` being a single flat global constant (`DamageSystem.ts:17`) means every hit in the string re-applies the _same_ fixed stun regardless of how many hits already landed — try to find a hit-rate/attack-speed combination where the target is functionally never able to act again for the rest of the engagement, purely from stacking flat per-hit stun with no combo-length-based falloff.

- **Scar**: _Tekken 7_'s Season 2 patch notes include a fix for a bug where a hit that knocked the opponent into a wall left them unable to perform an ukemi (tech roll / recovery input) afterward — a specific attack+geometry interaction silently denied the target's normal post-hit recovery option. — Source: [EventHubs — "Tekken 7 Season 2 patch notes released"](https://www.eventhubs.com/news/2018/sep/03/tekken-7-season-2-patch-notes-released/) ("Opponent was unable to do an ukemi when hitting a wall, so this was fixed.") _(⚠️ unverified this session: the quoted fix text itself is confirmed accurate against the source, but the specific attribution to Asuka's Rage Art / `During Rage d/f+1+2` notation could not be independently re-checked — WebSearch budget was exhausted before this task ran. Verify that character/input attribution directly against the EventHubs page before treating it as established; the underlying "wall + ukemi denial was a real patched Tekken 7 bug" claim is solid either way.)_
  **Test-for-us**: Land a knockback hit that drives the target into map/arena boundaries under each `hitShape` branch (`horizontal` via `horizontalKnockbackVector`, and the default `directionVector` branch, `combatReaction.ts:73-76`) and confirm `hitStunRemainingMs` still decrements normally and `MovementSystem` unlocks input the tick after it reaches 0 in every case — per Done-criteria #4, but specifically probe the wall/boundary-collision case, since that's exactly the kind of geometry-dependent edge case that broke recovery in the cited incident.

This project's own spec — `docs/agent-blueprint/06-hit-reaction-system.md` and `docs/MASTER_BLUEPRINT_v3.0.md` — decides what "correct" looks like here; the exemplar titles above only establish _what kind of failure to go try to reproduce_, not how any of them fixed it.
