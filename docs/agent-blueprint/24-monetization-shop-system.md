# 24. Monetization / Shop System

> Category: Economy · Generated via gold-standard FILL + adversarial CB-lite verify (2 seats), 2026-08-07 · **revised after verify flagged an issue**.

### Scope

Owns the **purchase-selection UI and client-side buy request** for currency top-ups: `CurrencyShopModal` (`src/components/CurrencyShopModal/CurrencyShopModal.tsx`), its package catalog (`GOLD_PACKAGES`/`GEM_PACKAGES`, `src/data/accountRepository.shared.ts:28-53`), and the `topUpGold`/`topUpGems` call it triggers, wired into `TopBar.tsx:114-120`. Does **not** own: the currency ledger/balance itself or `earnGold`/`redeemCoupon` (that's system #22 Currency System); gacha pull mechanics, rates, or pity (#23 Gacha System); a real payment gateway (none exists — see §5/§6); or the skins/season-pass/starter-pack catalog (MASTER_BLUEPRINT_v3.0.md:471, "Secondary (later)... TBD" — zero code for these exists anywhere in `src/`).

### Inputs/Outputs

- **In**: `currency: 'gold' | 'gem'`, a `packageId` (string) the player taps, chosen from `GoldPackage`/`GemPackage = { id: string; amount: number; priceLabel: string }` (`accountRepository.shared.ts:28-53`).
- **Out**: `Promise<CurrencyResult>` = `{ ok: true; player: Player; amount: number } | { ok: false; error: string }` (`accountRepository.shared.ts:57-58`).
- Component contract: `CurrencyShopModalProps = { currency: ShopCurrency; onBuy: (packageId: string) => Promise<CurrencyResult>; onClose: () => void }` (`CurrencyShopModal.tsx:16-21`).

### Dependencies

- **Currency System (#22)**: this shop's buy call is the only path into `earnGold`-adjacent balance mutation for real-money currency; it feeds the ledger, doesn't own it.
- **Backend / Server-Authority System (#25, §8)**: on the Supabase (production) backend, `topUpGold`/`topUpGems` are stubs that always return `{ok:false}` — "ระบบเติมเงินยังไม่เปิดให้ใช้งาน" (`accountRepository.supabase.ts:501-507`). Only the local/demo backend (`accountRepository.ts:600-642`) actually credits currency, unconditionally, with no payment verification.
- **Gacha System (#23)**: feeds it — gems bought here are the stated gacha currency (§7 "Core: Hero Gacha + star ascension").
- No dependency on combat/enemy/hero/adventure/pvp/social systems.

### Done-criteria

1. `topUpGold`/`topUpGems` on the Supabase backend credit currency only after a verified payment callback — not the current always-fail stub (`accountRepository.supabase.ts:501-507`).
2. A server-side RPC (`SECURITY DEFINER`, same pattern as `earn_gold`/`redeem_coupon`, `accountRepository.supabase.ts:480,492`) grants currency from a webhook — client-asserted "purchase succeeded" is never trusted.
3. Package catalog (id/amount/price) is validated server-side against the same source `CurrencyShopModal` renders from — today `priceLabel` is a display-only string with no server-side price check.
4. E2E test: buying `gold-small` credits exactly 1000 gold once, is idempotent under webhook retry, and lands in `currency_transactions` with `source='topup'`.
5. No purchasable SKU maps to combat power — audit passes §7's "must not sell best power primarily via direct purchase" (MASTER_BLUEPRINT_v3.0.md:472).

### World-class bar

The bar here isn't a specific competitor's architecture — it's the baseline every commercial app with in-app purchases meets (App Store/Play Store review requires it, and any payment processor enforces it): **server-side receipt/webhook verification before crediting currency, full stop — the client never gets to assert "I paid."** That's this project's current gap (done-criterion 2). No named-exemplar detail is being borrowed here (no specific receipt-validation service, anti-fraud, or rate-limit design) because none is needed — the fix is the industry-standard shape, and the `earn_gold`/`redeem_coupon` RPC pattern already used for other currency paths (`accountRepository.supabase.ts:480,492`) is the template to extend, not a new one to invent.

### Stay-current note

The actual payment gateway is undecided — `accountRepository.supabase.ts:501` cites "ยังไม่มี RPC (ไม่มี payment gateway จริง — ดู fork issue #19)" — and distribution channel (web vs. wrapped app) determines the choice (Omise/2C2P/TrueMoney for web vs. Apple/Google IAP for app stores), so the integration shape here will change once that's decided and §7's monetization moves off P14-deferred.

### Low-maintenance-cost design

Keep `GOLD_PACKAGES`/`GEM_PACKAGES` in `accountRepository.shared.ts` as the **single data-driven catalog** both the UI and future server-side price verification read from — don't fork a second list. And don't build a payment-gateway abstraction/interface now: zero real gateways are wired today, so an interface for "gateway #1 of 1" is exactly the premature abstraction this project's own style avoids (see AGENTS.md rule 12, proven-good-do-it-now — nothing proven yet to abstract over). When a gateway is actually chosen, wire it directly into `topUpGold`/`topUpGems` following the existing `earn_gold`/`redeem_coupon` RPC pattern; also respect `.agents/rules/currency-ledger-retention.md` (AGENTS.md item 14) — `topup` transactions must never be pruned once real money is involved.

**Note on the exemplar**: `docs/agent-blueprint/24-monetization-shop-system.md`'s "World-class bar" section does not actually cite a named shipped game/engine. It explicitly declines to: _"The bar here isn't a specific competitor's architecture... No named-exemplar detail is being borrowed here (no specific receipt-validation service, anti-fraud, or rate-limit design) because none is needed."_ (Verified verbatim against the file, lines 30-32.) Per the task instruction for this case, three well-documented real precedents were substituted, each matching a specific failure shape this system's done-criteria (1, 2, 4, 5) call out.

### Known scars (real historical precedent)

- **Scar**: Shipped Android games (Fruit Ninja, Juice Jam, PandaPop) validated in-app purchase receipts client-side only — via IAP libraries (Prime31, Unity IAP) that, per the article, "don't support server side validation" ("the library is only able to perform client side validation" for Prime31; "the Unity library is not able to handle server side verification"). This let unmodified-looking apps be patched to fake a successful purchase: researchers demonstrated it by editing Fruit Ninja's and Juice Jam's smali code to redirect/short-circuit the billing call, and separately, three real bypass tools already existed in the wild for exactly this class of bug — CreeHack and LeoPlaycard (both exploiting the same billing-validation gap the researchers found), and Lucky Patcher, which the article singles out as the one tool that "modifies the targeted application in order to bind the Google Play Billing service to Lucky Patcher" itself, letting it answer the app's purchase-verification call with a fabricated success result. — Source: Security Boulevard, "Get Freebies by Abusing the Android InApp Billing API" (2018), https://securityboulevard.com/2018/12/get-freebies-by-abusing-the-android-inapp-billing-api/ (fetched and confirmed to name all games/tools/quotes above verbatim).
  **Test-for-us**: Once `topUpGold`/`topUpGems` are wired to a real gateway, try forging a client-side "purchase succeeded" result (a fake `CurrencyResult{ok:true}`, or calling whatever function ultimately credits currency directly, bypassing the gateway) and confirm the server refuses to credit anything that didn't arrive via a verified webhook/receipt.

- **Scar**: A Diablo III patch (1.0.8) raised the real-money-auction-house gold stack cap from 1 million to 10 million; reporting at the time (citing Reddit technical speculation, not an official Blizzard postmortem) attributed the resulting exploit to an integer-overflow bug in the RMAH's gold-selling code once totals crossed 2,147,483,648 — letting a number of accounts duplicate gold. Blizzard pulled both the gold and real-money auction houses offline for "troubleshooting in a more stable environment," reduced the stack size back to 1 million, banned exploiting accounts, and refunded real-money purchases rather than doing a full server rollback. **Caution on scale**: a screenshot claiming one account hit 371 trillion gold circulated widely (and is still repeated by some outlets, e.g. PCGamesN, as an "alleged" figure) — but Blizzard itself confirmed that specific screenshot was a photo-edited fake, so it should not be cited as a real duplicated amount. — Sources: GamesBeat (VentureBeat's gaming vertical, now the article's live location — the original venturebeat.com/2013/05/08/... URL 403s), "Diablo III patch glitch results in gold duping that breaks auction house," https://gamesbeat.com/diablo-iii-patch-glitch-results-in-gold-duping-that-breaks-auction-house/; corroborated independently by Polygon, "Blizzard takes Diablo 3 Auction Houses offline after gold dupe bug," https://www.polygon.com/2013/5/8/4311002/blizzard-takes-diablo-3-auction-houses-offline-after-gold-dupe-bug/ and PCGamesN, "Blizzard pull Diablo 3 auction house offline after gold duping bug shakes game economy," https://www.pcgamesn.com/diablo/blizzard-pull-diablo-3-auction-house-offline-after-gold-duping-bug-shakes-game-economy (both fetched/confirmed to cover the same May 2013 incident; neither Engadget nor a "cancelled-transactions" mechanism is supported by any source checked — both details from the prior draft are dropped here as unverified). Note: no source checked states this incident was a cited factor in the RMAH's eventual 2014 shutdown, so that claim is dropped rather than carried forward unsourced.
  **Test-for-us**: Cancel, retry, or double-submit a top-up transaction mid-flight (double-click `onBuy`, replay the same webhook payload, or simulate a cancel-then-resend) and inspect the resulting balance/`currency_transactions` rows for over-crediting — this is the concrete failure shape (numeric edge cases + partial/retried flows over-crediting a currency balance) that the done-criterion-4 idempotency E2E test exists to catch, independent of whether Diablo III's specific root cause was an overflow or a cancellation race.

- **Scar**: RevenueCat's own documentation states its IAP webhooks are only "at-least-once" delivery (not exactly-once), and its community forum has real reports of the same purchase webhook firing more than once — an expected occurrence for any backend granting entitlements/currency off a payment webhook, not a rare edge case. — Source: RevenueCat docs, "Webhooks," https://www.revenuecat.com/docs/integrations/webhooks; RevenueCat Community, "Duplicate webhook call," https://community.revenuecat.com/general-questions-7/duplicate-webhook-call-6034
  **Test-for-us**: Fire the exact same simulated purchase-webhook payload twice (or N times) in quick succession at whatever eventually implements done-criterion 2's grant-from-webhook RPC, and confirm currency is credited exactly once, not "probably once."

This project's own spec — `docs/agent-blueprint/24-monetization-shop-system.md`'s done-criteria 1-5 and `docs/MASTER_BLUEPRINT_v3.0.md` — decides what "correct" looks like here; how Halfbrick/other Android devs, Blizzard, or RevenueCat each chose to fix their own incident is not being prescribed as the answer for this codebase. The precedents above only establish what kind of break to go test for.

---

### Revision log (what changed and why)

1. **Scar 1 (Android IAP)** — the prior draft attributed a "Freedom" tool and a "decompile-and-patch... forged purchase-succeeded signature" mechanism to the Security Boulevard citation. Re-fetched the article directly: it never mentions "Freedom." It names CreeHack, LeoPlaycard, and Lucky Patcher, with Lucky Patcher specifically described as binding the Google Play Billing service to itself to fake a success response. Swapped in the tools/mechanism the source actually documents; the Fruit Ninja/Juice Jam/PandaPop and Prime31/Unity "no server-side validation" claims were independently re-confirmed against the live article and needed no change.
2. **Scar 2 (Diablo III)** — the cited venturebeat.com URL 403s on direct fetch; found and fetched its live successor at gamesbeat.com, which carries the same article. That source: (a) attributes the integer-overflow mechanism to Reddit speculation, not an official Blizzard statement — reworded from asserted fact to "reporting at the time attributed"; (b) explicitly states the widely-repeated "371 trillion gold" figure came from a screenshot Blizzard confirmed was photo-edited/fake — the prior draft's "duplicated gold into the trillions" is not safe to state as fact, so it's now flagged as a debunked figure rather than cited as real; (c) does not mention "cancelled transactions" as the mechanism (the real trigger reported was patch 1.0.8's gold-stack-cap increase from 1M to 10M) — dropped; (d) does not mention Engadget covering this — swapped in Polygon and PCGamesN, both independently fetched and confirmed to cover the same incident, as the actual corroborating sources.
3. **Unsourced RMAH-shutdown claim** — none of the sources checked (GamesBeat, Polygon, PCGamesN) states this dupe incident was a cited factor in the RMAH's 2014 shutdown. Dropped per the citation-check finding rather than given a citation, since no supporting source was found.
4. **Scar 3 (RevenueCat)** — no change; both citations were independently re-confirmed as accurate.
