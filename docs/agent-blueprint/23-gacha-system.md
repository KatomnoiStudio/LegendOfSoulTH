# 23. Gacha System

> Category: Economy · Generated via gold-standard FILL + adversarial CB-lite verify (2 seats), 2026-08-07 · **revised after verify flagged an issue**.
>
> **⚠️ Rate/pity/cost numbers below are stale.** `docs/GACHA-RATE-DESIGN-LOCK.md` (2026-08-13) is now the source of truth for the whole rate domain — `5%/25%/70%`, `pity threshold 30`, `cost 100/900`, and the "hard/soft pity, Genshin-shaped" world-class bar below are all superseded (flat-rate + hard pity, FGO-shaped; see that doc §7.1 reconciliation and §11 for every derived number). This contract's own text has not been re-synced to it yet.

### Scope

Owns: gacha roll resolution (rate table, pity counter, banner cost), converting a roll result into hero-ownership writes, and the config that drives rates/pity/cost. Does **not** own: currency balance mutation itself (that's `accountRepository.ts`'s `appendTransaction`/gold-gem ledger, §8 server-authority), hero stat/kit data (`src/game/characters.ts`), star-ascension math or duplicate-conversion rules (§4.3, owned by the Star Ascension system, #15 in `AGENT_BLUEPRINT.md` line 63 — a sibling of, not the same system as, Hero Collection #13), or the shop/purchase UI for buying roll currency (§7 secondary monetization, System #24). Gacha consumes currency and hero-ownership as dependencies; it does not define either.

**Current state: shipped and live (P9, PR #97).** `MainNavigation.tsx`'s `'summon'` nav item is wired to `onOpenSummon()`, which opens `GachaModal.tsx`; pulls route through `accountRepository.supabase.ts::pullGacha` to the server-authoritative Postgres RPC `perform_gacha_pull` (`supabase/migrations/20260809073000_p9_gacha_server_authority.sql`) — an atomic, request-id-idempotent function covering rate-table-sum validation, hard pity, Gem debit, hero/shard grant, and private pull-history RLS. Two test files cover it (`src/data/gachaAuthority.integration.test.ts`, `src/components/GachaModal/GachaModal.test.tsx`). The migration was applied and verified in production 2026-08-10 (MEMORY.md item 176). `src/game/gacha/gachaEngine.ts`/`gachaService.ts` (a client-side pure-function PRNG resolver from an earlier, since-superseded client-authoritative iteration — see MEMORY.md item 169) was never wired into the live pull path and has been deleted (design-fork 2.b, 2026-08-10) rather than kept — see Done-criteria #1.

### Inputs/Outputs

Shipped, as of P9 (`supabase/migrations/20260809073000_p9_gacha_server_authority.sql`):

- **In:** `p_request_id` (client-generated UUID, idempotency key), `p_banner_id`, `p_pull_count` (1 | 10) passed to the RPC `perform_gacha_pull`; the caller's `uid` comes from `auth.uid()` server-side, never client-supplied. Currency spend routes through `currency_transactions` with `source = 'gacha'` (added to `currency_transactions_source_check`/`currency_source_match` alongside `'quest'|'drop'|'topup'|'coupon'|'admin'`).
- **Out:** `GachaPullResult` (`src/data/accountRepository.shared.ts:86-96`) — one or more `{ characterId, rarity, isPity, isNew, shardsGranted }` results, `cost`, `currencyUsed`, `newPity`, and `replayed` (true when the same `request_id` was already processed). New heroes and duplicate-shard increments both write to `owned_characters` inside the same RPC transaction as the currency debit.
- **Config in:** `gacha_banners` + `gacha_banner_pool` tables, seeded with the live Standard Banner (5 Production Batch 01 heroes, 5%/25%/70% rarity split, pity threshold 30, cost 100 gem single / 900 gem ×10).

### Dependencies

- **hero system** (`src/game/characters.ts`, `Character`/`Rarity` types) — gacha rolls _from_ the hero pool; needs rarity/archetype data to weight against.
- **economy** (`src/data/accountRepository.ts` currency ledger) — gacha spends gold/gems, feeds into `currency_transactions`.
- **backend** (§8 server-authority) — gacha outcome is "valuable data" per §8 line 480; this dependency is already satisfied — `perform_gacha_pull` (`supabase/migrations/20260809073000_p9_gacha_server_authority.sql`) resolves RNG, pity, currency debit, and hero/shard writes entirely server-side (SECURITY DEFINER), not client-trusted.
- Feeds **Star Ascension** (§4.3, System #15) — every roll is an input to star ascension math (owned by that system, not this one).

### Done-criteria

1. Roll output is statistically verified, not deterministically reproduced — a fixed-seed/no-`Math.random()` criterion is incompatible with a server-authoritative RNG a client can never legitimately predict (predictable server rolls are a real-money-adjacent risk surface, not a testing convenience). Verified instead by running N pulls against the live `perform_gacha_pull` RPC (via the PGlite-backed integration harness, `src/data/gachaAuthority.integration.test.ts`) and asserting: the observed rarity distribution stays within tolerance of the banner's configured `drop_rate`s, and pity guarantees the banner's configured `pity_rarity` by `pity_threshold` pulls, every time, with zero exceptions across the run. (Decided as design-fork 2.b, 2026-08-10 — see MEMORY.md; supersedes the deterministic-seed criterion this contract shipped with. `src/game/gacha/gachaEngine.ts`/`gachaService.ts`, the only code that met the old criterion, was dead — never called from the live pull path — and has been deleted, not kept as a reference module, since a client-predictable resolver for a server-authoritative RNG isn't a shape worth preserving.)
2. A roll never succeeds without a matching `currency_transactions` debit of the correct source/amount — verifiable by replaying a test account's transaction log against its `owned_characters` diff.
3. Pity counter writes are serialized by a real Postgres row lock (`perform_gacha_pull` takes `for update` on the player's `profiles` row and, per-banner, on `gacha_pity`), not the client-side multi-tab conflict-reject pattern this criterion originally anticipated — the shipped design supersedes it with server-side atomicity rather than implementing it, since the live gacha path never touches `accountRepository.ts`'s localStorage store (`useAuth.ts` calls `accountRepository.supabase.ts::pullGacha`, which invokes this RPC directly).
4. Rate table sums to 100% per banner at config-load time — a broken/misconfigured table fails a build-time or test-time assertion, never fails silently at runtime.
5. A roll can never produce a `characterId` outside the banner's configured hero pool, and every roll writes exactly one ledger debit and at most the rolled count of `owned_characters` rows — no partial/duplicate writes on retry.

### World-class bar

**Genshin Impact's gacha system (miHoYo/HoYoverse)** — the one concrete pattern worth borrowing: **hard/soft pity with a guaranteed rarity within a bounded pull count** (⚠️ unverified: whether Genshin's live wish UI displays a running numeric pity counter to the player is disputed — that specific UI feature spawned third-party pity-tracking tools/spreadsheets precisely because the in-game display doesn't show it; verify current UI before citing "visible counter" as the differentiator). The soft/hard-pity mechanic itself is accurately Genshin's and is the industry-referenced standard for "gacha the player can trust the fairness of" without requiring drop-rate secrecy. It directly supports this project's §7 "must not sell best power primarily via direct purchase" stance by making the free-to-play pity path a real, bounded floor rather than a marketing footnote — regardless of whether the counter itself is player-visible, the bound should be, at minimum, disclosed in-game text (rules, FAQ, or similar) even if not a live running number.

### Stay-current note

Gacha regulatory disclosure requirements (drop-rate publication, pity disclosure) vary by platform/region and have tightened over time (e.g. mobile storefront policies, some jurisdictions' loot-box laws) — whatever rate/pity numbers P9 locks in should be revisited against current platform policy at implementation time, not assumed stable from this contract's writing date.

### Low-maintenance-cost design

**Rate/pity/cost as one data table, not scattered constants** — mirrors the project's own already-established pattern: `RewardSystem.ts` centralizes its per-enemy reward numbers in a single `TEMPLATE_REWARD` const rather than inline math (`src/game/realtimeBattle/RewardSystem.ts:28`), and §5.1 explicitly locks the same principle for stage difficulty ("data-driven per-stage/per-chapter table... never a hardcoded formula in code," line 421). Gacha should follow suit: one config object (or one JSON/table file) is the single source of truth for rates/pity/cost per banner, tunable without touching roll logic. Per ponytail/YAGNI: do not build a banner-authoring UI, a generic "rules engine," or an abstraction over multiple gacha "modes" until a second banner type actually exists — P9 only needs the one banner shape the blueprint describes.

### Known scars (real historical precedent)

- **Scar**: In December 2022, a server-side bug broke Genshin Impact's in-game Wish History screen ("Connection failed" errors, blank screens, XML-parse errors). HoYoverse's own support response confirmed the underlying pity counter itself was never actually reset — only the _display/record_ of it broke. — Source: gamertweak.com, "Why Is Genshin Impact Wish History Not Updating Or Loading?" (published Dec 20, 2022; quotes HoYoverse support stating the issue "does not affect the pity system"). ⚠️ Single-sourced and unverified this pass: a prior fetch of this URL confirmed the title, date, and quote, but re-fetching it now returns HTTP 403, so it could not be re-checked live in this revision. The scar's previously-cited second source, rickyspears.com, has been dropped entirely — not merely caveated — because (a) it carries a rolling "current as of March 13th, 2026, going on 11 weeks now" framing for a Dec-2022 incident, a signature of an auto-rewritten content-farm page rather than a dateable historical account, and (b) its stated onset date ("around December 28th, 2022") is chronologically impossible against gamertweak.com's Dec 20, 2022 publish date describing the same symptoms as already active — that's a cross-source contradiction, not just an internal inconsistency in rickyspears' own timeline. No replacement corroborating source was found this pass (WebSearch budget exhausted for the session; direct WebFetch to likely candidates did not resolve) — treat the "display bug, not an actual pity reset" fact as resting on gamertweak.com alone until re-verified. The earlier draft's claim that this drove "reports of players avoiding spending or quitting out of that anxiety" has been cut outright: neither cited source states it, and no replacement citation was found, so it was an uncited addition rather than a fact to soften.
  **Test-for-us**: Deliberately corrupt or wipe just the _read/display path_ for the pity counter (e.g. the value shown to the player, or a stale localStorage read) while leaving the actual persisted counter and its writes untouched — then check whether anything downstream (UI, other game logic, or the player with no other way to check) would end up treating "display looks reset" as proof "pity is reset." Also check the reverse: is there any way for a player or QA to positively distinguish "display glitch" from "pity actually reset" without guessing?

- **Scar**: In February 2022 (v2.4, "Lantern Rite" / "Flowing Lights and Colors" event), a mail-configuration bug caused some players to receive two copies of the same reward mail, double-granting them Intertwined Fates — the game's gacha-pull currency. HoYoverse judged that clawing the duplicate back from players who'd already claimed it was worse than the bug itself, so instead of reverting the duplicate, they doubled the intended reward for everyone to match. — Source: reported independently by Sportskeeda ("Genshin Impact accidentally sends 2x Free Intertwined Fates to some players in new event"), Dualshockers, Siliconera, and Destructoid, all covering the same Feb 2022 incident; version number, event name, and resolution were confirmed against destructoid.com in the prior citation-check pass. ⚠️ The earlier draft's "Asia-server players" qualifier has been removed — no reachable source specifies a regional/server scope, and available coverage describes the bug and the fix as applying broadly (all affected players got the doubled reward), so the regional detail was an overspecification not backed by any named source. The v2.4 event title itself ("Flowing Lights and Colors") could not be re-confirmed live this pass (WebSearch exhausted, direct fetch to destructoid.com now 404s) — flagging rather than asserting: it may in fact be "Fleeting Colors in Flight." Re-verify the exact event name before treating it as locked.
  **Test-for-us**: Fire the same reward/grant call twice against our own currency-ledger + roll-resolution path (double-submit, client retry after a timed-out response, replayed/duplicate network request, re-processing the same claim/mail id) and confirm the system rejects or dedupes the second call outright at write time — never lets it land and only gets noticed after the fact. Our design has no "just double it for everyone" escape hatch once a duplicate roll has already resolved into a real `owned_characters` row and a currency debit, so the dedup has to hold _before_ that write, not be a policy decision made after the fact. Note on scope: the "replayed/duplicate network request" scenario specifically targets the future server-authoritative phase of gacha — per this project's own spec (Done-criteria #3), gacha today is client-only/localStorage-backed with no cross-request atomicity to build against (§8 marks server-authoritative as "eventually," not now). The double-submit/client-retry and duplicate-claim-id scenarios apply to the current client-only system now; the network-replay-defense half of this test is a requirement to carry into the server phase, not something to build against today's implementation.

This project's own spec — `docs/agent-blueprint/23-gacha-system.md`'s Done-criteria (deterministic rolls, matching ledger debit, conflict-reject on concurrent writes, no partial/duplicate writes on retry) and `docs/MASTER_BLUEPRINT_v3.0.md` — decides what "correct" looks like here; the exemplar above is cited only for the _shape_ of what broke, not as the fix to copy.
