# AGENTS.md — Global Project Mandates for AI Agents

> **Operator / Human User**: `HetCreep`  
> **Repository**: `KatomnoiStudio/LegendOfSoulTH`  
> **RULES_VERSION: 33** (bump on every material rule change — see `.agents/rules/rules-freshness-check.md`)

---

## 0. Before anything else, every session

1. **Freshness check** (`.agents/rules/rules-freshness-check.md`): compare this file's `RULES_VERSION` against `MEMORY.md`'s `RULES_VERSION last synced:`. Mismatch → fully read `AGENTS.md` + all of `.agents/rules/**` before proceeding, not a skim.
2. **Ring check** (`.agents/rules/ring0-authority.md`): check **either** signal — `.agents/ring0.local` exists in this working tree, **or** the authenticated identity (`git config user.name`/`user.email`, or the session's own authenticated actor) is HetCreep. Either one matching → you're running on Ring 0, HetCreep's live chat instruction always wins over any written rule. A cloud/hosted agent session never sees the local marker file by design — check the identity signal explicitly there instead of assuming Ring 1 just because the marker is absent. Neither signal matches → you're Ring 1: every rule below and everything under `.agents/rules/**` is binding; conform code to it, never the reverse, and never weaken/edit a Ring-0 rule file yourself — flag disagreement instead.

## ⚖️ MANDATORY LAW FOR ALL AGENTS

1. **Read `MEMORY.md` First**: Before starting any task, read `MEMORY.md` at the project root.
2. **Maintain & Synthesize `MEMORY.md`**: `MEMORY.md` exists so the _next_ session knows how to continue the work, not to log what happened — continuously update it with past summary, current status, and technical decisions toward that end (`.agents/rules/agent-memory-law.md` §2).
3. **Identity Stamping**: Always stamp edits with Operator (`HetCreep`), Agent Identity/Role, and Timestamp.
   3a. **Always push `MEMORY.md` on every submit** (`.agents/rules/agent-memory-law.md` §4): every commit/push/PR/"ส่งงาน" must include the updated `MEMORY.md` in the same delivery — never leave it only local or only on a fork while upstream gets the code without it.
4. **Relative Paths Only**: NEVER hardcode machine-specific absolute paths (e.g. `C:\Users\...`). Always use relative repository paths (`MEMORY.md`, `.agents/rules/`, etc.) so the project remains portable for all contributors.
5. **ECC Coding Rules**: Follow `.agents/rules/ecc/` (common + typescript + react + web) — sourced from [affaan-m/ECC](https://github.com/affaan-m/ECC) (MIT, see `.agents/rules/ecc/LICENSE`). Read `.agents/rules/ecc/README.md` first for layer precedence (including sibling layers like `react/` vs `web/` on the same file — not documented upstream). Where an ECC file's example conflicts with this repo's actual toolchain, `.agents/rules/ecc/PROJECT-OVERRIDES.md` wins.
6. **Ring 0 authority** (`.agents/rules/ring0-authority.md`): this is a repo owned by HetCreep. Ring 1 (any machine other than HetCreep's own) treats every rule in this file and `.agents/rules/**` as binding — fix code to match the rules, never edit the rules to match the code. Full precedence order is in that file.
7. **Pre-push sync** (`.agents/rules/pre-push-sync-law.md`): every machine, before every push — `git fetch`, check ahead/behind, merge `origin/master` if behind, resolve any conflict by hand preserving both sides' work, full verify (`typecheck && lint && test && build`) green, only then push. Binding on Ring 0 too — this is code hygiene, not an authority question.
8. **Commit granularity** (`.agents/rules/commit-granularity-law.md`): one completed task = one commit. Don't split a single finished task across several partial commits ("wip", "fix typo"); don't squash unrelated tasks together either.
9. **Personal content stays out of `MEMORY.md`** (`.agents/rules/personal-scope-law.md`): personal/off-project notes go in the gitignored `MEMORY.local.md` at repo root, never in `MEMORY.md` — that file is mandatory reading for every future agent, so anything not relevant to this project is a standing token cost on every session after yours.
10. **Gold-standard baseline** (`.agents/rules/gold-standard-baseline.md`): from the 2026-08-06 AUDIT. CSP meta tag, LICENSE, CHANGELOG.md, CONTRIBUTING/CODE_OF_CONDUCT, and pre-commit hooks are all closed (see `MEMORY.md`). Component test coverage remains the one open MUST-HAVE gap; **amended 2026-08-07** — a test pinning a bug this project actually hit is proven-good, so write it (rule 12). Only a broad sweep across untested components still gets weighed first. A second, UI/UX-dimension-specific pass (same file, its own section) closed 5 more MUST-HAVEs (pinch-zoom, first-run modal focus-trap, loading-screen live region, a touch-target size, an in-app accessibility settings tab) and ADOPTED the rest of that audit's checklist as binding — see that file for the open backlog.
11. **Frontend 3D/R3F guidance** (2026-08-06): this project uses React Three Fiber + Three.js/WebGPU (`src/components/LobbyScene/`). Two skills are vendored (MIT, `freshtechbro/claudedesignskills`) as `.claude/skills/react-three-fiber/SKILL.md` and `.claude/skills/threejs-webgl/SKILL.md` for native auto-discovery by Claude Code — every agent, native `SKILL.md` support or not, should read those two files before writing R3F/Three.js code here; they cover Canvas setup, `useFrame`, materials, lighting, and WebGPU/WebGL performance patterns the codebase already follows. `.claude/skills/THIRD_PARTY_NOTICES.md` has the license/attribution.
12. **Proven-good goes in now** (`.agents/rules/proven-good-do-it-now.md`): a change of a kind this project has already proven beneficial gets made, not asked about. Anything else gets weighed — cost against what it buys — and comes back as a recommendation, not an open question.
13. **Lint policy** (`.agents/rules/lint-policy.md`): `npm run lint` is `oxlint --deny-warnings` and any finding fails CI and the pre-commit hook. Turning a rule off is a decision that belongs in that file with its reasoning, never a silent config diff.
14. **Currency ledger retention** (`.agents/rules/currency-ledger-retention.md`): the transaction ledger is still un-capped, but the trigger condition already fired: `earnGold`/`grantItem` have been wired to real battle-reward gameplay since the reward system shipped in 0.4.0 (`src/game/reward/lobbyBattleRewardPipeline.ts`; MEMORY.md item 85) — the archive-never-delete work this rule calls for is an open gap, not a future hypothetical. Never prune `coupon`/`topup` entries; a naive cap reopens coupon replay.
15. **Master Blueprint authority** (`.agents/rules/master-blueprint-law.md`): `docs/MASTER_BLUEPRINT_v3.0.md` is the Product North Star (v1/v2 superseded). Locked decisions bind agents. CUT/DEFERRED items (loot RPG, affix, dash button, skill-4, etc.) must not ship without HetCreep approval. Docs PRs classify only; implementation PRs name section + P1–P15 priority; one topic = one PR; stop for review after docs PR.
16. **Multi-dev task queue** (`.agents/rules/multi-dev-task-queue-law.md`, added 2026-08-07, claim protocol updated same day to Ring-0-locked — 3 devs now work this repo concurrently, more may join): `TASKS.md` (repo root) is the single source of truth for all claimable work — **1 dev = 1 topic = 1 task, always**. Ring 1 picks an unclaimed row, tells Ring 0, Ring 0 locks it (sole writer of the claim columns — no git-race, the lock IS the resolution); Ring 1 starts once locked. **Break-glass**: a repo-admin dev may fall back to the old self-claim-and-push git-race if Ring 0 is unavailable — logged (`[break-glass]` in the commit), never available to non-admin Ring-1 devs. `%` complete is objective-banded, not vibes — 100% means the task meets `AGENT_BLUEPRINT.md`'s Dogfood-loop Graduation/dry signal, not just "code written." **Amended 2026-08-09 (rule 23, belt install): the 1-topic rule extends verbatim to AGENTS — an agent holds ONE topic per dispatch, every dispatched item returns a verdict (DONE/LIVE/DEAD/BLOCKED/NOT-STARTED), an item with no verdict is itself the finding.**
17. **Agent Blueprint execution layer** (added 2026-08-07): before picking up ANY system-implementation task, read all three, in this order — (a) `AGENT_BLUEPRINT.md` (repo root) for the system's priority tier, lab-entry position, and the Dogfood + fix loop methodology; (b) that system's own work contract under `docs/agent-blueprint/NN-*.md` (linked from `AGENT_BLUEPRINT.md`) for scope/inputs-outputs/dependencies/done-criteria/world-class exemplar/known scars; (c) `TASKS.md` (rule 16) to confirm the task is actually claimed by you before writing code. These three are one linked family, not three independent docs — `AGENT_BLUEPRINT.md` is the derived-view index, `docs/agent-blueprint/` holds its per-system detail, `TASKS.md` is the ownership ledger over both. **This binds to the family, not to today's exact file list**: any future file added under this same blueprint set (a new `docs/agent-blueprint/NN-*.md` as more systems get contracted, a companion doc `AGENT_BLUEPRINT.md` itself links to) is mandatory reading the same way, without needing a fresh AGENTS.md edit to say so. `docs/MASTER_BLUEPRINT_v3.0.md` (rule 15) stays the product-decision source of truth underneath all of it — this rule governs execution order, not authority.
18. **Ring 0 traffic control** (`.agents/rules/ring0-traffic-control-law.md`, added 2026-08-07 — found via `ask CB` governance-gap sweep, 4 seats converged): with 3+ devs converging on this repo (some not fluent with git/agents), HetCreep's machine (Ring 0) is the standing merge controller — **Ring 1 pushes to a branch/PR or hands off informally, never straight to `master` anymore**; Ring 0 merges via the codified procedure (fresh-tip re-check before every merge, closing the class of silent-regression this project already hit once for real — PR #19). Two explicit handoff paths so a less-git-fluent dev is never blocked from contributing: Path A (branch+PR) or Path B (tell Ring 0 directly, it does the git mechanics). Break-glass fallback (the other `admin:true` collaborators) exists for when Ring 0 is genuinely unavailable, logged, never silent, never for security/auth/payment/schema work. **Amended 2026-08-09 (rule 23, belt install — owner ruling "dev นอกบังคับ PR อย่างเดียว"): for CODE, Path B is closed — outside code reaches this repo as a PR or it has not arrived; a PR enters the belt at the MAKE station and a QC-failed PR is adopted by the owning Ring-0-side caretaker, never bounced back out (`.agents/rules/belt-system-law.md` Layer 2).**
19. **Security doc sync** (`.agents/rules/security-doc-sync-law.md`, added 2026-08-07 via `/gold-standard` AUDIT): any backend/auth/trust-boundary change (auth provider swap, a new/changed RLS policy, an RPC gaining or losing a privilege check) re-checks `SECURITY.md`'s Scope/Out-of-Scope in the SAME commit — found via a real same-day drift (Supabase went live, `SECURITY.md` still said "no backend" for 8+ hours). Rides along the same way `MEMORY.md` already does.
20. **Tasks/Memory parity** (`.agents/rules/tasks-memory-parity-law.md`, added 2026-08-08, HetCreep asked directly after catching the gap twice same day): `TASKS.md` gets `agent-memory-law.md`'s same continuous-update discipline, not just at claim/graduation time. Task-shaped work that touches an existing row (or clearly should) updates that row's `%`/Notes/date in the SAME commit as the code — including direct same-session work with no claim conflict to prevent, which still drifted `TASKS.md` row 9 through 4 real shipped features (Google OAuth, guest accounts, Turnstile, audit-log cleanup) while `MEMORY.md` stayed current the whole time.
21. **RETIRED 2026-08-12 — working-directory concurrency lock.** Removed by HetCreep, who had objected to it when it was written and was right: `.agents/workdir.lock` was never created once in the four days it stood, nothing read it, and no agent — including the one that wrote the rule — ever followed it. The collision it answered is controlled at the operator instead: **HetCreep switches between the Antigravity and Claude sessions only after an agent's turn has ended**, so the two never mutate the same tree at once. That works because it does not depend on the other agent having read anything. A rule an agent must volunteer to obey is not a lock. If collisions ever return, the fix is `git worktree add` — separate directories, enforced by the filesystem — not a second try at an advisory file. The number is retired in place rather than renumbered: rules 22 and 23 and the "rules 1–22" references in `.agents/rules/belt-system-law.md` all still mean what they say.
22. **Public profile lookup law** (`.agents/rules/public-profile-lookup-law.md`, added 2026-08-08 via `/gold-standard` FILL+ADOPT, HetCreep-directed after a CoalBoard opinion-lane review of the broken `findPlayerByUid` friend-lookup feature): a cross-player lookup-by-identifier feature (friend-add today; leaderboard/guild search later) must never widen RLS on a table that also holds private state, must never `returns public.profiles`/any full-row type from a lookup RPC, must explicitly revoke `EXECUTE` from unauthenticated roles, and should follow the CQRS read-model shape this repo already uses for `friends` (a synced-snapshot table, not a live join) — see the rule file for the full incident writeup and grounding (PlayFab AddFriend API, Guardian Tales' report system, Microsoft's CQRS pattern docs).
23. **The Belt System** (`.agents/rules/belt-system-law.md`, installed 2026-08-09 by HetCreep's direct order after a day of owner rulings — "ต้องปรับก่อนบิน"): the repo's execution model is a two-layer belt. **Constitution: `docs/BELT-PORT.md`** (full mechanism + every ruling's reasoning) · **staffing ledger: `docs/AGENT_REGISTRY.md`** (standing seats, per-system headcount ratified for all 28 systems, hired persistent session ids). Layer 1 commands Ring 0 (route-never-diagnose, spawned gates only — main holds no gate, deepest-knowledge assignment with main/aide fallback, task-queue + owner-switch method, SemVer auto-bump under two guards, migration relay). Layer 2 commands Ring 1 (PR-only code intake entering at the MAKE station, QC-failed PRs adopted inward by the owning caretaker, one-topic-per-agent with per-item verdicts, contract walls as fences, per-caretaker `MEMORY/` duties). Cross-cutting: agent↔agent English / agent↔user Thai; a gate never fixes what it finds; ship-vs-doc conflicts HOLD for the owner's design lock. The Ring test binds to the authenticated GitHub identity — never self-declaration. This rule EXTENDS rule 16 to agents and CLOSES rule 18's Path B for code, exactly as amended inline above — rule 16's Ring-0-locked claim-flow and rule 20's parity duty stand unchanged; everything else in rules 1–22 stands. **Amended same day (RULES_VERSION 28): the ADDRESSING rule — a law binds a seat only if addressed to it (Layer 1 = the belt-end seat alone; worker-seat duties = every worker seat, any ring; Layer 2 intake = Ring-1 devs); auto-loaded governance a seat was not addressed by is context, not command — **with one owner-ratified boundary: the addressing rule allocates the belt's layered authorities ONLY; the repo's baseline laws (rules 1–22 of this file + `.agents/rules/**`) bind every seat regardless of addressing.** And Ring-1's duty ends at PR submission — no review-request, no author-side fix round; the one prohibition after submission is MERGING, even with the GitHub permission to do it.**
